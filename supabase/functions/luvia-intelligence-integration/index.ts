// Integration Preview receives the shared Luvia Intelligence implementation
// through an independently deployable Edge Function. Production keeps using
// `luvia-intelligence` until the Integration acceptance has passed.
import '../luvia-intelligence/index.ts';

const defaults=['https://myluvia.app','https://www.myluvia.app','https://integration-luvia.njwnrvwbv5.workers.dev','https://luvia.njwnrvwbv5.workers.dev','http://localhost:3000','http://localhost:5500','http://127.0.0.1:5500'];
export function allowedOrigins(){const configured=(Deno.env.get('LUVIA_ALLOWED_ORIGINS')||'').split(',').map(v=>v.trim().replace(/\/$/,'')).filter(Boolean);return [...new Set([...defaults,...configured])];}
export function resolveOrigin(origin:string|null){const normalized=String(origin||'').trim().replace(/\/$/,'');if(!normalized)return allowedOrigins()[0]||'https://myluvia.app';if(allowedOrigins().includes(normalized))return normalized;if(/^https:\/\/[a-z0-9-]+\.github\.io$/i.test(normalized))return normalized;if(/^http:\/\/(localhost|127\.0\.0\.1)(:\d+)?$/i.test(normalized))return normalized;return '';}
export function corsHeaders(origin:string,requestId:string):HeadersInit{return{'Access-Control-Allow-Origin':origin||'null','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type, x-luvia-request-id, x-luvia-client-version, x-luvia-build, x-supabase-api-version, prefer','Access-Control-Allow-Methods':'POST, OPTIONS','Access-Control-Max-Age':'86400','Access-Control-Expose-Headers':'x-luvia-request-id,retry-after','Vary':'Origin','X-Luvia-Request-Id':requestId,'Cache-Control':'no-store'};}

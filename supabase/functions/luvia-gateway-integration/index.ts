// Integration uses the canonical Gateway through a separately deployed entry.
// Authentication, domain owners and provider budgets remain shared contracts;
// the production function keeps its previously accepted implementation.
import '../luvia-gateway/index.ts';

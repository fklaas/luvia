var LuviaTravelOrchestrationCoreV1=(()=>{
'use strict';

const CONTRACT_ID='intelligence.travel-orchestration.v1';
const VERSION='1';
const RUNTIME_VERSION='1.14.1-unicode-visit-write';
const PURPOSES=new Set(['timeline-suggestion','places-ranking','route-planning','weather-context','confirmed-visit','group-decision']);
const WRITE_VERBS=/(?:^|[^\p{L}\p{N}_])(plane|planen|einplane|einplanen|entplane|entplanen|trage|trag|eintragen|fuege|füge|hinzufuege|hinzufüge|hinzufuegen|hinzufügen|entferne|entfernen|buche|buchen|reserviere|reservieren|verschiebe|verschieben|wechsle|wechseln|loesche|lösche|storniere|stornieren|aendere|ändere|korrigiere|korrigieren|aktualisiere|aktualisieren|wiederherstelle|wiederherstellen|zurueckhole|zurückhole|zurueckholen|zurückholen|speichere|speichern|merke|merken|setze|setzen|stelle|einstellen|teile|teilen|aktiviere|aktivieren|deaktiviere|deaktivieren|erstelle|erstellen|stimme|abstimmen)(?=$|[^\p{L}\p{N}_])/iu;
const FORBIDDEN_COMMAND=/\b(ohne\s+(?:meine\s+)?bestätigung|bypass|umgeh(?:e|en)|heimlich|ungefragt|automatisch\s+(?:buchen|stornieren|löschen|teilen)|lösche\s+alles|teile\s+(?:den\s+)?(?:exakten|genauen)\s+standort\s+ungefragt)\b/i;
const MULTILINGUAL_FORBIDDEN_COMMAND=/(?:\bwithout\s+(?:my\s+)?confirmation\b|\bbypass\b|\bsecretly\b|\bsilently\b|\bautomatically\s+(?:book|cancel|delete|share)\b|\bdelete\s+everything\b|\bsin\s+(?:mi\s+)?confirmaci[oó]n\b|\bomite\s+la\s+confirmaci[oó]n\b|\ben\s+secreto\b|\bsans\s+(?:ma\s+)?confirmation\b|\bcontourne\s+la\s+confirmation\b|\ben\s+secret\b|\bsenza\s+(?:la\s+mia\s+)?conferma\b|\baggira\s+la\s+conferma\b|\bsegretamente\b|\bsem\s+(?:a\s+minha\s+)?confirma[cç][aã]o\b|\bzonder\s+(?:mijn\s+)?bevestiging\b)/i;
const TIME_PATTERN=/(?:\b(?:um|gegen)\s*([01]?\d|2[0-3])(?:[:.]([0-5]\d))?\s*(?:uhr)?\b)|(?:\b([01]?\d|2[0-3])[:.]([0-5]\d)\b(?!\.20\d{2}))|(?:\b([01]?\d|2[0-3])\s*uhr\b)/i;
const ALL_TIME_PATTERN=/(?:\b(?:um|gegen)\s*([01]?\d|2[0-3])(?:[:.]([0-5]\d))?\s*(?:uhr)?\b)|(?:\b([01]?\d|2[0-3])[:.]([0-5]\d)\b(?!\.20\d{2}))|(?:\b([01]?\d|2[0-3])\s*uhr\b)/gi;
const DAY_PATTERN=/\b(heute|morgen|uebermorgen|übermorgen|montag|dienstag|mittwoch|donnerstag|freitag|samstag|sonntag)\b/i;
const ISO_DATE_PATTERN=/\b(20\d{2})-(0[1-9]|1[0-2])-([0-2]\d|3[01])\b/;
const DE_DATE_PATTERN=/\b([0-2]?\d|3[01])\.(0?\d|1[0-2])\.(20\d{2})\b/;
const PARTY_PATTERN=/\b(?:für|fuer|mit)\s+(\d{1,2})\s+(?:personen?|leuten?|gästen?|gaesten?)\b/i;
const PARTY_WORD_PATTERN=/\b(?:für|fuer)\s+(?:uns\s+)?(zwei|drei|vier|fünf|fuenf|sechs|sieben|acht|neun|zehn)\b|\bzu\s+(zweit|dritt|viert|fünft|fuenft|sechst|siebt|acht|neunt|zehnt)\b/i;
const PARTY_WORDS=Object.freeze({zwei:2,zweit:2,drei:3,dritt:3,vier:4,viert:4,'fünf':5,fuenf:5,'fünft':5,fuenft:5,sechs:6,sechst:6,sieben:7,siebt:7,acht:8,neun:9,neunt:9,zehn:10,zehnt:10});
const BOOKING_REQUIREMENT_SUBJECT=/\b(?:reservier\w*|reservierung(?:en)?|buch\w*|booking|ticket(?:s)?|eintritt|einlass)\b/i;
const BOOKING_REQUIREMENT_INQUIRY=/\b(?:prüf\w*|pruef\w*|check\w*|klär\w*|klaer\w*|sag\w*|wissen|ob|muss|müssen|muessen|braucht|benötigt|benoetigt|nötig|noetig|notwendig|erforderlich|empfohlen|möglich|moeglich)\b/i;
const EXPLICIT_BOOKING_COMMAND=/\b(?:reserviere|buche)\s+(?:mir|uns|bitte|einen|eine|ein|für|fuer|am|um|bei|im)\b|\b(?:ich|wir)\s+(?:will|wollen|möchte|moechte|möchten|moechten)\b.{0,80}\b(?:reservieren|buchen)\b/i;
const LOCATION_ALLOW=/\b(standort|gps|position|ortung)\b.{0,40}\b(nutzen|teilen|freigeben|erlauben|aktivieren)\b|\b(nutze|teile|erlaube|aktiviere)\b.{0,40}\b(standort|gps|position|ortung)\b/i;
const LOCATION_DENY=/\b(standort|gps|position|ortung)\b.{0,40}\b(nicht|nie|sperren|deaktivieren|widerrufen)\b|\b(kein|keinen|deaktiviere|sperre|widerrufe)\b.{0,40}\b(standort|gps|position|ortung)\b/i;
const DOMAINS=Object.freeze([
  {id:'events',owner:'intelligence',contract:'intelligence.verified-events.v1',availability:'read-only',pattern:/\b(event|events|veranstaltung(?:en)?|festival|konzert(?:e)?|live[- ]?musik|wochenmarkt|aufführung|auffuehrung|spielplan|eventkalender)\b/i},
  {id:'places',owner:'places',contract:'places.v1',availability:'active',pattern:/\b(place|places|restaurants?|restaurant(?:e|en)|café|cafe|essen|strand|meer|museum|kultur|aktivitaet|aktivität|ausflug|fotospot|shopping|nachtleben|nachtclubs?|clubs?|natur|sehenswuerdig|sehenswürdig|ort(?:e|en)?|favorit(?:en)?|lieblingsort(?:e)?|besuch(?:e|en)?|aufenthalt|gps[- ]?moment|mini[ -]?golf|golf|luftmatratze(?:n)?|schwimmring|badespielzeug|geschäft|geschaeft|laden|shop|kaufen|besorgen)\b/i},
  {id:'booking',owner:'booking',contract:'booking.v1',availability:'active',pattern:/\b(buchung(?:en)?|buche|buchen|reservier\w*|stornier\w*|umbuch\w*|tisch|ticket|hotel|provider)\b/i},
  {id:'journey',owner:'journey',contract:'journey.v1',availability:'active',pattern:/\b(timeline|tagesbogen|tagesplan|reiseplan|route|danach|davor|verschieb\w*|plane|planen|einplan\w*|trage|trag|eintrag\w*|hinzufueg\w*|hinzufüg\w*|moment)\b/i},
  {id:'trip',owner:'trip',contract:'trip.v1',availability:'active',pattern:/\b(aktive\s+reise|meine\s+reisen|reisen\s+(?:anzeigen|zeigen|auswählen|auswaehlen)|zeig(?:e|t)?\s+(?:mir\s+)?(?:meine\s+)?reisen|(?:andere|nächste|naechste)\s+reise\s+(?:auswählen|auswaehlen|aktivieren|öffnen|oeffnen)|wechs(?:le|eln?)\s+(?:die|zur|meine)?\s*reise|reise\s+(?:wechseln|auswählen|auswaehlen|aktivieren|verwalten)|reisedaten|reiseziel|reisezeitraum|trip|my\s+trips?|show\s+(?:my\s+)?trips?|switch\s+(?:the\s+)?trip|select\s+(?:another|a|the)?\s*trip|mis\s+viajes|mes\s+voyages|i\s+miei\s+viaggi|mijn\s+reizen|minhas\s+viagens|urlaub\s+(?:umbenennen|ändern|aendern))\b/i},
  {id:'identity',owner:'identity',contract:'identity.v1',availability:'active',pattern:/\b(profil|identität|identitaet|reisekompass|vorliebe|präferenz|praeferenz|vegetar|vegan|barrierefrei|allerg)\b/i},
  {id:'feedback',owner:'intelligence',contract:'intelligence.v1',availability:'active',pattern:/\b(hat|war|fand|gefällt|gefaellt)\b.{0,48}\b(gefallen|gut|toll|super|schön|schoen|schlecht|enttäuschend|enttaeuschend|nicht\s+gefallen)\b|\b(gefällt|gefaellt|mag|liebe)\b.{0,48}\b(das|dies|diesen|diese)\b/i},
  {id:'privacy',owner:'identity',contract:'identity.v1',availability:'active',pattern:/\b(privatsphäre|privatsphaere|privacy|einwilligung|consent|datenfreigabe|standortfreigabe)\b|\b(?:gps|standort|position|ortung)\b.{0,48}\b(?:nicht|nie|widerrufen|freigeben|teilen)\b/i},
  {id:'device-position',owner:'platform',contract:'LocationPort',availability:'active',pattern:/\b(gps|standort|position|ortung|in\s+meiner\s+nähe|in\s+meiner\s+naehe|hier\s+in\s+der\s+nähe)\b/i},
  {id:'collaboration',owner:'collaboration',contract:'collaboration.membership.v1',availability:'foundation',pattern:/\b(abstimmung|abstimmen|stimme|gruppe|gruppenentscheidung|mitreisend|familie|freunde|gemeinsam|einladen|einladung|rolle)\b|\blade\b.{0,40}\bein\b/i},
  {id:'memory',owner:'memory',contract:'memory.v1',availability:'active',pattern:/\b(erinnerung(?:en)?|fotos?|videos?|moment\s+bewahren|stor(?:y|ies)|reels?|fotobuch|alben|album)\b|\bspeicher\w*\b.{0,40}\bmoment\b|\bmoment\b.{0,40}\bspeicher\w*\b/i}
]);
const GOAL_TYPE_DOMAINS=Object.freeze({
  meal:['places'],restaurant:['places'],food:['places'],dining:['places'],place:['places'],places:['places'],visit:['places'],confirmed_visit:['places'],discovery:['places'],activity:['places'],attraction:['places'],culture:['places'],nature:['places'],shopping:['places'],nightlife:['places'],photo:['places'],event:['events'],events:['events'],festival:['events'],concert:['events'],
  booking:['booking'],reservation:['booking'],cancel_booking:['booking'],journey:['journey'],timeline:['journey'],schedule:['journey'],day_plan:['journey'],route:['journey'],trip:['trip'],travel:['trip'],
  identity:['identity'],profile:['identity'],preference:['identity'],preferences:['identity'],feedback:['feedback'],rating:['feedback'],privacy:['privacy'],consent:['privacy'],'location':['device-position'],gps:['device-position'],position:['device-position'],
  collaboration:['collaboration'],group_decision:['collaboration'],invite:['collaboration'],vote:['collaboration'],memory:['memory'],story:['memory'],album:['memory']
});
const WRITE_OPERATIONS=new Set(['add','book','cancel','change','connect','create','delete','disable','enable','invite','modify','plan','remove','reserve','save','set','share','switch','update','vote']);

const clean=value=>String(value??'').trim();
const clamp=(value,min,max)=>Math.max(min,Math.min(max,value));
const iso=value=>{if(value==null||clean(value)==='')return null;const date=new Date(value);return Number.isNaN(date.getTime())?null:date.toISOString()};
const unique=(items,max=48)=>[...new Set(items.map(clean).filter(Boolean))].slice(0,max);
function immutable(value){if(value==null||typeof value!=='object')return value;if(Array.isArray(value))return Object.freeze(value.map(immutable));return Object.freeze(Object.fromEntries(Object.entries(value).map(([key,item])=>[key,immutable(item)])))}
function stable(value){if(value==null||typeof value!=='object')return JSON.stringify(value);if(Array.isArray(value))return`[${value.map(stable).join(',')}]`;return`{${Object.keys(value).sort().map(key=>`${JSON.stringify(key)}:${stable(value[key])}`).join(',')}}`}
function digest(value){const source=stable(value);let hash=2166136261;for(let index=0;index<source.length;index++){hash^=source.charCodeAt(index);hash=Math.imul(hash,16777619)}return`fnv1a-${(hash>>>0).toString(16).padStart(8,'0')}`}

function clausesFor(message){
  const parts=clean(message).split(/\s+(und\s+danach|danach|anschließend|anschliessend|außerdem|ausserdem|sowie|oder)\s+|[;\n]+/i),clauses=[];let relation='parallel';
  for(const part of parts){const value=clean(part);if(!value)continue;if(/^(?:und\s+danach|danach|anschließend|anschliessend)$/i.test(value)){relation='after';continue}if(/^(?:außerdem|ausserdem|sowie|oder)$/i.test(value)){relation='parallel';continue}clauses.push({text:value,relation:clauses.length?relation:'root'});relation='parallel'}
  return clauses;
}
const DAY_INDEX=Object.freeze({sonntag:0,montag:1,dienstag:2,mittwoch:3,donnerstag:4,freitag:5,samstag:6});
function dateKey(value){const date=new Date(value);return Number.isNaN(date.getTime())?null:`${date.getUTCFullYear()}-${String(date.getUTCMonth()+1).padStart(2,'0')}-${String(date.getUTCDate()).padStart(2,'0')}`}
function exactDate(source){const isoMatch=source.match(ISO_DATE_PATTERN);if(isoMatch){const value=`${isoMatch[1]}-${isoMatch[2]}-${isoMatch[3]}`;return dateKey(`${value}T12:00:00Z`)===value?value:null}const deMatch=source.match(DE_DATE_PATTERN);if(!deMatch)return null;const value=`${deMatch[3]}-${String(Number(deMatch[2])).padStart(2,'0')}-${String(Number(deMatch[1])).padStart(2,'0')}`;return dateKey(`${value}T12:00:00Z`)===value?value:null}
function resolveDay(day,context={}){
  const token=clean(day).toLocaleLowerCase('de-DE').replace('uebermorgen','übermorgen');if(!token)return null;
  const trip=context.trip||{},start=dateKey(trip.startDate||trip.start_date),end=dateKey(trip.endDate||trip.end_date);
  if(DAY_INDEX[token]!=null&&start&&end){for(let cursor=new Date(`${start}T12:00:00Z`),last=new Date(`${end}T12:00:00Z`);cursor<=last;cursor=new Date(cursor.getTime()+86400000))if(cursor.getUTCDay()===DAY_INDEX[token])return dateKey(cursor)}
  if(['heute','morgen','übermorgen'].includes(token)){const anchor=new Date(context.now||Date.now());if(Number.isNaN(anchor.getTime()))return null;const offset=token==='heute'?0:token==='morgen'?1:2,dayDate=dateKey(new Date(Date.UTC(anchor.getUTCFullYear(),anchor.getUTCMonth(),anchor.getUTCDate()+offset,12)));if(start&&end&&(dayDate<start||dayDate>end))return null;return dayDate}
  return null;
}
function normalizedTime(match){if(!match)return null;const hour=match[1]??match[3]??match[5],minute=match[2]??match[4]??0;return hour==null?null:`${String(Number(hour)).padStart(2,'0')}:${String(Number(minute)).padStart(2,'0')}`}
function timeHint(source,context={}){const time=source.match(TIME_PATTERN),day=source.match(DAY_PATTERN),explicitDate=exactDate(source),dayToken=day?.[1]?.toLocaleLowerCase('de-DE')||null,date=explicitDate||resolveDay(dayToken,context);return{day:dayToken,date,time:normalizedTime(time),explicit:Boolean(day||explicitDate||time)}}
function allTimes(source){const times=[];for(const match of source.matchAll(ALL_TIME_PATTERN))times.push(normalizedTime(match));return unique(times)}
function categoryHints(source){const hints=[];for(const [id,pattern] of [
  ['food',/\b(restaurants?|restaurant(?:e|en)|café|cafe|essen|vegetar\w*|vegan\w*|genuss)\b/i],['culture',/\b(museum|kultur|geschichte|galerie|theater|konzert|event)\b/i],['nature',/\b(natur|strand|meer|wald|park|ruhe|erholung)\b/i],
  ['activity',/\b(aktivitaet|aktivität|ausflug|sport|schwimm|rad|wander|mini[ -]?golf|golf|kletter|reiten|spielen)\b/i],['photo',/\b(foto|fotospot|aussicht)\b/i],['nightlife',/\b(nachtleben|club|bar|abend)\b/i],['shopping',/\b(shopping|einkauf|markt|kaufen|besorgen|geschäft|geschaeft|laden|shop|luftmatratze(?:n)?|schwimmring|badespielzeug)\b/i]
])if(pattern.test(source))hints.push(id);return hints}
function preferencePatch(source){
  const patch={},negativeDiet=/\b(?:nicht|kein(?:e|en)?|ohne)\s+(?:mehr\s+)?(?:vegan|vegetar)\w*\b/i.test(source),dietary=[];
  if(!negativeDiet&&/\bvegan\w*\b/i.test(source))dietary.push('vegan');
  if(!negativeDiet&&/\bvegetar\w*\b/i.test(source))dietary.push('vegetarian');
  if(dietary.length)patch.dietaryPreferences=dietary;
  if(/\b(?:entspannt|ruhig|gemütlich|gemuetlich)\b/i.test(source))patch.travelPace='relaxed';else if(/\b(?:ausgewogen|balanciert|mittel(?:schnell)?)\b/i.test(source))patch.travelPace='balanced';else if(/\b(?:aktiv|zügig|zuegig|viel erleben)\b/i.test(source))patch.travelPace='active';
  if(/\b(?:günstig|guenstig|sparsam|kleines budget|niedriges budget)\b/i.test(source))patch.budgetPreference='low';else if(/\b(?:mittleres budget|mittelpreisig)\b/i.test(source))patch.budgetPreference='medium';else if(/\b(?:premium|luxus|großes budget|grosses budget)\b/i.test(source))patch.budgetPreference='premium';
  const interests=[];if(/\b(?:kultur|museum|geschichte)\b/i.test(source))interests.push('culture');if(/\b(?:natur|wandern|strand|meer)\b/i.test(source))interests.push('nature');if(/\bshopping\b/i.test(source))interests.push('shopping');if(interests.length)patch.travelInterests=interests;
  return patch;
}
function explicitDestination(source){const match=clean(source).match(/\b(?:in|rund\s+um)\s+([\p{Lu}ÄÖÜ][\p{L}\p{M}'’.-]*(?:\s+[\p{Lu}ÄÖÜ][\p{L}\p{M}'’.-]*){0,2})/u);return clean(match?.[1]).replace(/[.,;:!?]+$/,'')||null}
function entityHints(source){const party=source.match(PARTY_PATTERN),partyWord=source.match(PARTY_WORD_PATTERN),partyToken=clean(partyWord?.[1]||partyWord?.[2]).toLocaleLowerCase('de-DE'),namedTrip=/\b(?:zur|die)\s+reise\s+(?!wechseln\b|ändern\b|aendern\b)[\p{L}\d][\p{L}\d .'-]{1,80}/iu.test(source);return{partySize:party?Number(party[1]):PARTY_WORDS[partyToken]||null,destinationName:explicitDestination(source),hasNamedTarget:/[„“"'][^„“"']{2,80}[„“"']/.test(source)||namedTrip,hasChoice:/\b(?:oder|zwischen)\b/i.test(source),preferencePatch:preferencePatch(source)}}
function bookingRequirementRead(source,operation=''){
  const text=clean(source),semantic=clean(operation).toLowerCase().replace(/[\s-]+/g,'_');
  if(['read','list','search','compare','check','inspect','check_requirement','check_reservation_requirement'].includes(semantic)&&BOOKING_REQUIREMENT_SUBJECT.test(text))return true;
  if(EXPLICIT_BOOKING_COMMAND.test(text))return false;
  return BOOKING_REQUIREMENT_SUBJECT.test(text)&&BOOKING_REQUIREMENT_INQUIRY.test(text)&&(/\bob\b/i.test(text)||/\b(?:muss|müssen|muessen|braucht|benötigt|benoetigt|nötig|noetig|notwendig|erforderlich|empfohlen|möglich|moeglich)\b/i.test(text));
}
function requiredInputs(domain,mode,clause,hint,entities,operation=''){
  if(mode!=='propose-write')return domain.id==='device-position'&&!/\b(?:in\s+meiner\s+nähe|in\s+meiner\s+naehe|hier)\b/i.test(clause)?['purpose']:[];
  if(domain.id==='booking'){
    const semantic=clean(operation).toLowerCase().replace(/[\s-]+/g,'_'),cancel=['cancel','storno','stornieren'].includes(semantic)||/\bstornier\w*|\bcancel\w*/i.test(clause),modify=['change','modify','update','rebook','umbuchen'].includes(semantic)||/\b(?:änder|aender|umbuch|verschieb)\w*/i.test(clause);
    if(cancel)return[];
    if(modify)return[hint.date||hint.time||entities.partySize?null:'booking-change'].filter(Boolean);
    return[!entities.hasNamedTarget&&!/\b(?:restaurant|hotel|ticket|tisch|museum|aktivität|aktivitaet|attraktion|event|veranstaltung|tour)\b/i.test(clause)?'bookable-target':null,!hint.date?'date':null,!hint.time?'time':null,!entities.partySize?'party-size':null].filter(Boolean);
  }
  if(domain.id==='journey'){
    const removal=['delete','remove','unplan'].includes(clean(operation).toLowerCase())||/\b(?:entfern|entplan|lösch|loesch|remove|unplan)\w*/i.test(clause);
    if(removal)return[];
    return[!hint.date?'date':null,!hint.time?'time-or-open-period':null,!/\b(?:restaurant|ort|place|museum|strand|route|moment|besuch|aufenthalt|tagesplan|abend|evening|morgen|morning|nachmittag|afternoon|tag|day)\b/i.test(clause)?'journey-target':null].filter(Boolean);
  }
  if(domain.id==='trip')return[/\bwechsel/i.test(clause)&&!entities.hasNamedTarget?'target-trip':null].filter(Boolean);
  if(domain.id==='identity')return[Object.keys(entities.preferencePatch||{}).length?null:'preference-change'].filter(Boolean);
  if(domain.id==='privacy')return[/\b(?:teile|teilen|freigebe|freigeben|aktiviere|aktivieren|deaktiviere|deaktivieren|widerrufe|widerrufen)\b/i.test(clause)?null:'privacy-setting',/\b(?:standort|gps|position|ortung)\b/i.test(clause)?'position-purpose':null].filter(Boolean);
  if(domain.id==='device-position')return['explicit-position-grant','purpose'];
  if(domain.id==='collaboration')return[/\babstimm/i.test(clause)&&!entities.hasChoice?'decision-options':null,/\b(?:stimme|abstimm)\b/i.test(clause)&&!entities.hasNamedTarget?'selected-choice':null].filter(Boolean);
  if(domain.id==='memory')return[!/\b(?:foto|video|moment|story|reel|fotobuch|album)\b/i.test(clause)?'memory-source-or-target':null].filter(Boolean);
  return[];
}
function likelyDateInTrip(requested,start,end){const parts=clean(requested).split('-').map(Number);if(parts.length!==3)return null;for(let year=new Date(start).getUTCFullYear();year<=new Date(end).getUTCFullYear();year++){const candidate=`${year}-${String(parts[1]).padStart(2,'0')}-${String(parts[2]).padStart(2,'0')}`,parsed=Date.parse(`${candidate}T00:00:00Z`);if(Number.isFinite(parsed)&&parsed>=Date.parse(`${start}T00:00:00Z`)&&parsed<=Date.parse(`${end}T23:59:59Z`))return candidate}return null}
function conflictSet(text,intents,offline,context={}){
  const conflicts=[];
  if(LOCATION_ALLOW.test(text)&&LOCATION_DENY.test(text))conflicts.push({code:'position-consent-conflict',message:'Der Wunsch erlaubt und verweigert Standortzugriff zugleich.',intentIds:intents.filter(item=>['device-position','privacy'].includes(item.domain)).map(item=>item.id)});
  if(/\b(?:buche|reserviere)\b/i.test(text)&&/\b(?:storniere|lösche)\b/i.test(text))conflicts.push({code:'booking-lifecycle-conflict',message:'Buchung und Stornierung stehen im selben Wunsch ohne eindeutiges Ziel.',intentIds:intents.filter(item=>item.domain==='booking').map(item=>item.id)});
  for(const intent of intents){const times=allTimes(intent.clause);if(times.length>1)conflicts.push({code:'multiple-time-values',message:`Für eine Teilaufgabe sind mehrere Zeiten genannt: ${times.join(' / ')}.`,intentIds:[intent.id]})}
  const trip=context.trip||{},tripStart=clean(trip.startDate||trip.start_date),tripEnd=clean(trip.endDate||trip.end_date);if(/^20\d{2}-\d{2}-\d{2}$/.test(tripStart)&&/^20\d{2}-\d{2}-\d{2}$/.test(tripEnd)){for(const intent of intents){if(intent.mode!=='propose-write')continue;const date=intent.temporalHint?.date;if(!date||date>=tripStart&&date<=tripEnd)continue;const suggested=likelyDateInTrip(date,tripStart,tripEnd);conflicts.push({code:'date-outside-active-trip',message:`${date.split('-').reverse().join('.')} liegt außerhalb dieser Reise (${tripStart.split('-').reverse().join('.')}–${tripEnd.split('-').reverse().join('.')}).${suggested?` Meintest du ${suggested.split('-').reverse().join('.')}?`:''}`,intentIds:[intent.id],requestedDate:date,suggestedDate:suggested})}}
  if(offline&&intents.some(item=>item.domain==='booking'&&item.mode==='propose-write'))conflicts.push({code:'offline-external-command',message:'Externe Booking-Befehle bleiben offline blockiert; der Entwurf kann lokal erhalten bleiben.',intentIds:intents.filter(item=>item.domain==='booking').map(item=>item.id)});
  return conflicts;
}
function canonicalIntentText(value){return clean(value).normalize('NFKD').replace(/[\u0300-\u036f]/g,'').toLocaleLowerCase('de-DE').replace(/[^\p{L}\p{N}]+/gu,' ').trim()}
function canonicalizeIntents(intents=[]){
  const exact=new Map();
  for(const intent of intents){const key=[intent.domain,intent.mode,canonicalIntentText(intent.clause),intent.temporalHint?.date||'',intent.temporalHint?.time||''].join('|');if(!exact.has(key))exact.set(key,intent)}
  const deduped=[...exact.values()],placeTimelineWrites=new Set(deduped.filter(intent=>intent.domain==='places'&&intent.mode==='propose-write'&&/\b(?:timeline|reiseplan|tagesplan|plane|planen|plan|einplan\w*|eintrag\w*|trage|trag|hinzufueg\w*|hinzufüg\w*|schedule|add)\b/i.test(intent.clause)).map(intent=>canonicalIntentText(intent.clause))),filtered=deduped.filter(intent=>!(intent.domain==='journey'&&intent.mode==='propose-write'&&placeTimelineWrites.has(canonicalIntentText(intent.clause))));
  const sequenceByClause=new Map();let nextSequence=0;
  return filtered.map(intent=>{const clauseKey=canonicalIntentText(intent.clause)||`sequence-${intent.sequence}`;if(!sequenceByClause.has(clauseKey))sequenceByClause.set(clauseKey,++nextSequence);const sequence=sequenceByClause.get(clauseKey);return{...intent,id:`intent-${sequence}-${intent.domain}`,sequence,relation:sequence===1?'root':intent.relation||'after',dependencies:sequence===1?[]:[`clause-${sequence-1}`]}});
}
function graphFromIntents(text,intents,context={},meta={}){
  const offline=context.offline===true||context.online===false,uniqueIntents=canonicalizeIntents(intents),blockedCommands=[];
  if(FORBIDDEN_COMMAND.test(text)||MULTILINGUAL_FORBIDDEN_COMMAND.test(text))blockedCommands.push({code:'confirmation-bypass-forbidden',message:'Luvia führt keine stille, ungefragte oder bestätigungslose Mutation aus.'});
  for(const intent of uniqueIntents)if(intent.mode==='propose-write'&&intent.ownerAvailability!=='active')blockedCommands.push({code:'owner-command-not-productized',message:`${intent.ownerContract} ist noch nicht als produktiver Mutationspfad verfügbar.`,intentIds:[intent.id]});
  const confidence=Number(meta.dialogueConfidence),semanticLowConfidence=meta.compilerSource==='openai-structured'&&Number.isFinite(confidence)&&confidence<.72,conflicts=conflictSet(text,uniqueIntents,offline,context),missingInputs=uniqueIntents.flatMap(intent=>intent.missingInputs.map(input=>({intentId:intent.id,domain:intent.domain,input}))),status=blockedCommands.length?'blocked':conflicts.length?'conflicted':missingInputs.length||semanticLowConfidence?'needs-clarification':uniqueIntents.length?'compiled':'unresolved';
  const ownerRoutes=unique([...new Set(uniqueIntents.map(intent=>intent.ownerContract))]),steps=uniqueIntents.map(intent=>({intentId:intent.id,sequence:intent.sequence,relation:intent.relation,owner:intent.owner,ownerContract:intent.ownerContract,mode:intent.mode,status:intent.missingInputs.length?'needs-input':intent.ownerAvailability==='active'?'ready':'owner-foundation',requiresConfirmation:intent.requiresConfirmation}));
  return immutable({contractId:CONTRACT_ID,kind:'intent-graph',messageHash:digest(text),locale:clean(context.locale)||'de-DE',compiledAt:iso(context.now)||null,connectivity:offline?'offline':'online',status,intents:uniqueIntents,ownerRoutes,steps,missingInputs,conflicts,blockedCommands,requiresConfirmation:uniqueIntents.some(intent=>intent.requiresConfirmation),readPlan:steps.filter(step=>step.mode==='read'),mutationPlan:steps.filter(step=>step.mode==='propose-write'),automaticMutation:false,rawMessageStored:false,compilerSource:clean(meta.compilerSource)||'deterministic',dialogueConfidence:Number.isFinite(Number(meta.dialogueConfidence))?clamp(Number(meta.dialogueConfidence),0,1):null,dialogueSummary:meta.dialogueSummary?{headline:clean(meta.dialogueSummary.headline),intro:clean(meta.dialogueSummary.intro)}:null,followUpQuestion:meta.followUpQuestion?.text?{text:clean(meta.followUpQuestion.text),reason:clean(meta.followUpQuestion.reason)}:semanticLowConfidence?{text:'Ich bin noch nicht sicher, welche Luvia-Funktion du meinst. Möchtest du etwas finden, öffnen oder verändern?',reason:'Die Bedeutung war nicht eindeutig genug für eine sichere Owner-Zuordnung.'}:null});
}
function compileIntent(message,context={}){
  const text=clean(message),empty={contractId:CONTRACT_ID,kind:'intent-graph',messageHash:digest(''),intents:[],ownerRoutes:[],requiresConfirmation:false,status:'empty',automaticMutation:false,rawMessageStored:false};if(!text)return immutable(empty);
  const clauses=clausesFor(text),intents=[],offline=context.offline===true||context.online===false;
  clauses.forEach((entry,index)=>{const matched=DOMAINS.filter(domain=>domain.pattern.test(entry.text)),whole=clauses.length===1?DOMAINS.filter(domain=>domain.pattern.test(text)):[],domains=matched.length?matched:whole,operation=/(?:^|[^\p{L}\p{N}_])(?:wiederherstell|restore|zurückhol|zurueckhol)\w*|\bwieder\s+her\b/iu.test(entry.text)?'restore':/(?:^|[^\p{L}\p{N}_])(?:entfern|entplan|lösch|loesch|remove|unplan)\w*/iu.test(entry.text)?'remove':/(?:^|[^\p{L}\p{N}_])(?:korrig|änder|aender|verschieb|update|change)\w*/iu.test(entry.text)?'update':/\b(?:favorit|merk|speicher)\w*/i.test(entry.text)?'save':/\b(?:plane|planen|einplan|eintrag|trage|trag|hinzufueg|hinzufüg|schedule|add)\w*/i.test(entry.text)?'plan':null,requirementRead=bookingRequirementRead(entry.text,operation);for(const domain of domains){const requestedWrite=WRITE_VERBS.test(entry.text)&&!requirementRead,hint=timeHint(entry.text,context),entities={...entityHints(entry.text),bookingRequirementRead:requirementRead},tripChoiceWithoutTarget=domain.id==='trip'&&requestedWrite&&/\b(?:wechsel|auswähl|auswaehl|aktivier)\w*/i.test(entry.text)&&!entities.hasNamedTarget,write=requestedWrite&&!tripChoiceWithoutTarget,missing=requiredInputs(domain,write?'propose-write':'read',entry.text,hint,entities,operation);intents.push({id:`intent-${index+1}-${domain.id}`,sequence:index+1,domain:domain.id,owner:domain.owner,ownerContract:domain.contract,ownerAvailability:domain.availability,mode:write?'propose-write':'read',requiresConfirmation:write,clause:entry.text,relation:entry.relation,categoryHints:domain.id==='places'?categoryHints(entry.text):[],temporalHint:hint,entityHints:entities,missingInputs:missing,dependencies:index&&entry.relation==='after'?[`clause-${index}`]:[],automaticMutation:false,semanticOperation:requirementRead?'check_requirement':operation})}});
  return graphFromIntents(text,intents,{...context,offline},{compilerSource:'deterministic'});
}

function dialogueConstraints(dialogue,goal){return[...(Array.isArray(dialogue?.hardConstraints)?dialogue.hardConstraints:[]),...(Array.isArray(dialogue?.softPreferences)?dialogue.softPreferences:[]),...(Array.isArray(goal?.hardConstraints)?goal.hardConstraints:[]),...(Array.isArray(goal?.softPreferences)?goal.softPreferences:[])].map(item=>({key:clean(item?.key).toLowerCase().replace(/[\s-]+/g,'_'),value:clean(item?.value).toLowerCase(),label:clean(item?.label)})).filter(item=>item.key||item.value||item.label).slice(0,32)}
function dialogueOperation(constraints){const item=constraints.find(entry=>['action','command','intent','operation','requested_operation'].includes(entry.key));return clean(item?.value).toLowerCase().replace(/[\s-]+/g,'_')}
function dialogueDomains(goal,constraints,message=''){
  const type=clean(goal?.type).toLowerCase().replace(/[\s-]+/g,'_'),operation=dialogueOperation(constraints),direct=GOAL_TYPE_DOMAINS[type]||[],domains=[...direct],semanticSource=[message,goal?.label,...constraints.map(item=>`${item.key} ${item.value} ${item.label}`)].join(' ');
  // A structured model may call a journey a "journey" even when the requested
  // operation is the application control "switch/select trip".  The model is
  // still the primary meaning compiler; this invariant only prevents that
  // explicit control operation from inheriting Journey's date/time fields.
  const explicitTripSelection=['switch','select'].includes(operation)&&(/\b(?:trip|reise|journey)\b/i.test(semanticSource)||constraints.some(item=>/trip|journey|reise/.test(item.key)));
  if(explicitTripSelection)return['trip'];
  if(['book','reserve','cancel'].includes(operation)&&!domains.includes('booking'))domains.push('booking');if(['add','plan','schedule'].includes(operation)&&!domains.includes('journey'))domains.push('journey');if(!domains.length)domains.push(...DOMAINS.filter(domain=>domain.pattern.test(semanticSource)).map(domain=>domain.id));return unique(domains,8)
}
function namedDialogueTarget(constraints=[]){
  const generic=/^(?:another|other|different|next|current|active|any|one|eine andere|andere|nächste|naechste|aktuelle|irgendeine|eine)$/i;
  return constraints.find(item=>/target|place|provider|restaurant|hotel|event|trip/.test(item.key)&&item.value&&!generic.test(clean(item.value)));
}
function namedDialogueDestination(constraints=[]){
  const generic=/^(?:here|nearby|current|active|any|hier|in der nähe|in der naehe|aktuell|beliebig)$/i;
  return constraints.find(item=>/^(?:destination|destination_name|city|locality|town|reiseort|reiseziel)$/.test(item.key)&&item.value&&!generic.test(clean(item.value)));
}
function dialogueCategoryHints(goal,constraints){const type=clean(goal?.type).toLowerCase().replace(/[\s-]+/g,'_'),values=constraints.map(item=>`${item.key}:${item.value}`).join(' '),hints=[];if(['meal','restaurant','food','dining'].includes(type)||/dietary|cuisine|restaurant|meal|food/.test(values))hints.push('food');if(['activity','attraction'].includes(type))hints.push('activity');if(['culture','nature','shopping','nightlife','photo'].includes(type))hints.push(type);return unique(hints.length?hints:categoryHints(clean(goal?.label)),8)}
function dialoguePreferencePatch(constraints){const patch={},dietary=[],interests=[];for(const item of constraints){const key=item.key,value=item.value;if(/diet/.test(key)){if(/vegan/.test(value))dietary.push('vegan');if(/vegetar/.test(value))dietary.push('vegetarian')}if(/pace/.test(key)&&['relaxed','balanced','active'].includes(value))patch.travelPace=value;if(/budget/.test(key)&&['low','medium','premium'].includes(value))patch.budgetPreference=value;if(/interest/.test(key)&&['culture','nature','shopping'].includes(value))interests.push(value)}if(dietary.length)patch.dietaryPreferences=unique(dietary);if(interests.length)patch.travelInterests=unique(interests);return patch}
function dialogueTemporalHint(goal,constraints=[],context={}){const timeWindow=goal?.timeWindow||{},constraintSource=(Array.isArray(constraints)?constraints:[]).filter(item=>/(?:^|_)(?:date|day|time|start|end)(?:_|$)/.test(clean(item?.key))).flatMap(item=>[item.key,item.value,item.label]),source=[goal?.label,timeWindow.label,timeWindow.start,timeWindow.end,...constraintSource].map(clean).filter(Boolean).join(' '),hint=timeHint(source,context),start=clean(timeWindow.start),date=start.match(/\b(20\d{2}-\d{2}-\d{2})(?=T|\s|$)/)?.[1]||hint.date,time=start.match(/[T\s]([01]\d|2[0-3]):([0-5]\d)/)?.slice(1,3).join(':')||hint.time;return{day:hint.day,date:date||null,time:time||null,explicit:Boolean(timeWindow.label||start||timeWindow.end||constraintSource.length||hint.explicit)}}
function compileDialogue(message,dialogue={},context={}){
  const text=clean(message),goals=Array.isArray(dialogue?.goals)?dialogue.goals.slice(0,8):[];if(!text||!goals.length)return compileIntent(text,context);
  const sharedDestination=explicitDestination(text);
  const intents=[];
  goals.forEach((goal,index)=>{const constraints=dialogueConstraints(dialogue,goal),operation=dialogueOperation(constraints),domains=dialogueDomains(goal,constraints,text),clause=[clean(goal.label),...constraints.map(item=>item.label||item.value)].filter(Boolean).reduce((parts,item)=>parts.some(part=>clean(part).toLocaleLowerCase().includes(clean(item).toLocaleLowerCase()))?parts:[...parts,item],[]).join(' · ')||text,hint=dialogueTemporalHint(goal,constraints,context),baseEntities=entityHints(clause),requirementRead=bookingRequirementRead([text,clause].join(' '),operation),preference={...baseEntities.preferencePatch,...dialoguePreferencePatch(constraints)},partyConstraint=constraints.find(item=>/party|participant|people|person/.test(item.key)),targetConstraint=namedDialogueTarget(constraints),destinationConstraint=namedDialogueDestination(constraints),choiceConstraint=constraints.find(item=>/choice|option/.test(item.key)&&item.value),entities={...baseEntities,partySize:Number(partyConstraint?.value)||baseEntities.partySize,destinationName:clean(destinationConstraint?.value)||baseEntities.destinationName||sharedDestination||null,hasNamedTarget:Boolean(targetConstraint)||baseEntities.hasNamedTarget,targetName:clean(targetConstraint?.value)||null,hasChoice:Boolean(choiceConstraint)||baseEntities.hasChoice,preferencePatch:preference,bookingRequirementRead:requirementRead};for(const domainId of domains){const domain=DOMAINS.find(item=>item.id===domainId);if(!domain)continue;const local=compileIntent(clause,context).intents.find(item=>item.domain===domainId),requestedWrite=(local?.mode==='propose-write'||WRITE_OPERATIONS.has(operation))&&!requirementRead,tripChoiceWithoutTarget=domainId==='trip'&&['switch','select','open'].includes(operation)&&!entities.hasNamedTarget,write=requestedWrite&&!tripChoiceWithoutTarget,validationClause=[clause,write?'plane':'',domainId==='booking'?'restaurant tisch':domainId==='journey'?'tagesplan place':domainId==='identity'?'vorliebe':domainId==='privacy'?'standort freigeben':domainId==='collaboration'?'abstimmung':domainId==='memory'?'moment':''].join(' '),missing=requiredInputs(domain,write?'propose-write':'read',validationClause,hint,entities,operation);intents.push({id:`intent-${index+1}-${domain.id}`,sequence:index+1,domain:domain.id,owner:domain.owner,ownerContract:domain.contract,ownerAvailability:domain.availability,mode:write?'propose-write':'read',requiresConfirmation:write,clause,relation:index?'after':'root',categoryHints:domain.id==='places'?dialogueCategoryHints(goal,constraints):[],temporalHint:hint,entityHints:entities,missingInputs:missing,dependencies:index?[`clause-${index}`]:[],automaticMutation:false,semanticGoalType:clean(goal.type)||'open',semanticOperation:requirementRead?'check_requirement':operation||null})}});
  return graphFromIntents(text,intents,context,{compilerSource:'openai-structured',dialogueConfidence:dialogue.confidence,dialogueSummary:dialogue.summary,followUpQuestion:dialogue.followUpQuestion});
}
function sliceIntentGraph(compiled,sequence){
  if(compiled?.contractId!==CONTRACT_ID)return immutable({contractId:CONTRACT_ID,kind:'intent-graph',status:'unresolved',intents:[],ownerRoutes:[],steps:[],missingInputs:[],conflicts:[],blockedCommands:[],requiresConfirmation:false,automaticMutation:false,rawMessageStored:false});
  const intents=(compiled.intents||[]).filter(intent=>Number(intent.sequence)===Number(sequence)),ids=new Set(intents.map(intent=>intent.id)),applies=item=>!item.intentIds?.length||item.intentIds.some(id=>ids.has(id)),missingInputs=(compiled.missingInputs||[]).filter(item=>ids.has(item.intentId)),conflicts=(compiled.conflicts||[]).filter(applies),blockedCommands=(compiled.blockedCommands||[]).filter(applies),steps=(compiled.steps||[]).filter(step=>ids.has(step.intentId)),status=blockedCommands.length?'blocked':conflicts.length?'conflicted':missingInputs.length?'needs-clarification':intents.length?'compiled':'unresolved';
  return immutable({...compiled,status,intents,ownerRoutes:unique(intents.map(intent=>intent.ownerContract)),steps,missingInputs,conflicts,blockedCommands,requiresConfirmation:intents.some(intent=>intent.requiresConfirmation),readPlan:steps.filter(step=>step.mode==='read'),mutationPlan:steps.filter(step=>step.mode==='propose-write'),sequenceScope:Number(sequence)});
}
function sequencePlan(compiled){return immutable(unique((compiled?.intents||[]).map(intent=>String(intent.sequence))).map(value=>{const sequence=Number(value),intents=(compiled.intents||[]).filter(intent=>Number(intent.sequence)===sequence);return{sequence,label:clean(intents[0]?.clause)||`Wunsch ${sequence}`,intentIds:intents.map(intent=>intent.id),ownerRoutes:unique(intents.map(intent=>intent.ownerContract)),requiresConfirmation:intents.some(intent=>intent.requiresConfirmation)}}))}

function planningTrace(input={}){
  const compiled=input.compiled?.contractId===CONTRACT_ID?input.compiled:compileIntent(input.message||'',input),now=Date.parse(input.now||new Date().toISOString());
  const evidence=(input.evidence||[]).slice(0,24).map(item=>{const observedAt=iso(item.observedAt),freshUntil=iso(item.freshUntil),freshUntilMs=freshUntil?Date.parse(freshUntil):Number.NaN,observedMs=observedAt?Date.parse(observedAt):Number.NaN,freshness=!observedAt?'unknown':Number.isFinite(freshUntilMs)&&freshUntilMs<now?'stale':Number.isFinite(observedMs)?'observed':'unknown';return{id:clean(item.id)||digest({source:item.source,observedAt:item.observedAt,kind:item.kind}),source:clean(item.source),kind:clean(item.kind),observedAt,freshUntil,freshness,supports:(item.supports||[]).map(clean).filter(Boolean),verified:item.verified===true}});
  const decisions=(input.decisions||[]).slice(0,24).map(item=>({id:clean(item.id)||digest(item),owner:clean(item.owner),action:clean(item.action),reasonCodes:(item.reasonCodes||[]).map(clean).filter(Boolean),evidenceIds:(item.evidenceIds||[]).map(clean).filter(Boolean),requiresConfirmation:item.requiresConfirmation!==false,status:clean(item.status)||'proposed'}));
  return immutable({contractId:CONTRACT_ID,kind:'explainable-planning-trace',traceId:digest({messageHash:compiled.messageHash,evidence,decisions}),compiled:{messageHash:compiled.messageHash,intentIds:compiled.intents.map(item=>item.id),ownerRoutes:compiled.ownerRoutes,status:compiled.status},evidence,decisions,missingEvidence:decisions.flatMap(decision=>decision.evidenceIds.filter(id=>!evidence.some(item=>item.id===id))),staleEvidence:evidence.filter(item=>item.freshness==='stale').map(item=>item.id),privateRawContextStored:false,exactLocationStored:false,automaticMutation:false});
}
function gateContext(input={}){
  const purpose=clean(input.purpose),grant=input.grant||{},source=input.context||{},now=Date.parse(input.now||new Date().toISOString()),expires=grant.expiresAt?Date.parse(grant.expiresAt):Number.NaN,observedAt=iso(source.observedAt),observed=observedAt?Date.parse(observedAt):Number.NaN,maxAge=Math.max(1000,Number(input.maxObservationAgeMs)||300000),coordinates=source.coordinates||source.position||null,hasCoordinates=coordinates&&Number.isFinite(Number(coordinates.lat??coordinates.latitude))&&Number.isFinite(Number(coordinates.lng??coordinates.longitude));
  const reason=grant.revoked===true?'grant-revoked':grant.granted!==true?'no-explicit-grant':!PURPOSES.has(purpose)?'purpose-not-allowed':input.background===true?'background-request':!Number.isFinite(expires)||expires<now?'grant-expired':!hasCoordinates?'sensor-unavailable':!Number.isFinite(observed)||now-observed>maxAge?'context-observation-stale':null,precision=grant.precision==='precise'?'precise':'coarse',receipt={purpose,precision,expiresAt:iso(grant.expiresAt),allowed:reason===null,reason,persist:false,coordinatesIncluded:false};
  if(reason)return immutable({contractId:CONTRACT_ID,kind:'on-device-context-gate',allowed:false,purpose,reason,context:null,persist:false,decisionReceipt:receipt});
  const reduced={lat:precision==='precise'?Number(coordinates.lat??coordinates.latitude):Number(Number(coordinates.lat??coordinates.latitude).toFixed(2)),lng:precision==='precise'?Number(coordinates.lng??coordinates.longitude):Number(Number(coordinates.lng??coordinates.longitude).toFixed(2))};
  return immutable({contractId:CONTRACT_ID,kind:'on-device-context-gate',allowed:true,purpose,precision,context:{coordinates:reduced,observedAt,source:clean(source.source)||'device'},persist:false,expiresAt:iso(grant.expiresAt),decisionReceipt:receipt});
}
function causalFeedback(input={}){const explicit=input.explicit===true,confirmed=input.confirmedOutcome===true;if(!explicit||!confirmed)return immutable({contractId:CONTRACT_ID,kind:'causal-feedback-learning',accepted:false,reason:!explicit?'feedback-not-explicit':'outcome-not-confirmed',proposedAdjustments:[],automaticProfileMutation:false});const outcome=clean(input.outcome),allowed=new Set(['travelInterests','dietaryPreferences','travelPace','budgetPreference']),raw=(input.signals||[]).filter(signal=>clean(signal.feature)&&Number.isFinite(Number(signal.effect))).slice(0,12);if(raw.some(signal=>!allowed.has(clean(signal.feature))))return immutable({contractId:CONTRACT_ID,kind:'causal-feedback-learning',accepted:false,reason:'feature-not-allowed',proposedAdjustments:[],automaticProfileMutation:false});if(raw.some(signal=>!clean(signal.evidenceId)))return immutable({contractId:CONTRACT_ID,kind:'causal-feedback-learning',accepted:false,reason:'missing-evidence',proposedAdjustments:[],automaticProfileMutation:false});const contradictory=raw.some((signal,index)=>raw.some((other,otherIndex)=>otherIndex!==index&&clean(other.feature)===clean(signal.feature)&&clean(other.value)===clean(signal.value)&&Math.sign(Number(other.effect))!==Math.sign(Number(signal.effect))));if(contradictory)return immutable({contractId:CONTRACT_ID,kind:'causal-feedback-learning',accepted:false,reason:'contradictory-feedback',proposedAdjustments:[],automaticProfileMutation:false});const proposedAdjustments=raw.map(signal=>({feature:clean(signal.feature),value:clean(signal.value)||null,delta:Number(clamp(Number(signal.effect),-.08,.08).toFixed(3)),basis:clean(signal.basis)||outcome,evidenceId:clean(signal.evidenceId),requiresIdentityOwnerConfirmation:true}));return immutable({contractId:CONTRACT_ID,kind:'causal-feedback-learning',accepted:Boolean(proposedAdjustments.length),outcome,proposedAdjustments,automaticProfileMutation:false,ownerRoute:'identity.v1'})}
function orchestrate(message,input={}){const compiled=compileIntent(message,input),trace=planningTrace({compiled,evidence:input.evidence,decisions:input.decisions,now:input.now});return immutable({contractId:CONTRACT_ID,kind:'travel-orchestration-proposal',compiled,trace,ownerCommands:compiled.intents.map(intent=>({owner:intent.owner,ownerContract:intent.ownerContract,ownerAvailability:intent.ownerAvailability,intentId:intent.id,mode:intent.mode,requiresConfirmation:intent.requiresConfirmation,blocked:compiled.blockedCommands.some(item=>item.intentIds?.includes(intent.id)),missingInputs:intent.missingInputs,payload:{clause:intent.clause,categoryHints:intent.categoryHints,temporalHint:intent.temporalHint,entityHints:intent.entityHints}})),automaticMutation:false})}
function diagnostics(){return immutable({contractId:CONTRACT_ID,version:VERSION,runtimeVersion:RUNTIME_VERSION,browserless:true,features:['multi-intent-compiler','multilingual-structured-compiler','sequential-intent-slices','owner-route-graph','conflict-detection','missing-input-detection','offline-command-gate','explainable-planning-trace','causal-feedback-learning','on-device-context-gate'],ownerRouted:true,directOwnerMutation:false,rawMessagePersistence:false})}

return Object.freeze({contractId:CONTRACT_ID,version:VERSION,runtimeVersion:RUNTIME_VERSION,compileIntent,compileDialogue,sliceIntentGraph,sequencePlan,planningTrace,gateContext,causalFeedback,orchestrate,diagnostics});
})();

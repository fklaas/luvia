'use strict';
const assert=require('node:assert/strict');const fs=require('node:fs');const html=fs.readFileSync('tests/fixtures/m16.5-block0-human-ai-parity-failure-browser.html','utf8');
assert.match(html,/luvia-human-ai-parity-failure-matrix\.v1\.json/,'visible gate must load the canonical generated matrix');assert.match(html,/human-ai-parity-failure-matrix-core\.js/,'visible gate must use the browserless query/projector core');assert.match(html,/data-matrix-search/);assert.match(html,/data-matrix-release/);assert.match(html,/data-matrix-category/);assert.match(html,/data-matrix-rows/);assert.match(html,/data-matrix-detail/);assert.match(html,/Zwölf Nachweise/);assert.match(html,/Was bei Fehlern passieren muss/);assert.match(html,/User kann/);assert.match(html,/Luvia kann/);assert.match(html,/document\.body\.dataset\.loadState='pass'/);assert.doesNotMatch(html,/rows:\s*\[\s*\{/,'fixture must not contain a copied demo action list');assert.match(html,/@media\(max-width:620px\)/,'visible matrix needs a mobile layout');
console.log('M16.5 Block 0 visible Human-AI parity/failure matrix browser: PASS');

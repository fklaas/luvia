'use strict';const assert=require('node:assert/strict');const fs=require('node:fs');const fixture=fs.readFileSync('tests/fixtures/m16.5-block0-human-ai-lifecycle-browser.html','utf8'),document=JSON.parse(fs.readFileSync('config/luvia-human-ai-action-lifecycles.v1.json','utf8'));assert.match(fixture,/Block 0\.06/);assert.match(fixture,/human-ai-action-lifecycle-core\.js/);assert.match(fixture,/luvia-human-ai-action-lifecycles\.v1\.json/);assert.match(fixture,/Favorit setzen und rückgängig machen/);assert.match(fixture,/Buchung mit unklarem Ausgang/);assert.match(fixture,/Nur Restaurant lesen/);assert.match(fixture,/Kein Chat-Satz überspringt die Vorschau/);assert.equal(document.summary.lifecycleActions,329);assert.equal(document.summary.stateChanging,124);assert.equal(document.summary.truthfulUndo,20);console.log('M16.5 Block 0 visible complete Human-AI lifecycle browser: PASS');

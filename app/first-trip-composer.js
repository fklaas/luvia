(() => {
  'use strict';

  const VERSION='1.4.1-brief-error-recovery';
  const ROUTE='first-trip-composer';
  const STEPS=Object.freeze(['welcome','identity','feeling','destination','dates','people','modules','accent','ready']);
  const ALL_STEPS=Object.freeze([...STEPS,'brief','preview']);
  const ENTRY_FLOWS=Object.freeze({
    guided:Object.freeze([...STEPS]),
    quick:Object.freeze(['welcome','destination','dates','accent','ready']),
    ai:Object.freeze(['welcome','destination','dates','brief','preview','accent','ready'])
  });
  const STEP_LABELS=Object.freeze({welcome:'Ankommen',identity:'Identität',feeling:'Gefühl',destination:'Ort',dates:'Zeit',people:'Gemeinsam',modules:'Bausteine',brief:'Wünsche',preview:'Entwurf',accent:'Farbe',ready:'Bereit'});
  const STEP_COPY=Object.freeze({
    welcome:['ERSTE REISE','Eine Richtung entsteht','Los geht’s'],identity:['01 · IDENTITÄT','Gebt eurer Reise einen Namen','Weiter'],feeling:['02 · GEFÜHL','Wie soll sie sich anfühlen?','Weiter'],
    destination:['03 · REISEHORIZONT','Wohin zieht es euch?','Weiter'],dates:['04 · ZEIT','Wann darf sie beginnen?','Weiter'],people:['05 · GEMEINSAM','Wer reist mit?','Weiter'],
    modules:['06 · BAUSTEINE','Was soll euch begleiten?','Weiter'],brief:['LUVIA AI · WÜNSCHE','Was soll diese Reise können?','Entwurf ansehen'],preview:['LUVIA AI · VORSCHAU','So baut Luvia den Reiseentwurf','Gestaltung wählen'],accent:['07 · REISEFARBE','Gebt der Reise ihren Ton','Weiter'],ready:['BEREIT','Eure Reise kann beginnen','Reise anlegen']
  });
  const SYMBOLS=Object.freeze(['✦','♡','☀','⌁','◌','△','≈','◇']);
  const FEELINGS=Object.freeze([
    ['slow','Viel Luft','Ruhig und ohne engen Takt.'],['curious','Neugierig','Kleine Gassen, große Entdeckungen.'],['together','Zeit füreinander','Gemeinsam statt nebeneinander.'],
    ['active','In Bewegung','Draußen, lebendig und spontan.'],['culture','Kultur nah erleben','Geschichten, Orte und Menschen.'],['indulgent','Genussvoll','Gute Tische und lange Abende.']
  ]);
  const MODULES=Object.freeze([
    ['places','⌖','Places','Orte entdecken, verstehen und merken.'],['journey','⌁','Journey','Tage, Wege und Momente verbinden.'],['booking','▣','Booking','Anfragen und Buchungen zusammenhalten.'],
    ['wallet-documents','▤','Wallet & Dokumente','Belege und Reiseunterlagen griffbereit.'],['memories','♡','Memories','Aus Augenblicken eure Geschichte machen.'],
    ['move','↝','Move','Wege und Mobilität passend einordnen.'],['collaboration','◎','Gemeinsam reisen','Einladungen nach der Reiseerstellung übergeben.']
  ]);
  const TRIP_INTERESTS=Object.freeze([['food','Essen & Trinken'],['culture','Kultur'],['nature','Natur'],['nightlife','Nachtleben'],['shopping','Shopping'],['wellness','Wellness'],['family','Familie'],['active','Aktiv sein']]);
  const BUDGETS=Object.freeze([['economy','Bewusst'],['balanced','Ausgewogen'],['generous','Großzügig'],['open','Noch offen']]);
  const AI_CATEGORIES=Object.freeze({food:['food','Essen & Trinken'],culture:['culture','Kultur'],nature:['nature','Natur & Erholung'],nightlife:['nightlife','Nachtleben'],shopping:['shopping','Shopping'],wellness:['wellness','Wellness & Spa'],family:['activities','Aktivitäten für Familien'],active:['activities','Aktivitäten']});
  const ACCENTS=Object.freeze([
    ['coral','#ec6555','Coral'],['sea','#2f95a8','Sea'],['sand','#c99562','Sand'],['sage','#799b7a','Sage'],['lavender','#9b82c5','Lavender'],
    ['sunset','#ef7b62','Sunset'],['fjord','#4c8792','Fjord'],['berry','#ae5e7a','Berry'],['lagoon','#38a69b','Lagoon'],['ember','#d36d46','Ember'],
    ['aurora','#db7fa6','Aurora'],['tide','#3e91a8','Tide'],['coast','#69a8ad','Coast'],['harbor','#426f82','Harbor'],['reef','#38a6a9','Reef'],
    ['seagrass','#6e9d87','Seagrass'],['pine','#426f5b','Pine'],['fern','#648b67','Fern'],['meadow','#8da66f','Meadow'],['olive','#8b8b58','Olive'],
    ['moss','#66785a','Moss'],['alpine','#71988c','Alpine'],['glacier','#80b9c7','Glacier'],['sky','#75a9cf','Sky'],['storm','#68788e','Storm'],
    ['slate','#65717d','Slate'],['midnight','#354b69','Midnight'],['indigo','#5f62a7','Indigo'],['lilac','#a88dc2','Lilac'],['orchid','#b272a5','Orchid'],
    ['plum','#7e506f','Plum'],['rose','#c66f85','Rose'],['blush','#df96a0','Blush'],['poppy','#dd5c50','Poppy'],['peach','#e99775','Peach'],
    ['apricot','#dda166','Apricot'],['saffron','#d5a044','Saffron'],['honey','#bd9550','Honey'],['dune','#b69b78','Dune'],['clay','#b87862','Clay'],
    ['terracotta','#b65f4c','Terracotta'],['copper','#a76748','Copper'],['cocoa','#7c5e52','Cocoa'],['chestnut','#805449','Chestnut'],['stone','#8b8982','Stone'],
    ['pearl','#b6aaa2','Pearl'],['cloud','#9ba9ad','Cloud'],['chalk','#c5bdb3','Chalk'],['moon','#9c9bb0','Moon'],['ink','#334d5b','Ink']
  ]);
  const SCENES=Object.freeze({welcome:'prototype-coast-morning.png',identity:'prototype-coast-morning.png',feeling:'prototype-harbor-lunch.png',destination:'prototype-coast-bike.png',dates:'prototype-coast-morning.png',people:'prototype-harbor-lunch.png',modules:'prototype-coast-bike.png',brief:'prototype-harbor-lunch.png',preview:'prototype-coast-bike.png',accent:'prototype-memory-sunset.png',ready:'prototype-memory-sunset.png'});
  let mounted=null,searchTimer=0,searchSequence=0,placesSessionToken='';

  const clone=value=>value==null?value:JSON.parse(JSON.stringify(value));
  const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
  const storage=()=>window.LuviaPlatformPorts?.get?.('StoragePort')||window.LuviaIdentityPlatformWebPorts?.StoragePort||null;
  const auth=()=>window.LuviaPlatformPorts?.get?.('AuthSessionPort')?.snapshot?.()||window.LuviaIdentityPlatformWebPorts?.AuthSessionPort?.snapshot?.()||window.LuviaAuth?.getState?.()||{};
  const trip=()=>window.LuviaTripContractV1;
  const userId=()=>auth()?.user?.id||window.LuviaIdentityContractV1?.getViewerIdentity?.()?.userId||'viewer';
  const draftKey=()=>`luviaFirstTripComposerDraftV1:${userId()}`;
  const deferredKey=()=>`luviaFirstTripComposerDeferredV1:${userId()}`;
  const readSession=(key,fallback=null)=>{try{return storage()?.get?.(key,fallback,{scope:'session'})??fallback}catch{return fallback}};
  const writeSession=(key,value)=>{try{return storage()?.set?.(key,value,{scope:'session'})??value}catch{return value}};
  const removeSession=key=>{try{return storage()?.remove?.(key,{scope:'session'})??false}catch{return false}};
  const createKey=()=>crypto.randomUUID?.()||`first-trip-${Date.now()}-${Math.random().toString(36).slice(2)}`;
  const reduceMotion=()=>document.documentElement.classList.contains('reduce-motion')||document.body.classList.contains('reduce-motion')||window.matchMedia?.('(prefers-reduced-motion: reduce)')?.matches;

  function shouldStart(){return (trip()?.reads?.listTrips?.()||[]).length===0&&readSession(deferredKey(),false)!==true;}
  function defer(){writeSession(deferredKey(),true);}
  function clearDeferred(){removeSession(deferredKey());}
  function draftStep(){const value=readSession(draftKey(),null);return ALL_STEPS.includes(value?.step)?value.step:'welcome';}
  function readDraft(){const value=readSession(draftKey(),null);return [1,2,3].includes(value?.version)&&value?.userId===userId()?value:null;}
  function clearDraft(){removeSession(draftKey());}
  function persist(state,{required=false}={}){
    const checkpoint=(state.checkpoint||0)+1;state.checkpoint=checkpoint;
    writeSession(state.storageKey,{version:3,userId:state.viewerId,checkpoint,idempotencyKey:state.idempotencyKey,step:currentStep(state),highest:state.highest,data:clone(state.data),draftSelections:clone(state.draftSelections||[]),briefAnswers:clone(state.briefAnswers||[]),openingComplete:state.openingComplete,aiDraft:['ready','brief-ready'].includes(state.aiDraft?.status)?clone(state.aiDraft):null,confirmation:clone(state.confirmation||null),updatedAt:new Date().toISOString()});
    if(required){const saved=readSession(state.storageKey);if(saved?.checkpoint!==checkpoint||saved?.idempotencyKey!==state.idempotencyKey)throw new Error('Der Fortschritt konnte auf diesem Gerät nicht gesichert werden. Bitte Speicherzugriff erlauben und erneut bestätigen.');}
  }
  function defaultData(){return {title:'',subtitle:'',symbol:'✦',feelings:[],destination:{name:'',formattedAddress:'',country:'',countryCode:'',placeId:'',latitude:null,longitude:null},scheduleMode:'fixed',startDate:'',endDate:'',flexibility:'Etwa eine Woche',participantPlan:'solo-first',privacy:'private',modules:MODULES.map(item=>item[0]),accent:'#ec6555',entryMode:'guided',requestBrief:'',tripPreferences:{budgetLevel:'open',pace:'open',interests:[],food:[],accessibility:[],mobility:[],notes:''},durablePreferenceProposal:{requested:false,fields:[]}};}
  function currentFlow(state){return ENTRY_FLOWS[state?.data?.entryMode]||ENTRY_FLOWS.guided;}
  function currentStep(state){return currentFlow(state)[state.index]||'welcome';}
  function initialState(root,options={}){
    const draft=options.resume===false?null:readDraft();
    const data={...defaultData(),...(draft?.data||{})};
    data.tripPreferences={...defaultData().tripPreferences,...(draft?.data?.tripPreferences||{})};
    data.durablePreferenceProposal={...defaultData().durablePreferenceProposal,...(draft?.data?.durablePreferenceProposal||{})};
    data.entryMode=ENTRY_FLOWS[options.entryMode]?options.entryMode:(ENTRY_FLOWS[data.entryMode]?data.entryMode:'guided');
    const flow=ENTRY_FLOWS[data.entryMode],requested=ALL_STEPS.includes(options.step)?options.step:draft?.step;
    const index=draft?.confirmation?flow.indexOf('ready'):Math.max(0,flow.indexOf(requested||'welcome'));
    return {root,viewerId:userId(),storageKey:draftKey(),options:{...options},mode:options.mode==='create'?'create':'first-use',index,highest:Math.max(index,Number(draft?.highest||0)),idempotencyKey:draft?.idempotencyKey||createKey(),data,draftSelections:Array.isArray(draft?.draftSelections)?draft.draftSelections:[],briefAnswers:draft?.briefAnswers||[],activeDay:null,openingComplete:Boolean(draft?.highest||draft?.openingComplete),aiDraft:draft?.version===3&&['ready','brief-ready'].includes(draft.aiDraft?.status)?draft.aiDraft:{status:'idle',signature:'',draft:null,places:[],rehearsals:[],error:null},confirmation:draft?.confirmation||null,checkpoint:draft?.checkpoint||0,loadSequence:0,mapProjection:null,saving:false,finished:false,error:null,cleanup:[],toastTimer:0};
  }
  function compassMarkup(className=''){
    return `<span class="ftc-compass ${esc(className)}" aria-hidden="true"><span class="ftc-compass-rings"></span><span class="ftc-compass-face"><img src="assets/public-landing/luvia-compass-face.svg" alt=""><span><img src="assets/public-landing/luvia-compass-needle.svg" alt=""></span><img class="ftc-compass-hub" src="assets/public-landing/luvia-compass-hub.svg" alt=""></span></span>`;
  }
  function storyOpening(state){
    const cards=[['slow','Durchatmen','Mehr Raum. Weniger müssen.','prototype-coast-morning.png','≈'],['curious','Eintauchen','Gassen, Geschichten, neue Lieblingsorte.','prototype-harbor-lunch.png','✦'],['active','Losziehen','Draußen sein. Neues wagen.','prototype-coast-bike.png','↗']];
    return `<article class="ftc-panel ftc-story-opening"><header><span class="ftc-kicker">Eure Reise beginnt hier. In eurem Kopf.</span><h1>Noch ist alles möglich.<br><em>Was zieht euch an?</em></h1><p>Schiebt die Momente durch. Berührt, was sich nach euch anfühlt. Daraus entwickeln wir eure Reise.</p></header><div class="ftc-story-moments" aria-label="Reisegefühl entdecken">${cards.map(([id,title,copy,photo,symbol])=>`<button type="button" class="ftc-story-moment ${state.data.feelings.includes(id)?'is-chosen':''}" data-ftc-story-feeling="${id}" aria-pressed="${state.data.feelings.includes(id)}"><img src="assets/public-landing/${photo}" alt="" draggable="false"><span class="ftc-story-number">0${cards.findIndex(card=>card[0]===id)+1}</span><i>${symbol}</i><span class="ftc-story-moment-copy"><strong>${title}</strong><small>${copy}</small></span><b>${state.data.feelings.includes(id)?'✓':'+'}</b></button>`).join('')}</div><footer><div class="ftc-story-thread"><i>✦</i><span>${state.data.feelings.length?state.data.feelings.map(id=>FEELINGS.find(item=>item[0]===id)?.[1]||id).join(' · '):'Euer erster Impuls. Alles bleibt veränderbar.'}</span></div><button type="button" class="ftc-story-start" data-ftc-story-start>Mit Luvia meine Reise entwickeln <span>↗</span></button><button type="button" class="ftc-story-other" data-ftc-story-other>Lieber selbst gestalten oder schnell starten</button></footer></article>`;
  }
  function bindStoryOpening(state){
    state.root.querySelectorAll('[data-ftc-story-feeling]').forEach(button=>button.addEventListener('click',()=>{
      const feeling=button.dataset.ftcStoryFeeling,selected=state.data.feelings.includes(feeling);state.data.feelings=selected?state.data.feelings.filter(id=>id!==feeling):[...state.data.feelings,feeling].slice(-3);
      state.data.tripPreferences.pace=state.data.feelings.includes('slow')&&!state.data.feelings.includes('active')?'slow':state.data.feelings.includes('active')&&!state.data.feelings.includes('slow')?'active':'balanced';
      button.classList.toggle('is-chosen',!selected);button.setAttribute('aria-pressed',String(!selected));button.querySelector('b').textContent=selected?'+':'✓';
      const thread=state.root.querySelector('.ftc-story-thread span');if(thread)thread.textContent=state.data.feelings.length?state.data.feelings.map(id=>FEELINGS.find(item=>item[0]===id)?.[1]||id).join(' · '):'Euer erster Impuls. Alles bleibt veränderbar.';persist(state);
    }));
    state.root.querySelector('[data-ftc-story-start]')?.addEventListener('click',()=>{state.openingComplete=true;state.data.entryMode='ai';go(state,1,{skipValidation:true})});
    state.root.querySelector('[data-ftc-story-other]')?.addEventListener('click',()=>{state.openingComplete=true;render(state,{focus:true})});
  }
  function worldMarkup(state){return `<div class="ftc-world" aria-hidden="true"><figure><img src="assets/public-landing/${SCENES[currentStep(state)]}" alt=""></figure><i></i><span class="ftc-mark mark-a">⌁</span><span class="ftc-mark mark-b">✦</span><span class="ftc-mark mark-c">♡</span></div>`;}
  function welcomePanel(state){if(!state.openingComplete)return storyOpening(state);const modes=[['guided','Geführt','Alle Entscheidungen Schritt für Schritt treffen.','9 Schritte'],['quick','Schnellstart','Ziel und Zeitraum festlegen, Gestaltung wählen, loslegen.','5 Schritte'],['ai','Mit Luvia AI','Wünsche frei beschreiben und vor dem Speichern einen ehrlichen Entwurf prüfen.','7 Schritte']];return `<article class="ftc-panel ftc-welcome"><div class="ftc-story"><span class="ftc-kicker">Euer nächstes Kapitel</span><h1>Eine Richtung. <em>Euer eigener Weg.</em></h1><p>Ihr entscheidet, wie viel ihr selbst gestalten möchtet. Eure Eingaben bleiben beim Wechsel erhalten.</p><div class="ftc-entry-modes">${modes.map(([id,label,copy,count])=>`<button type="button" data-ftc-entry-mode="${id}" class="${state.data.entryMode===id?'is-selected':''}" aria-pressed="${state.data.entryMode===id}"><i>${id==='guided'?'⌁':id==='quick'?'↝':'✦'}</i><span><strong>${label}</strong><small>${copy}</small><b>${count}</b></span></button>`).join('')}</div><div class="ftc-scope-line"><span>Anfrage bleibt flüchtig</span><span>Reisewünsche gelten für diese Reise</span><span>Profiländerungen brauchen Bestätigung</span></div></div><aside class="ftc-hero-compass">${compassMarkup('is-hero')}<span class="ftc-orbit orbit-a">Wohin?</span><span class="ftc-orbit orbit-b">Wann?</span><span class="ftc-orbit orbit-c">Wie?</span><div class="ftc-ticket"><small>TRIP COMPOSER</small><strong>Euer nächster Horizont.</strong><span>Schritt für Schritt, direkt zum Ziel oder mit ersten Vorschlägen.</span></div></aside></article>`;}
  function identityPanel(state){return `<article class="ftc-panel"><header class="ftc-copy"><span class="ftc-kicker">Reiseidentität</span><h1>Wie soll diese Reise heißen?</h1><p>Ein Name, eine kleine Zeile und ein Zeichen – alles später änderbar.</p></header><div class="ftc-form"><label><span>Reisename</span><input data-ftc-field="title" maxlength="80" value="${esc(state.data.title)}" placeholder="Unser Sommer am Meer" autocomplete="off"></label><label><span>Unterzeile</span><input data-ftc-field="subtitle" maxlength="120" value="${esc(state.data.subtitle)}" placeholder="Zeit für uns, ohne engen Takt" autocomplete="off"></label><fieldset class="ftc-symbols"><legend>Euer Zeichen</legend>${SYMBOLS.map(symbol=>`<button type="button" data-ftc-symbol="${symbol}" class="${state.data.symbol===symbol?'is-selected':''}" aria-pressed="${state.data.symbol===symbol}">${symbol}</button>`).join('')}</fieldset></div></article>`;}
  function feelingPanel(state){return `<article class="ftc-panel"><header class="ftc-copy"><span class="ftc-kicker">Reisegefühl</span><h1>Wonach soll sich diese Reise anfühlen?</h1><p>Bis zu drei Schwerpunkte für genau diese Reise. Dein persönlicher Reisekompass bleibt unverändert.</p></header><div class="ftc-choice-grid">${FEELINGS.map(([id,label,copy],index)=>{const on=state.data.feelings.includes(id);return `<button type="button" data-ftc-feeling="${id}" class="ftc-feeling ${on?'is-selected':''}" aria-pressed="${on}"><i>${['≈','✦','♡','↝','◇','☀'][index]}</i><span><strong>${label}</strong><small>${copy}</small></span></button>`}).join('')}</div><div class="ftc-preference-note"><i>◎</i><span><strong>Profil bleibt Basis · Reisegefühl setzt den Schwerpunkt</strong><small>Ernährung, Barrierefreiheit und Mobilität aus dem Profil bleiben vorrangig. Diese Auswahl wird mit der Reise gespeichert; ihre Gewichtung in Places, Planung und Journey folgt in der nächsten Core-Etappe.</small></span></div></article>`;}
  function destinationPanel(state){const selected=state.data.destination?.placeId;return `<article class="ftc-panel"><header class="ftc-copy"><span class="ftc-kicker">Euer Reiseziel</span><h1>Wohin zieht es euch?</h1><p>Sucht einen Ort oder eine Region und wählt den passenden Vorschlag.</p></header><div class="ftc-destination"><label><span>Ort oder Region</span><input data-ftc-destination autocomplete="off" value="${esc(state.data.destination.formattedAddress||state.data.destination.name)}" placeholder="Kopenhagen eingeben …" aria-autocomplete="list" aria-controls="ftc-suggestions"></label><div id="ftc-suggestions" data-ftc-suggestions></div><div class="ftc-place-proof ${selected?'is-confirmed':''}" data-ftc-place-proof><i>${selected?'✓':'⌖'}</i><span><strong>${selected?esc(state.data.destination.name):'Noch kein Ort bestätigt'}</strong><small>${selected?esc(state.data.destination.formattedAddress):'Bitte einen Vorschlag aus der Suche wählen.'}</small></span></div></div></article>`;}
  function datesPanel(state){const fixed=state.data.scheduleMode!=='flexible',windows=['Ein paar Tage','Etwa eine Woche','Etwa ein Monat','Ganz offen'];return `<article class="ftc-panel"><header class="ftc-copy"><span class="ftc-kicker">Reisezeit</span><h1>Wie genau kennt ihr eure Reisezeit?</h1><p>Wählt entweder konkrete Daten oder einen groben Rahmen. Beides kann später geändert werden.</p></header><div class="ftc-date-mode" role="radiogroup" aria-label="Reisezeit festlegen"><button type="button" data-ftc-schedule="fixed" class="${fixed?'is-selected':''}" aria-pressed="${fixed}"><i>▣</i><strong>Daten stehen fest</strong><small>Anreise und Rückreise eintragen.</small></button><button type="button" data-ftc-schedule="flexible" class="${!fixed?'is-selected':''}" aria-pressed="${!fixed}"><i>≈</i><strong>Zeitraum noch offen</strong><small>Nur einen ungefähren Rahmen wählen.</small></button></div>${fixed?`<div class="ftc-date-grid"><label><span>Anreise</span><input type="date" data-ftc-field="startDate" value="${esc(state.data.startDate)}"></label><label><span>Rückreise</span><input type="date" data-ftc-field="endDate" value="${esc(state.data.endDate)}" min="${esc(state.data.startDate)}"></label>${state.data.entryMode==='ai'?`<label class="ftc-timezone"><span>Zeitzone am Reiseziel</span><select data-ftc-timezone><option value="">Bitte bestätigen …</option>${[...new Set([state.data.destination.timezone,...(Intl.supportedValuesOf?.('timeZone')||['Europe/Berlin','Europe/Lisbon','Europe/London','America/New_York','Asia/Tokyo'])].filter(Boolean))].sort().map(zone=>`<option value="${esc(zone)}" ${tripZone(state)===zone?'selected':''}>${esc(zone.replaceAll('_',' '))}</option>`).join('')}</select><small>Alle geplanten Uhrzeiten gelten in dieser Zeitzone.</small></label>`:''}</div>`:`<fieldset class="ftc-flex-options"><legend>Welcher Rahmen passt gerade?</legend>${windows.map(value=>`<button type="button" data-ftc-flexibility="${value}" class="${state.data.flexibility===value?'is-selected':''}" aria-pressed="${state.data.flexibility===value}">${value}</button>`).join('')}</fieldset>`}</article>`;}
  function peoplePanel(state){const invite=state.data.participantPlan==='invite-after-creation';return `<article class="ftc-panel"><header class="ftc-copy"><span class="ftc-kicker">Gemeinsam reisen</span><h1>Wie möchtet ihr starten?</h1><p>Entscheidet nur, was direkt nach der Reiseerstellung passiert. Menschen und Rollen werden erst im anschließenden Einladungsschritt festgelegt.</p></header><div class="ftc-date-mode" role="radiogroup" aria-label="Reisegruppe starten"><button type="button" data-ftc-people="solo-first" class="${!invite?'is-selected':''}" aria-pressed="${!invite}"><i>◌</i><strong>Nur für mich starten</strong><small>Die Reise bleibt privat. Einladen geht jederzeit später.</small></button><button type="button" data-ftc-people="invite-after-creation" class="${invite?'is-selected':''}" aria-pressed="${invite}"><i>◎</i><strong>Danach Menschen einladen</strong><small>Nach dem Erstellen öffnet sich der Einladungsschritt.</small></button></div><div class="ftc-owner-note"><i>${invite?'→':'✓'}</i><span><strong>${invite?'Als Nächstes: Einladungen':'Privater Start'}</strong><small>${invite?'Bis jemand eine Einladung annimmt, bleibt die Reise geschützt.':'Nur du siehst die Reise. Diese Entscheidung ist später änderbar.'}</small></span></div></article>`;}
  function modulesPanel(state){return `<article class="ftc-panel"><header class="ftc-copy"><span class="ftc-kicker">Reisebausteine</span><h1>Was soll euch von Anfang an begleiten?</h1><p>Wählt die Bereiche, die ihr auf eurer Reise nutzen möchtet. Ihr könnt eure Auswahl später ändern.</p></header><div class="ftc-module-grid">${MODULES.map(([id,icon,label,copy])=>{const on=state.data.modules.includes(id);return `<button type="button" data-ftc-module="${id}" class="${on?'is-selected':''}" aria-pressed="${on}"><i>${icon}</i><span><strong>${label}</strong><small>${copy}</small></span><b>${on?'✓':'+'}</b></button>`}).join('')}</div></article>`;}
  function briefPanel(state){const p=state.data.tripPreferences||{};return `<article class="ftc-panel"><header class="ftc-copy"><span class="ftc-kicker">Anfrage an Luvia AI</span><h1>Was soll eure Reise können?</h1><p>Schreibt frei oder setzt Schwerpunkte. Der Text gilt nur für diesen Entwurf und wird nicht als Profilmerkmal gespeichert. Luvia fasst eure Beschreibung zuerst zu einem Reiseauftrag zusammen. Ihr prüft ihn, bevor daraus Ortsvorschläge entstehen.</p></header><div class="ftc-brief-layout"><label class="ftc-brief-field"><span>Eure Wünsche in eigenen Worten</span><textarea data-ftc-field="requestBrief" maxlength="1200" placeholder="Wir möchten viel am Wasser sein, ruhig frühstücken, zwei besondere Abende erleben und nicht jeden Tag durchplanen.">${esc(state.data.requestBrief)}</textarea><small>${state.data.requestBrief.length}/1200 · Geltung: nur diese Anfrage</small></label><section class="ftc-trip-preferences"><div><span>Schwerpunkte dieser Reise</span><div class="ftc-chip-grid">${TRIP_INTERESTS.map(([id,label])=>{const on=(p.interests||[]).includes(id);return `<button type="button" data-ftc-trip-interest="${id}" class="${on?'is-selected':''}" aria-pressed="${on}">${label}</button>`}).join('')}</div></div><div><span>Budgetgefühl dieser Reise</span><div class="ftc-chip-grid is-budget">${BUDGETS.map(([id,label])=>`<button type="button" data-ftc-budget="${id}" class="${p.budgetLevel===id?'is-selected':''}" aria-pressed="${p.budgetLevel===id}">${label}</button>`).join('')}</div></div><div><span>Wie viel möchtet ihr erleben?</span><div class="ftc-chip-grid">${[['slow','Viel Freiraum'],['balanced','Gute Mischung'],['active','Viel entdecken']].map(([id,label])=>`<button type="button" data-ftc-pace="${id}" aria-pressed="${p.pace===id}" class="${p.pace===id?'is-selected':''}">${label}</button>`).join('')}</div></div><button type="button" class="ftc-durable-choice ${state.data.durablePreferenceProposal.requested?'is-selected':''}" data-ftc-durable aria-pressed="${state.data.durablePreferenceProposal.requested}"><i>${state.data.durablePreferenceProposal.requested?'✓':'+'}</i><span><strong>Später als Profilvorliebe prüfen</strong><small>Luvia darf nach der Reiseerstellung eine getrennte Bestätigung anbieten. Ohne diese Bestätigung bleibt euer Profil unverändert.</small></span></button></section></div></article>`;}
  function previewPanel(state){
    if(state.aiDraft?.status==='brief-ready')return briefReviewPanel(state);
    const p=effectiveTripPreferences(state),interestLabels=(p.interests||[]).map(id=>TRIP_INTERESTS.find(item=>item[0]===id)?.[1]||id),ai=state.aiDraft||{};
    if(ai.status==='idle'||ai.status==='loading')return `<article class="ftc-panel"><header class="ftc-copy"><span class="ftc-kicker">Erste Ideen für eure Reise</span><h1>Eure Reise nimmt Form an.</h1><p>Eure Schwerpunkte und Profilvorlieben fließen in die Suche ein. Bereits geprüfte Ergebnisse werden wiederverwendet.</p></header><div class="ftc-ai-loading" role="status"><i>⌖</i><span><strong>${state.data.requestBrief.trim()&&!ai.brief?'Luvia versteht eure Wünsche …':'Orte werden gesucht …'}</strong><small>Es wird noch nichts gespeichert oder eingeplant.</small></span></div></article>`;
    if(ai.status==='error')return `<article class="ftc-panel"><header class="ftc-copy"><span class="ftc-kicker">Vorschläge noch nicht verfügbar</span><h1>Die Vorschläge konnten gerade nicht geladen werden.</h1><p>${esc(ai.error||'Die Ortsquellen sind gerade nicht erreichbar.')}</p></header><button type="button" class="ftc-ai-retry" data-ftc-ai-retry>Erneut suchen</button><div class="ftc-owner-note"><i>✓</i><span><strong>Nichts verändert</strong><small>Reise, Timeline, Places und Profil sind unverändert.</small></span></div></article>`;
    const draft=ai.draft||{days:[],unknownFactors:[],alternatives:[]},rehearsals=new Map((ai.rehearsals||[]).map(item=>[item.dayId,item])),entries=draft.days.flatMap(day=>day.entries||[]),selected=state.draftSelections.filter(item=>item.action!=='excluded'),planned=selected.filter(item=>item.action==='planned').length,saved=selected.filter(item=>item.action==='saved').length;
    const actionLabel={planned:'Wird eingeplant',saved:'Wird vorgemerkt',excluded:'Wird weggelassen'};
    return `<article class="ftc-panel ftc-ai-owner-draft"><header class="ftc-copy"><span class="ftc-kicker">Erste Reiseideen · noch nicht gespeichert</span><h1>Tag für Tag. Eure Reise.</h1><p>${esc(state.data.destination.name)} · ${entries.length} Orte · ${draft.days.length} ${state.data.scheduleMode==='flexible'?'beispielhafte Tage':'Reisetage'} · ${planned} einplanen · ${saved} vormerken. Jede Karte zeigt vor der finalen Bestätigung, was geschehen würde.</p></header>${ai.brief?`<div class="ftc-brief-recap"><strong>So haben wir euch verstanden</strong><p>${esc(ai.brief.understanding)}</p>${ai.brief.unresolved.map(item=>`<small>Noch offen: ${esc(item.label)}</small>`).join('')}</div>`:''}<nav class="ftc-day-strip" aria-label="Eure Reisetage"><button type="button" data-ftc-day="all" aria-pressed="${!state.activeDay||state.activeDay==='all'}">Alle Tage</button>${draft.days.map(day=>`<button type="button" data-ftc-day="${day.id}" aria-pressed="${state.activeDay===day.id}">${esc(day.label)}<small>${esc(day.date?dateLabel(day.date):'offen')}</small></button>`).join('')}</nav><div class="ftc-ai-draft-layout"><section class="ftc-ai-map" data-ftc-ai-map aria-label="Gemeinsame Places-Karte des Reiseentwurfs"></section><div class="ftc-ai-days">${draft.days.filter(day=>!state.activeDay||state.activeDay==='all'||state.activeDay===day.id).map(day=>{const rehearsal=rehearsals.get(day.id);return `<section class="ftc-ai-day"><header><span>${esc(day.date?dateLabel(day.date):day.label)}</span><small>${rehearsal?`${esc(rehearsal.statusLabel)}`:'Zeit noch offen'}</small></header>${state.draftSelections.filter(entry=>entry.dayId===day.id).map(entry=>{const choice=state.draftSelections.find(item=>item.slotId===entry.slotId)||entry,action=choice.action||(choice.date?'planned':'saved');return `<article class="ftc-ai-place is-${esc(action)}" data-ftc-ai-place="${esc(choice.providerPlaceId)}"><div class="ftc-ai-place-visual">${choice.imageUrl?`<img src="${esc(choice.imageUrl)}" alt="${esc(choice.name)}" loading="lazy">`:'<i>⌖</i>'}</div><div><small>${esc(choice.date?`${choice.time} Uhr`:placeLabel(choice))}${choice.date?` · ${esc(placeLabel(choice))}`:''}</small><strong>${esc(choice.name)}</strong><span>${esc(choice.formattedAddress||'Adresse über Places belegt')}</span><em>${esc(actionLabel[action])}</em><p class="ftc-place-reason">${esc(choice.reason||'Von euch ausgewählter Ort.')}</p>${choice.date?`<div class="ftc-draft-time"><label><span>Reisetag</span><select data-ftc-draft-date="${esc(choice.slotId)}">${draft.days.filter(item=>item.date).map(item=>`<option value="${item.date}" ${item.date===choice.date?'selected':''}>${esc(dateLabel(item.date))}</option>`).join('')}</select></label><label><span>Uhrzeit</span><input type="time" data-ftc-draft-time="${esc(choice.slotId)}" value="${esc(choice.time)}"></label><label><span>Dauer in Minuten</span><input type="number" min="15" max="720" step="15" data-ftc-draft-duration="${esc(choice.slotId)}" value="${esc(choice.durationMinutes)}"></label></div>`:''}</div><div class="ftc-ai-place-actions" role="group" aria-label="Entwurf für ${esc(choice.name)}"><button type="button" data-ftc-map-focus="${esc(choice.slotId)}">Auf der Karte</button><button type="button" data-ftc-draft-action="planned" data-ftc-draft-slot="${esc(choice.slotId)}" class="${action==='planned'?'is-selected':''}" ${choice.date?'':'disabled title="Erst nach Wahl eines Reisetages verfügbar"'}>Einplanen</button><button type="button" data-ftc-draft-action="saved" data-ftc-draft-slot="${esc(choice.slotId)}" class="${action==='saved'?'is-selected':''}">Vormerken</button><button type="button" data-ftc-draft-swap="${esc(choice.slotId)}" ${draft.alternatives?.length?'':'disabled'}>Tauschen</button><button type="button" data-ftc-draft-action="excluded" data-ftc-draft-slot="${esc(choice.slotId)}" class="${action==='excluded'?'is-selected':''}">Weglassen</button></div></article>`}).join('')}<footer><span>${rehearsal?esc(rehearsal.summary):'Konkrete Zeiten werden nach der Auswahl geprüft.'}</span><small>Erst eure Bestätigung übernimmt die Auswahl</small></footer></section>`}).join('')}</div><aside class="ftc-ai-evidence"><strong>Was noch offen ist</strong><span>${state.data.scheduleMode==='flexible'?'Drei beispielhafte Tage ohne festgelegte Termine.':'Der ganze gewählte Zeitraum ist sichtbar. Tage ohne überprüfte Vorschläge bleiben bewusst frei.'}</span>${(ai.coverage||[]).filter(item=>item.status!=='ready').map(item=>`<span>${esc(item.label)}: ${item.status==='unavailable'?'Quelle gerade nicht erreichbar':'noch keine überprüften Vorschläge'}</span>`).join('')}<span>${esc((draft.unknownFactors||[]).join(' · ')||'Keine weiteren unbekannten Faktoren')}</span><small>Unbelegte Bilder, Öffnungszeiten, Wetter, Auslastung, Störungen, Preise und Buchbarkeit bleiben ausdrücklich unbekannt.</small></aside></div><footer class="ftc-ai-selection-proof"><span><b>${planned}</b> einplanen</span><span><b>${saved}</b> vormerken</span><span><b>${entries.length-selected.length}</b> weglassen</span><strong>Mit der letzten Bestätigung legt ihr die Reise an und übernehmt eure Auswahl.</strong></footer></article>`;
  }
  function briefReviewPanel(state){
    const brief=state.aiDraft.brief,question=brief.question;
    return `<article class="ftc-panel ftc-brief-review"><header class="ftc-copy"><span class="ftc-kicker">Luvia hat zugehört</span><h1>Ist das eure Vorstellung?</h1><p>Prüft zuerst euren Reiseauftrag. Danach suchen wir die Orte dafür.</p></header><blockquote>${esc(brief.understanding)}</blockquote><div class="ftc-brief-goals">${brief.goals.map(goal=>`<span>✦ ${esc(goal.label)}</span>`).join('')}</div><section><h2>So wird daraus euer Entwurf</h2><p>Bis zu ${brief.policy.maximumPerDay} ${brief.policy.maximumPerDay===1?'Vorschlag':'Vorschläge'} pro Tag · zwischen ${esc(brief.policy.notBefore)} und ${esc(brief.policy.notAfter)} Uhr.</p><p>Eure Profilvorgaben bleiben aktiv. Termine, Preise, Wege und Öffnungen werden durch diese Interpretation noch nicht bestätigt.</p>${brief.unresolved.map(item=>`<p class="ftc-brief-open"><strong>${item.hard?'Noch zu klären':'Zusätzlicher Wunsch'}:</strong> ${esc(item.label)}</p>`).join('')}${!brief.automaticPlanningAllowed?'<p>Solange wichtige Anforderungen offen sind, werden die gefundenen Ideen nur vorgemerkt. Ihr könnt sie anschließend selbst prüfen und einplanen.</p>':''}</section>${question?`<section class="ftc-brief-question"><h2>${esc(question.text)}</h2><p>${esc(question.reason)}</p><label><span>Eure Antwort</span><textarea data-ftc-brief-response maxlength="600" rows="3"></textarea></label><button type="button" class="ftc-ai-retry" data-ftc-brief-answer>Antwort berücksichtigen</button></section>`:'<button type="button" class="ftc-ai-retry" data-ftc-brief-search>Ja, damit Orte suchen</button>'}</article>`;
  }
  function accentPanel(state){const selected=ACCENTS.find(([,color])=>state.data.accent===color)?.[2]||'Reisefarbe';return `<article class="ftc-panel"><header class="ftc-copy"><span class="ftc-kicker">Aktive Reisefarbe</span><h1>Welche Farbe trägt eure Reise?</h1><p>Alle 50 Farben aus der Landingpage stehen auch hier zur Wahl. Die persönliche Farbe eurer Mitreisenden bleibt davon unabhängig.</p></header>${state.data.entryMode==='guided'?'':`<fieldset class="ftc-symbols ftc-symbols-compact"><legend>Euer Reisezeichen</legend>${SYMBOLS.map(symbol=>`<button type="button" data-ftc-symbol="${symbol}" class="${state.data.symbol===symbol?'is-selected':''}" aria-pressed="${state.data.symbol===symbol}">${symbol}</button>`).join('')}</fieldset>`}<div class="ftc-accent-stage" style="--ftc-accent:${esc(state.data.accent)}"><div class="ftc-pass"><span>${esc(state.data.symbol)}</span><small>${esc(state.data.subtitle||'Eure Reise beginnt')}</small><strong>${esc(state.data.title||'Unsere Reise')}</strong><p>${esc(state.data.destination.name||'Euer nächster Horizont')}</p><i></i></div><div class="ftc-swatches"><div class="ftc-palette-head"><strong>50 Reisefarben</strong><span>${esc(selected)} gewählt</span></div>${ACCENTS.map(([id,color,label])=>`<button type="button" data-ftc-accent="${color}" class="${state.data.accent===color?'is-selected':''}" style="--swatch:${color}" aria-label="${label}" aria-pressed="${state.data.accent===color}" title="${label}"><i></i><span>${label}</span></button>`).join('')}</div></div></article>`;}
  function readyPanel(state){const d=state.data,p=d.tripPreferences||{},dates=d.scheduleMode==='flexible'?d.flexibility:(d.startDate&&d.endDate?`${dateLabel(d.startDate)} – ${dateLabel(d.endDate)}`:'Noch offen'),mode={guided:'Geführt',quick:'Schnellstart',ai:'Luvia AI'}[d.entryMode]||'Geführt',chosen=(state.draftSelections||[]).filter(item=>item.action!=='excluded'),planned=chosen.filter(item=>item.action==='planned').length,saved=chosen.filter(item=>item.action==='saved').length;return `<article class="ftc-panel ftc-ready"><div class="ftc-ready-visual" style="--ftc-accent:${esc(d.accent)}">${compassMarkup('is-result')}<span class="ftc-ready-symbol">${esc(d.symbol)}</span><i></i></div><div class="ftc-ready-copy"><span class="ftc-kicker">Eure Auswahl · ${mode}</span><h1>${esc(d.title||'Eure Reise')}</h1><p>${esc(d.subtitle||'Eine neue gemeinsame Richtung.')}</p><dl><div><dt>Reiseziel</dt><dd>${esc(d.destination.formattedAddress||d.destination.name)}</dd></div><div><dt>Zeitraum</dt><dd>${esc(dates)}</dd></div><div><dt>Reisewünsche</dt><dd>${d.entryMode==='ai'?`${(p.interests||[]).length} Schwerpunkte · ${BUDGETS.find(item=>item[0]===p.budgetLevel)?.[1]||'offen'}`:(d.feelings.map(id=>esc(FEELINGS.find(item=>item[0]===id)?.[1]||id)).join(' · ')||'Offen')}</dd></div><div><dt>${d.entryMode==='ai'?'Tagesentwurf':'Bausteine'}</dt><dd>${d.entryMode==='ai'?`${planned} einplanen · ${saved} vormerken`:`${d.modules.length} aktiviert`}</dd></div></dl>${d.entryMode==='ai'?`<div class="ftc-scope-summary"><span><b>Anfrage</b><small>nur für diesen Entwurf</small></span><span><b>Reise</b><small>eure Reisewünsche</small></span><span><b>Profil</b><small>${d.durablePreferenceProposal.requested?'separate Bestätigung folgt':'bleibt unverändert'}</small></span></div>`:''}${state.error?`<div class="ftc-error" role="alert"><strong>Die Bestätigung ist noch nicht vollständig.</strong><span>${esc(state.error)}</span></div>`:''}<button type="button" class="ftc-create" data-ftc-create ${state.saving?'disabled':''}>${state.saving?'Reise und Auswahl werden gespeichert …':state.confirmation?.status==='complete'?'Gespeicherte Reise öffnen':state.confirmation?'Übernahme fortsetzen':d.entryMode==='ai'?`Reise und ${chosen.length} Vorschläge bestätigen`:'Diese Reise beginnen'} <span>${state.saving?'☁':'→'}</span></button><small>Ihr bestätigt die Reise und die gewählten Orte. Euer Profil bleibt unverändert.</small></div></article>`;}
  function panelMarkup(state){return ({welcome:welcomePanel,identity:identityPanel,feeling:feelingPanel,destination:destinationPanel,dates:datesPanel,people:peoplePanel,modules:modulesPanel,brief:briefPanel,preview:previewPanel,accent:accentPanel,ready:readyPanel})[currentStep(state)](state);}
  function progressMarkup(state){const flow=currentFlow(state);return `<nav class="ftc-progress" aria-label="Fortschritt der Reiseerstellung">${flow.map((step,index)=>`${index?`<b class="${index<=state.index?'is-complete':''}"></b>`:''}<button type="button" data-ftc-progress="${index}" class="${index===state.index?'is-active':''} ${index<state.index?'is-complete':''}" ${index>state.highest?'disabled':''} aria-current="${index===state.index?'step':'false'}"><i>${index<state.index?'✓':index+1}</i><span>${STEP_LABELS[step]}</span></button>`).join('')}</nav>`;}
  function render(state,{focus=false}={}){
    if(mounted!==state||state.viewerId!==userId())return;
    const sameStep=state.renderedStep===currentStep(state),scrollTop=sameStep?(state.root.querySelector('.ftc-stage')?.scrollTop||0):0;
    const mapNode=currentStep(state)==='preview'&&state.aiDraft?.status==='ready'&&state.mapSignature===state.aiDraft.signature?state.root.querySelector('[data-ftc-ai-map]'):null;
    if(!mapNode){state.mapProjection?.destroy?.();state.mapProjection=null;}
    const step=currentStep(state),copy=STEP_COPY[step];
    if(step==='preview'&&state.data.entryMode==='ai'&&!state.confirmation&&['ready','brief-ready','error'].includes(state.aiDraft.status)&&(state.aiDraft.signature!==aiSignature(state)||state.aiDraft.profileSignature!==profileSignature()))state.aiDraft={status:'idle'};
    if(state.aiDraft.status==='ready')state.aiDraft.rehearsals=rehearseDraft(state);
    persist(state);
    state.root.innerHTML=`<section class="ftc" data-first-trip-composer data-step="${step}" data-mode="${state.mode}" data-entry-mode="${state.data.entryMode}" style="--ftc-accent:${esc(state.data.accent)}">${worldMarkup(state)}<header class="ftc-header"><button type="button" class="ftc-brand" data-ftc-close aria-label="Reiseerstellung schließen">${compassMarkup('is-brand')}<span><strong>LUVIA</strong><small>Eine neue Richtung beginnt.</small></span></button><button type="button" class="ftc-quiet" data-ftc-close>${state.mode==='first-use'?'Später fortsetzen':'Später fortsetzen'}</button></header>${progressMarkup(state)}<main class="ftc-stage" aria-live="polite">${panelMarkup(state)}</main><footer class="ftc-footer"><button type="button" class="ftc-back" data-ftc-back ${state.index===0?'hidden':''}><span>←</span> Zurück</button><div><small>${copy[0]}</small><strong>${copy[1]}</strong></div><button type="button" class="ftc-next" data-ftc-next ${step==='ready'?'hidden':''}>${copy[2]} <span>→</span></button></footer><div class="ftc-toast" data-ftc-toast role="status" aria-live="polite" hidden><i>!</i><span><strong></strong><small></small></span></div></section>`;
    state.root.querySelector('.ftc')?.setAttribute('data-transition',String(!sameStep));state.renderedStep=step;
    if(mapNode&&state.aiDraft.status==='ready'){state.root.querySelector('[data-ftc-ai-map]')?.replaceWith(mapNode);state.mapProjection?.update?.(mapPlaces(state));}else if(mapNode){state.mapProjection?.destroy?.();state.mapProjection=null;}
    const stage=state.root.querySelector('.ftc-stage');if(stage){stage.scrollTop=scrollTop;if(state.data.destination?.placeId){const ribbon=document.createElement('aside');ribbon.className='ftc-journey-ribbon';ribbon.setAttribute('aria-label','Eure Reise nimmt Form an');ribbon.innerHTML=`<i>${esc(state.data.symbol)}</i><span><small>EURE REISE NIMMT FORM AN</small><strong>${esc(state.data.destination.name)}</strong></span><b>${esc(state.data.scheduleMode==='flexible'?state.data.flexibility:validDate(state.data.startDate)?dateLabel(state.data.startDate):'Zeitraum wählen')}</b>`;stage.prepend(ribbon);}}
    if(state.confirmation)state.root.querySelectorAll('button,input,textarea,select').forEach(node=>{if(!node.matches('[data-ftc-create],[data-ftc-close]'))node.disabled=true;else if(state.saving)node.disabled=true;});
    bind(state);requestAnimationFrame(()=>state.root.querySelector('.ftc')?.classList.add('is-ready'));if(step==='preview'&&state.data.entryMode==='ai'){if(state.aiDraft.status==='idle')queueMicrotask(()=>loadAiDayDraft(state));else if(state.aiDraft.status==='ready')requestAnimationFrame(()=>mountAiMap(state));}if(focus)requestAnimationFrame(()=>state.root.querySelector('.ftc-panel input,.ftc-panel button,.ftc-panel h1')?.focus?.({preventScroll:true}));
  }
  function toast(state,title,message){clearTimeout(state.toastTimer);const node=state.root.querySelector('[data-ftc-toast]');if(!node)return;node.querySelector('strong').textContent=title;node.querySelector('small').textContent=message;node.hidden=false;state.toastTimer=setTimeout(()=>{if(node.isConnected)node.hidden=true},3400);}
  function validate(state,index=state.index){
    const step=currentFlow(state)[index],d=state.data;
    if(step==='identity'&&!d.title.trim())return ['Ein Name fehlt','Gebt eurer Reise einen Namen.'];
    if(step==='feeling'&&!d.feelings.length)return ['Ein Gefühl fehlt','Wählt mindestens ein Reisegefühl.'];
    if(step==='destination'&&!d.destination?.placeId)return ['Der Ort ist noch nicht bestätigt','Wählt bitte einen Vorschlag aus der Suche.'];
    if(step==='dates'&&d.scheduleMode==='fixed'&&(!validDate(d.startDate)||!validDate(d.endDate)||d.endDate<d.startDate))return ['Der Zeitraum passt noch nicht','Bitte gültige Anreise und Rückreise eintragen. Die Rückreise darf nicht vor der Anreise liegen.'];
    if(step==='preview'&&state.aiDraft?.status!=='ready')return ['Entwurf noch nicht bereit','Bitte wartet auf die Vorschläge oder versucht es erneut.'];
    if(step==='dates'&&d.entryMode==='ai'&&d.scheduleMode==='fixed'&&!validZone(tripZone(state)))return ['Reisezeitzone fehlt','Wählt die Zeitzone eures Reiseziels für eindeutige Uhrzeiten.'];
    if(step==='modules'&&!d.modules.length)return ['Kein Reisebaustein aktiv','Wählt mindestens einen Bereich.'];
    return null;
  }
  function projectStep(state,replace=false){const nav=window.LuviaNavigationContractV1,history=window.LuviaNavigationHistoryV1;if(!nav?.createIntent||!history?.project)return;history.project(nav.createIntent(ROUTE,{source:'first-trip-composer',replace,params:{tripStep:currentStep(state),tripMode:state.mode,tripEntry:state.data.entryMode,returnTo:state.options.returnTo||'today'}}),{replace,source:'first-trip-composer'});}
  function go(state,index,{skipValidation=false}={}){const flow=currentFlow(state);if(state.saving||state.confirmation||index<0||index>=flow.length)return;if(currentStep(state)==='welcome'&&!state.openingComplete&&index>0){state.openingComplete=true;render(state,{focus:true});return;}if(index>state.index&&!skipValidation){const problem=validate(state);if(problem)return toast(state,...problem);}state.index=index;state.highest=Math.max(state.highest,index);projectStep(state);render(state,{focus:true});}
  async function places(){const api=window.LuviaPlacesContractV1;if(!api?.reads?.suggestDestinations||!api?.reads?.getDestination)throw new Error('Der öffentliche Places-Contract ist noch nicht verfügbar.');return api;}
  function within(promise,timeoutMs=8500,message='Die Zielsuche hat zu lange gedauert.',code='PLACES_DESTINATION_TIMEOUT'){return new Promise((resolve,reject)=>{const timer=setTimeout(()=>{const error=new Error(message);error.code=code;reject(error)},timeoutMs);Promise.resolve(promise).then(value=>{clearTimeout(timer);resolve(value)},error=>{clearTimeout(timer);reject(error)})})}
  async function details(placeId){const api=await places();const destination=await api.reads.getDestination(placeId,{languageCode:'de',regionCode:'DE',sessionToken:placesSessionToken});if(destination?.owner!=='places'||destination?.contractId!=='places.v1'||!destination?.placeId)throw new Error('Places hat das Reiseziel nicht kanonisch bestätigt.');return {name:destination.name,formattedAddress:destination.formattedAddress,country:destination.country,countryCode:destination.countryCode,placeId:destination.placeId,latitude:destination.latitude??null,longitude:destination.longitude??null,timezone:destination.timezone||''};}
  const providerId=place=>String(place?.providerPlaceId||place?.provider_place_id||place?.id||'').replace(/^places\//,'').trim();
  const placeCoordinates=place=>{const source=place?.coordinates||place?.location||{},lat=source.latitude??source.lat,lng=source.longitude??source.lng,latitude=Number(lat),longitude=Number(lng);return lat!=null&&lng!=null&&lat!==''&&lng!==''&&Number.isFinite(latitude)&&Number.isFinite(longitude)&&Math.abs(latitude)<=90&&Math.abs(longitude)<=180?{latitude,longitude}:null};
  const validDate=value=>/^\d{4}-\d{2}-\d{2}$/.test(value||'')&&!Number.isNaN(Date.parse(value+'T12:00:00Z'))&&new Date(value+'T12:00:00Z').toISOString().slice(0,10)===value;
  const placeLabel=place=>({restaurant:'Restaurant',cafe:'Café',museum:'Museum',art_gallery:'Galerie',park:'Park',bar:'Bar',night_club:'Club',tourist_attraction:'Sehenswürdigkeit',sports_activity_location:'Aktivität',spa:'Wellness'}[place.primaryType]||'Ort zum Entdecken');
  const dateLabel=value=>validDate(value)?new Intl.DateTimeFormat('de-DE',{day:'2-digit',month:'2-digit',year:'numeric',timeZone:'UTC'}).format(new Date(value+'T12:00:00Z')):'Termin offen';
  const tripZone=state=>state.data.destination?.timezone||'';
  const validZone=value=>{try{if(!value)return false;new Intl.DateTimeFormat('de-DE',{timeZone:value});return true}catch{return false}};
  const currentPreferences=()=>{const identity=window.LuviaIdentityContractV1,read=identity?.reads?.getPreferences||identity?.getPreferences;if(typeof read!=='function')throw new Error('Eure Profilvorlieben sind noch nicht verfügbar. Bitte erneut versuchen.');return read('self')};
  const profileSignature=()=>{try{return JSON.stringify(currentPreferences())}catch{return null}};
  const aiSignature=state=>JSON.stringify({destination:state.data.destination,startDate:state.data.startDate,endDate:state.data.endDate,scheduleMode:state.data.scheduleMode,tripPreferences:state.data.tripPreferences,feelings:state.data.feelings,brief:state.data.requestBrief||'',answers:state.briefAnswers||[]});
  const effectiveTripPreferences=state=>state.aiDraft?.brief?.tripPreferences||state.data.tripPreferences||{};
  function aiCategoryRequests(state){const interests=effectiveTripPreferences(state).interests||[],ids=interests.length?interests:['food','culture','active'],seen=new Set();return ids.map(id=>AI_CATEGORIES[id]).filter(item=>item&&!seen.has(item[0])&&seen.add(item[0]))}
  function reusablePlaces(api,state,category){const snapshot=api.reads?.getActiveDiscovery?.({destination:state.data.destination?.name,surface:'places',fitOnly:true,maxAgeMs:10*60*1000}),center=placeCoordinates({coordinates:state.data.destination});if(snapshot?.category!==category||!snapshot?.places?.length||!center)return[];const radius=api.reads?.localSearchRadius?.(state.data.destination)||5000;return snapshot.places.filter(place=>{const point=placeCoordinates(place);if(!point||place.profileFit?.state!=='matched')return false;const lat=(point.latitude-center.latitude)*111320,lng=(point.longitude-center.longitude)*111320*Math.cos(center.latitude*Math.PI/180);return Math.hypot(lat,lng)<=radius}).map(place=>({...place,requestCategory:category}))}
  async function readAiPlaces(state){
    const api=await places();if(typeof api.reads?.recommend!=='function')throw new Error('Die Ortsvorschläge sind gerade nicht erreichbar.');
    const originalProfile=await currentPreferences(),profilePreferences=state.aiDraft?.brief?.requestPreferences||originalProfile,destination={...state.data.destination,center:{lat:Number(state.data.destination.latitude),lng:Number(state.data.destination.longitude)}},requests=aiCategoryRequests(state),lists=[],coverage=[];
    // Each category uses the canonical cache/provider router. At most three reads run together.
    for(let offset=0;offset<requests.length;offset+=3){
      const batch=requests.slice(offset,offset+3),results=await Promise.allSettled(batch.map(async([category,label])=>{
        const cached=state.aiDraft?.brief?[]:reusablePlaces(api,state,category);if(cached.length>=4)return {places:cached.slice(0,4),source:'cache'};
        const result=await within(api.reads.recommend({destination,category,text:label,query:label,subjectText:'',tripComposition:{tripPreferences:effectiveTripPreferences(state)},preferences:profilePreferences,requirePreferenceEvidence:true,fastPath:true,fastQueryLimit:1,providers:['auto'],candidateLimit:20,limit:4,providerTimeoutMs:3000,languageCode:'de',regionCode:state.data.destination.countryCode||'DE'}),6500,'Die Ortsquelle hat nicht rechtzeitig geantwortet.','PLACES_RECOMMEND_TIMEOUT');
        return {places:[...cached,...(result?.places||[])].filter(place=>place.recommendation?.constraintState!=='blocked').map(place=>({...place,requestCategory:category})),source:'provider',degraded:result?.providerDiagnostics?.degraded===true||result?.providerDiagnostics?.status==='unavailable'};
      }));
      results.forEach((result,index)=>{const [category,label]=batch[index],value=result.status==='fulfilled'?result.value:null,found=value?.places||[];lists.push(found);coverage.push({category,label,count:found.length,status:result.status==='rejected'||value?.degraded?'unavailable':found.length?'ready':'empty'});});
    }
    const interleaved=[];for(let index=0;index<Math.max(0,...lists.map(list=>list.length));index++)for(const list of lists)if(list[index])interleaved.push(list[index]);
    const seen=new Set(),found=interleaved.filter(place=>{const id=providerId(place);if(!id||!place?.name||!placeCoordinates(place)||seen.has(id))return false;seen.add(id);return true}).slice(0,24);
    return {places:found,coverage,profileSignature:JSON.stringify(originalProfile)};
  }
  async function hydrateAiMedia(state,signature,ids=null){const api=await places();if(typeof api.reads?.getCard!=='function'||state.aiDraft.status!=='ready')return;const allowed=ids?new Set(ids):null,targets=state.draftSelections.filter(item=>!item.imageUrl&&(!allowed||allowed.has(item.providerPlaceId))).slice(0,6),sourceById=new Map(state.aiDraft.places.map(place=>[providerId(place),place]));if(!targets.length)return;const cards=await Promise.allSettled(targets.map(item=>within(api.reads.getCard(item.providerPlaceId,{source:sourceById.get(item.providerPlaceId),maxWidthPx:720,maxHeightPx:520}),5500,'Das Bild dieses Places ist noch nicht verfügbar.','PLACES_MEDIA_TIMEOUT')));if(state.aiDraft.signature!==signature||mounted!==state||state.confirmation)return;const media=new Map();cards.forEach((result,index)=>{const url=result.status==='fulfilled'?result.value?.image?.url:null;if(url)media.set(targets[index].providerPlaceId,url)});if(!media.size)return;state.draftSelections=state.draftSelections.map(item=>media.has(item.providerPlaceId)?{...item,imageUrl:media.get(item.providerPlaceId)}:item);state.aiDraft.places=state.aiDraft.places.map(place=>media.has(providerId(place))?{...place,image:{...(place.image||{}),url:media.get(providerId(place))}}:place);const nextDraft=clone(state.aiDraft.draft);nextDraft.days=(nextDraft.days||[]).map(day=>({...day,entries:(day.entries||[]).map(item=>media.has(item.providerPlaceId)?{...item,imageUrl:media.get(item.providerPlaceId)}:item)}));nextDraft.alternatives=(nextDraft.alternatives||[]).map(item=>media.has(item.providerPlaceId)?{...item,imageUrl:media.get(item.providerPlaceId)}:item);state.aiDraft.draft=nextDraft;persist(state);if(currentStep(state)==='preview')render(state)}
  function zonedIso(date,time,timeZone){return window.LuviaIntelligenceActionContractCoreV1?.zonedDateTimeToIso?.(date,time,timeZone)||null}
  function rehearseDraft(state){
    const draft=state.aiDraft?.draft||{},rehearse=window.LuviaJourneyContractV1?.reads?.rehearseDay,timeZone=tripZone(state);
    return (draft.days||[]).map(day=>{
      const selected=(state.draftSelections||[]).filter(entry=>entry.action==='planned'&&entry.date===day.date);
      if(!day.date||!selected.length)return {dayId:day.id,status:'empty',statusLabel:'Freiraum',summary:'Hier ist noch nichts fest eingeplant.'};
      if(!validZone(timeZone)||typeof rehearse!=='function')return {dayId:day.id,status:'unknown',statusLabel:'Zeitprüfung offen',summary:'Die Reisezeitzone oder Tagesprüfung ist noch nicht verfügbar.'};
      const entries=selected.map(entry=>{const startAt=zonedIso(entry.date,entry.time,timeZone),endAt=startAt?new Date(Date.parse(startAt)+Number(entry.durationMinutes)*60000).toISOString():null;return {id:entry.slotId,title:entry.name,startAt,endAt,durationMinutes:entry.durationMinutes,routeMode:'walking',routeEvidence:[]}});
      if(entries.some(entry=>!entry.startAt||!entry.endAt))return {dayId:day.id,status:'unknown',statusLabel:'Uhrzeit prüfen',summary:'Mindestens eine Uhrzeit ist in der Reisezeitzone nicht eindeutig.'};
      try{const result=rehearse({entries,travelSpeed:effectiveTripPreferences(state).pace==='slow'?'slow':'balanced',contextEvidence:[]}),labels={ready:'Keine Terminüberschneidung',attention:'Puffer prüfen',blocked:'Konflikt'};return {dayId:day.id,status:result?.status||'unknown',statusLabel:labels[result?.status]||'Prüfung offen',summary:result?.status==='blocked'?'Die gewählten Zeiten überschneiden sich oder lassen zu wenig Wegpuffer.':entries.length+' Reisemomente · '+entries.reduce((sum,item)=>sum+Number(item.durationMinutes),0)+' Min. Programm. Wege und Öffnungszeiten sind noch zu prüfen.',raw:result};}catch{return {dayId:day.id,status:'unknown',statusLabel:'Prüfung nicht erreichbar',summary:'Die Tagesprüfung konnte gerade nicht abgeschlossen werden.'};}
    });
  }
  function briefFailureMessage(error){
    if(error?.code==='AI_RATE_LIMITED')return 'Luvia AI nimmt gerade keine weiteren Anfragen an. Bitte versucht es später erneut. Eure Angaben bleiben erhalten.';
    if(['AI_EDGE_ERROR','AI_PAYLOAD_CIRCUIT_OPEN'].includes(error?.code)||/Edge Function returned a non-2xx/i.test(error?.message||''))return 'Luvia AI ist gerade nicht verfügbar. Eure Angaben bleiben erhalten. Ihr könnt sie bearbeiten und den Entwurf später erneut starten.';
    return error?.message||'Der Reiseentwurf konnte noch nicht geladen werden.';
  }
  async function loadAiDayDraft(state,{force=false,confirmedBrief=false}={}){
    const signature=aiSignature(state),initialProfileSignature=profileSignature();if(!force&&state.aiDraft.signature===signature&&state.aiDraft.profileSignature===profileSignature()&&['loading','ready'].includes(state.aiDraft.status))return;
    const brief=confirmedBrief&&state.aiDraft?.signature===signature&&state.aiDraft?.profileSignature===profileSignature()?state.aiDraft.brief:null;
    const sequence=++state.loadSequence;state.aiDraft={brief,status:'loading',signature,profileSignature:profileSignature(),draft:null,places:[],rehearsals:[],error:null};state.draftSelections=[];render(state);
    const current=()=>mounted===state&&state.viewerId===userId()&&sequence===state.loadSequence&&aiSignature(state)===signature&&!state.confirmation;
    try{
      if(state.data.requestBrief.trim()&&!brief){
        const interpret=window.LuviaIntelligenceContractV1?.reads?.interpretTripBrief;
        if(typeof interpret!=='function')throw new Error('Die KI für euren Reiseauftrag ist gerade nicht verfügbar. Bitte erneut versuchen.');
        const interpreted=await within(interpret({...clone(state.data),profilePreferences:clone(currentPreferences()),answers:clone(state.briefAnswers||[])}),20000,'Luvia braucht gerade zu lange, um eure Wünsche zu verstehen. Bitte erneut versuchen.','TRIP_BRIEF_TIMEOUT');
        if(!current())return;if(initialProfileSignature!==profileSignature())throw new Error('Eure Profilvorlieben haben sich geändert. Bitte erneut prüfen.');
        if(interpreted?.kind!=='trip-planning-brief'||interpreted?.owner!=='intelligence'||interpreted?.source!=='ai')throw new Error('Der Reiseauftrag konnte noch nicht zuverlässig bestätigt werden.');
        state.aiDraft={status:'brief-ready',signature,profileSignature:profileSignature(),brief:interpreted,places:[],rehearsals:[]};render(state);return;
      }
      const owner=trip();if(typeof owner?.composition?.composeDayDraft!=='function')throw new Error('Der Reiseentwurf ist noch nicht verfügbar.');const result=await readAiPlaces(state);if(!current())return;if(initialProfileSignature!==profileSignature())throw new Error('Eure Profilvorlieben haben sich geändert. Bitte erneut prüfen.');if(!result.places.length)throw new Error('Für diese Auswahl sind gerade keine überprüften Vorschläge verfügbar. Das bedeutet nicht, dass es vor Ort keine passenden Orte gibt.');const draft=owner.composition.composeDayDraft(state.data,{places:result.places,brief,generatedAt:new Date().toISOString()});state.draftSelections=(draft.days||[]).flatMap(day=>day.entries||[]).map(entry=>({...entry,action:entry.suggestedAction||(entry.date?'planned':'saved')}));state.aiDraft={status:'ready',brief,signature,profileSignature:result.profileSignature,draft,places:result.places,coverage:result.coverage,rehearsals:[],error:null};state.aiDraft.rehearsals=rehearseDraft(state);render(state);queueMicrotask(()=>hydrateAiMedia(state,signature).catch(()=>{}));
    }catch(error){if(!current())return;state.aiDraft={status:'error',signature,profileSignature:profileSignature(),draft:null,places:[],rehearsals:[],error:briefFailureMessage(error)};render(state,{focus:true});}
  }
  function mapPlaces(state){const selected=new Set(state.draftSelections.filter(item=>item.action!=='excluded').map(item=>item.providerPlaceId));return state.aiDraft.places.filter(place=>selected.has(providerId(place)))}
  function mountAiMap(state){if(state.mapProjection)return;const container=state.root.querySelector('[data-ftc-ai-map]'),spatial=window.LuviaPlacesSpatialExperience;if(!container)return;if(typeof spatial?.mountProjection!=='function'){container.innerHTML='<span class="ftc-ai-map-fallback">Eure Vorschläge bleiben verfügbar. Die Karte konnte gerade nicht geladen werden.</span>';return}const selected=state.draftSelections.find(item=>item.action!=='excluded');state.mapSignature=state.aiDraft.signature;state.mapProjection=spatial.mountProjection(container,mapPlaces(state),{selectedId:selected?.providerPlaceId||null,initialCenter:{lat:Number(state.data.destination.latitude),lng:Number(state.data.destination.longitude)},category:'all',label:'Orte für eure Reise',onSelect:id=>{const normalized=String(id).replace(/^places\//,''),card=Array.from(state.root.querySelectorAll('[data-ftc-ai-place]')).find(node=>node.dataset.ftcAiPlace===normalized);card?.scrollIntoView?.({behavior:reduceMotion()?'auto':'smooth',block:'center'});card?.classList.add('is-map-selected');setTimeout(()=>card?.classList.remove('is-map-selected'),1400)}})}
  function refreshDraft(state){state.aiDraft.rehearsals=rehearseDraft(state);persist(state);render(state)}
  function setDraftAction(state,slotId,action){if(state.confirmation||state.saving)return;const item=state.draftSelections.find(entry=>entry.slotId===slotId);if(!item||!['planned','saved','excluded'].includes(action)||(!item.date&&action==='planned')||item.action===action)return;item.action=action;refreshDraft(state)}
  function swapDraftSelection(state,slotId){
    if(state.confirmation||state.saving)return;const index=state.draftSelections.findIndex(entry=>entry.slotId===slotId),current=state.draftSelections[index];if(!current)return;
    const used=new Set(state.draftSelections.map(entry=>entry.providerPlaceId)),alternative=(state.aiDraft.draft?.alternatives||[]).find(place=>!used.has(place.providerPlaceId));if(!alternative)return toast(state,'Keine weitere Alternative','Für diesen Entwurf ist gerade kein zusätzlicher überprüfter Ort vorhanden.');
    const replacement={...alternative,reason:'Von euch als Alternative gewählt. Wege, Öffnung und Preise für diesen Besuch bleiben zu prüfen.',slotId:current.slotId,dayId:current.dayId,date:current.date,time:current.time,durationMinutes:current.durationMinutes,confirmationRequired:true,automaticMutation:false,action:current.action};
    state.aiDraft.draft={...state.aiDraft.draft,alternatives:[...state.aiDraft.draft.alternatives.filter(place=>place.providerPlaceId!==alternative.providerPlaceId),current]};state.draftSelections[index]=replacement;refreshDraft(state);
    if(!replacement.imageUrl)queueMicrotask(()=>hydrateAiMedia(state,state.aiDraft.signature,[replacement.providerPlaceId]).catch(()=>{}));
  }
  function confirmationProblem(state){
    if(state.confirmation)return null;
    for(let index=0;index<currentFlow(state).length;index++){const problem=validate(state,index);if(problem)return problem[1];}
    if(!state.data.title.trim()||!state.data.destination?.placeId||!state.data.modules.length)return 'Bitte Name, Reiseziel und Reisebausteine vervollständigen.';
    if(state.data.entryMode!=='ai')return null;
    if(state.aiDraft.status!=='ready'||state.aiDraft.signature!==aiSignature(state)||state.aiDraft.profileSignature!==profileSignature())return 'Eure Angaben haben sich geändert. Bitte den Entwurf erneut öffnen und prüfen.';
    for(const item of state.draftSelections.filter(item=>item.action==='planned')){
      if(!validDate(item.date)||item.date<state.data.startDate||item.date>state.data.endDate||!zonedIso(item.date,item.time,tripZone(state))||!Number.isInteger(Number(item.durationMinutes))||item.durationMinutes<15||item.durationMinutes>720)return 'Bitte Termin und Dauer von '+item.name+' prüfen.';
    }
    const rehearsals=rehearseDraft(state);if(rehearsals.some(day=>['blocked','unknown'].includes(day.status)))return 'Bitte die offenen Zeitprüfungen oder Überschneidungen im Entwurf lösen. Ihr könnt die betreffenden Orte auch vormerken.';
    return null;
  }
  async function commitAiDayDraft(state,receipt){
    const choices=state.draftSelections.filter(item=>item.action!=='excluded'),runtime=window.LuviaAIActionRuntime,timeZone=tripZone(state),tripId=receipt.tripId;
    if(choices.length&&(typeof runtime?.prepare!=='function'||typeof runtime?.execute!=='function'))throw new Error('Die ausgewählten Orte können gerade nicht gespeichert werden.');
    for(const item of choices){
      if(state.viewerId!==userId())throw new Error("Die Anmeldung hat sich geändert. Bitte mit dem ursprünglichen Konto fortsetzen.");
      if(state.confirmation.actionReceipts[item.slotId])continue;
      const planned=item.action==='planned',actionId=planned?'places.place.plan':'places.place.favorite',plannedAt=planned?zonedIso(item.date,item.time,timeZone):null;
      if(planned&&!plannedAt)throw new Error('Bitte die Uhrzeit für '+item.name+' prüfen.');
      const payload={tripId,providerPlaceId:item.providerPlaceId,placeType:item.primaryType,name:item.name,...(planned?{date:item.date,time:item.time,fields:{planned_at:plannedAt,metadata:{durationMinutes:Number(item.durationMinutes)},place_name:item.name,notes:'Über den bestätigten Luvia-Reiseentwurf'}}:{})};
      const idempotencyKey=state.idempotencyKey+':draft:'+item.slotId+':'+item.action;
      state.confirmation.attempts=state.confirmation.attempts||{};const attempt=state.confirmation.attempts[item.slotId];
      if(attempt?.status==='outcome_unknown')throw new Error('Der Speicherstand von '+item.name+' ist noch unklar. Bitte zuerst in der Reise prüfen; dieser Vorgang wird nicht blind wiederholt.');
      persist(state,{required:true});
      const prior=attempt?.ledgerId?runtime.getActionState?.(attempt.ledgerId):null;
      const retryable=prior?.status==='failed'&&prior?.outcomeUnknown!==true&&typeof runtime.retry==='function';
      const prepared=retryable?{ledgerId:attempt.ledgerId}:runtime.prepare(actionId,payload,{userGesture:true,surface:'first-trip-composer',timeZone,idempotencyKey});
      if(!prepared?.ledgerId)throw new Error('Die Auswahl für '+item.name+' konnte nicht vorbereitet werden.');
      state.confirmation.attempts[item.slotId]={ledgerId:prepared.ledgerId,status:'pending'};persist(state,{required:true});
      const ownerReceipt=retryable?await runtime.retry(prepared.ledgerId,{userGesture:true,confirmed:true}):await runtime.execute(actionId,{}, {ledgerId:prepared.ledgerId,userGesture:true,confirmed:true}),evidence=ownerReceipt?.evidence;
      state.confirmation.attempts[item.slotId]={ledgerId:prepared.ledgerId,status:evidence?.status==='failed'?'failed':'outcome_unknown'};persist(state,{required:true});
      if(ownerReceipt?.kind!=='receipt'||ownerReceipt?.owner!=='places'||evidence?.status!=='completed'||evidence?.actionId!==actionId||evidence?.ownerCommand!==true||evidence?.idempotencyKey!==idempotencyKey||evidence?.reference?.tripId!==tripId||evidence?.reference?.providerPlaceId!==item.providerPlaceId||evidence?.reference?.readbackVerified!==true)throw new Error(item.name+' wurde noch nicht vollständig bestätigt. Bitte Übernahme fortsetzen; bereits gespeicherte Einträge bleiben erhalten.');
      state.confirmation.attempts[item.slotId].status='completed';state.confirmation.actionReceipts[item.slotId]=clone(ownerReceipt);persist(state,{required:true});
    }
    return Object.values(state.confirmation.actionReceipts);
  }
  function bindDestination(state,input){if(!input)return;input.addEventListener('input',event=>{state.data.destination={name:'',formattedAddress:'',country:'',countryCode:'',placeId:'',latitude:null,longitude:null};persist(state);clearTimeout(searchTimer);const sequence=++searchSequence,value=event.target.value.trim(),box=state.root.querySelector('[data-ftc-suggestions]'),proof=state.root.querySelector('[data-ftc-place-proof]');if(proof){proof.classList.remove('is-confirmed');proof.innerHTML=value.length<2?'<i>⌖</i><span><strong>Noch kein Ort bestätigt</strong><small>Gebt mindestens zwei Zeichen ein.</small></span>':'<i>⌖</i><span><strong>Suche läuft …</strong><small>Maximal wenige Sekunden.</small></span>'}if(value.length<2){if(box){box.className='';box.innerHTML=''}return;}searchTimer=setTimeout(async()=>{try{const api=await places();const response=await within(api.reads.suggestDestinations(value,{sessionToken:placesSessionToken,regionCode:(navigator.language||'de-DE').split('-')[1]||'DE',timeoutMs:7000}),8500);if(sequence!==searchSequence||mounted!==state)return;placesSessionToken=response?.sessionToken||placesSessionToken;const suggestions=response?.suggestions||[];if(box){box.className='ftc-suggestions';box.innerHTML=suggestions.slice(0,6).map(item=>`<button type="button" data-ftc-place-id="${esc(item.placeId)}" data-ftc-place-text="${esc(item.text)}"><i>⌖</i><span>${esc(item.text)}</span></button>`).join('')||'<span class="ftc-no-results">Keine passenden Orte gefunden.</span>'}if(proof){proof.innerHTML=suggestions.length?'<i>⌖</i><span><strong>Vorschläge bereit</strong><small>Bitte einen bestätigten Ort auswählen.</small></span>':'<i>⌖</i><span><strong>Kein Ort gefunden</strong><small>Prüft die Schreibweise oder erweitert die Suche.</small></span>';}}catch(error){if(sequence!==searchSequence||mounted!==state)return;if(box){box.className='ftc-suggestions';box.innerHTML='<button type="button" class="ftc-destination-retry" data-ftc-destination-retry>Erneut suchen</button>'}if(proof){proof.innerHTML='<i>!</i><span><strong>Zielsuche nicht erreichbar</strong><small>Ihr könnt sie direkt erneut starten.</small></span>'}toast(state,'Places antwortet gerade nicht',error?.message||'Bitte versucht die Suche gleich noch einmal.');}},260);});}
  async function save(state){
    if(state.saving||state.viewerId!==userId())return;const problem=confirmationProblem(state);if(problem){state.error=problem;return render(state,{focus:true});}
    const owner=trip();if(!owner?.commands?.createFirstTrip){state.error='Die Reise kann gerade nicht gespeichert werden.';return render(state,{focus:true});}
    state.confirmation=state.confirmation||{status:'pending',tripReceipt:null,actionReceipts:{}};state.saving=true;state.error=null;render(state);
    try{
      persist(state,{required:true});
      if(!state.confirmation.tripReceipt){const receipt=await owner.commands.createFirstTrip({...state.data,tripPreferences:effectiveTripPreferences(state),idempotencyKey:state.idempotencyKey},{idempotencyKey:state.idempotencyKey});if(receipt?.owner!=='trip'||receipt?.action!=='trip.first.create'||receipt?.status!=='committed'||!receipt?.tripId||receipt?.idempotencyKey!==state.idempotencyKey)throw new Error('Die Reiseerstellung wurde noch nicht eindeutig bestätigt. Bitte dieselbe Übernahme fortsetzen.');state.confirmation.tripReceipt=clone(receipt);persist(state,{required:true});}
      const receipt=state.confirmation.tripReceipt,dayDraftReceipts=state.data.entryMode==='ai'?await commitAiDayDraft(state,receipt):[];
      state.confirmation.status='complete';state.confirmation.result={...receipt,dayDraftReceipts};persist(state,{required:true});clearDeferred();
    }catch(error){state.saving=false;state.error=(state.confirmation.tripReceipt?'Die Reise ist angelegt. '+Object.keys(state.confirmation.actionReceipts).length+' Vorschläge sind bereits bestätigt. ':'')+(error?.message||'Bitte die Übernahme erneut versuchen.');return render(state,{focus:true});}
    state.saving=false;
    if(mounted!==state||state.viewerId!==userId())return;
    try{await state.options.onComplete?.(state.confirmation.result);state.finished=true;removeSession(state.storageKey);}catch{state.error='Die Reise und eure Auswahl sind gespeichert. Die nächste Ansicht konnte nicht geöffnet werden. Bitte erneut öffnen.';render(state,{focus:true});}
  }
  function cancel(state){if(state.saving)return;if(state.mode==='first-use')defer();state.options.onCancel?.({mode:state.mode,entryMode:state.data.entryMode,step:currentStep(state),dirty:Boolean(readDraft())});}
  function bind(state){
    bindStoryOpening(state);
    state.root.querySelector('[data-ftc-next]')?.addEventListener('click',()=>go(state,state.index+1));state.root.querySelector('[data-ftc-back]')?.addEventListener('click',()=>go(state,state.index-1,{skipValidation:true}));state.root.querySelectorAll('[data-ftc-close]').forEach(node=>node.addEventListener('click',()=>cancel(state)));
    state.root.querySelectorAll('[data-ftc-entry-mode]').forEach(button=>button.addEventListener('click',()=>{const entry=button.dataset.ftcEntryMode;if(!ENTRY_FLOWS[entry])return;state.data.entryMode=entry;state.index=0;state.highest=0;persist(state);projectStep(state,true);render(state,{focus:true})}));
    state.root.querySelectorAll('[data-ftc-progress]').forEach(node=>node.addEventListener('click',()=>{const index=Number(node.dataset.ftcProgress);if(index<=state.highest)go(state,index,{skipValidation:true})}));
    state.root.querySelectorAll('[data-ftc-field]').forEach(input=>input.addEventListener('input',()=>{state.data[input.dataset.ftcField]=input.value;if(input.dataset.ftcField==='requestBrief')state.briefAnswers=[];persist(state)}));
    state.root.querySelectorAll('[data-ftc-symbol]').forEach(button=>button.addEventListener('click',()=>{state.data.symbol=button.dataset.ftcSymbol;persist(state);render(state)}));
    state.root.querySelectorAll('[data-ftc-feeling]').forEach(button=>button.addEventListener('click',()=>{const id=button.dataset.ftcFeeling,index=state.data.feelings.indexOf(id);if(index>=0)state.data.feelings.splice(index,1);else if(state.data.feelings.length<3)state.data.feelings.push(id);else return toast(state,'Bis zu drei Gefühle','Entfernt zuerst eines, um ein anderes zu wählen.');persist(state);render(state)}));
    state.root.querySelectorAll('[data-ftc-schedule]').forEach(button=>button.addEventListener('click',()=>{state.data.scheduleMode=button.dataset.ftcSchedule;persist(state);render(state)}));
    state.root.querySelectorAll('[data-ftc-flexibility]').forEach(button=>button.addEventListener('click',()=>{state.data.flexibility=button.dataset.ftcFlexibility;persist(state);render(state)}));
    state.root.querySelectorAll('[data-ftc-people]').forEach(button=>button.addEventListener('click',()=>{state.data.participantPlan=button.dataset.ftcPeople;state.data.privacy=state.data.participantPlan==='invite-after-creation'?'invite-only':'private';persist(state);render(state)}));
    state.root.querySelectorAll('[data-ftc-privacy]').forEach(button=>button.addEventListener('click',()=>{state.data.privacy=button.dataset.ftcPrivacy;persist(state);render(state)}));
    state.root.querySelectorAll('[data-ftc-module]').forEach(button=>button.addEventListener('click',()=>{const id=button.dataset.ftcModule,index=state.data.modules.indexOf(id);if(index>=0)state.data.modules.splice(index,1);else state.data.modules.push(id);persist(state);render(state)}));
    state.root.querySelectorAll('[data-ftc-accent]').forEach(button=>button.addEventListener('click',()=>{state.data.accent=button.dataset.ftcAccent;persist(state);render(state)}));
    state.root.querySelectorAll('[data-ftc-trip-interest]').forEach(button=>button.addEventListener('click',()=>{const id=button.dataset.ftcTripInterest,values=state.data.tripPreferences.interests||[],index=values.indexOf(id);if(index>=0)values.splice(index,1);else values.push(id);state.data.tripPreferences.interests=values;persist(state);render(state)}));
    state.root.querySelectorAll('[data-ftc-budget]').forEach(button=>button.addEventListener('click',()=>{state.data.tripPreferences.budgetLevel=button.dataset.ftcBudget;persist(state);render(state)}));
    state.root.querySelector('[data-ftc-timezone]')?.addEventListener('change',event=>{state.data.destination.timezone=event.target.value;persist(state)});
    state.root.querySelectorAll('[data-ftc-draft-time]').forEach(input=>input.addEventListener('change',()=>{if(state.confirmation)return;const item=state.draftSelections.find(entry=>entry.slotId===input.dataset.ftcDraftTime);if(!item)return;item.time=input.value;refreshDraft(state)}));
    state.root.querySelectorAll('[data-ftc-draft-duration]').forEach(input=>input.addEventListener('change',()=>{if(state.confirmation)return;const item=state.draftSelections.find(entry=>entry.slotId===input.dataset.ftcDraftDuration);if(!item)return;item.durationMinutes=Number(input.value);refreshDraft(state)}));
    state.root.querySelector('[data-ftc-durable]')?.addEventListener('click',()=>{const requested=!state.data.durablePreferenceProposal.requested;state.data.durablePreferenceProposal={requested,fields:requested?['interests','budgetLevel']:[]};persist(state);render(state)});
    state.root.querySelectorAll('[data-ftc-map-focus]').forEach(button=>button.addEventListener('click',()=>{const item=state.draftSelections.find(entry=>entry.slotId===button.dataset.ftcMapFocus);if(!item)return;state.root.querySelector('[data-ftc-ai-map]')?.scrollIntoView?.({behavior:reduceMotion()?'auto':'smooth',block:'nearest'});state.mapProjection?.map?.flyTo?.({center:[item.coordinates.longitude,item.coordinates.latitude],zoom:15,duration:reduceMotion()?0:650});}));
    state.root.querySelectorAll('[data-ftc-pace]').forEach(button=>button.addEventListener('click',()=>{state.data.tripPreferences.pace=button.dataset.ftcPace;persist(state);render(state)}));
    state.root.querySelector('[data-ftc-brief-search]')?.addEventListener('click',()=>{if(state.aiDraft.status==='brief-ready'&&!state.aiDraft.brief?.question)loadAiDayDraft(state,{confirmedBrief:true})});
    state.root.querySelector('[data-ftc-brief-answer]')?.addEventListener('click',()=>{const answer=state.root.querySelector('[data-ftc-brief-response]')?.value?.trim();if(!answer)return toast(state,'Eine Antwort fehlt','Schreibt kurz, was euch wichtig ist.');state.briefAnswers=[...(state.briefAnswers||[]),{question:state.aiDraft.brief?.question?.text||'',answer:answer.slice(0,600)}].slice(-5);loadAiDayDraft(state,{force:true})});
    state.root.querySelectorAll('[data-ftc-draft-date]').forEach(input=>input.addEventListener('change',()=>{if(state.confirmation)return;const item=state.draftSelections.find(entry=>entry.slotId===input.dataset.ftcDraftDate),day=state.aiDraft.draft?.days.find(day=>day.date===input.value);if(!item||!day)return;item.date=day.date;item.dayId=day.id;state.activeDay=day.id;refreshDraft(state)}));
    state.root.querySelectorAll('[data-ftc-day]').forEach(button=>button.addEventListener('click',()=>{state.activeDay=button.dataset.ftcDay;render(state);const item=state.draftSelections.find(item=>item.dayId===state.activeDay&&item.action!=='excluded');if(item)state.mapProjection?.map?.flyTo?.({center:[item.coordinates.longitude,item.coordinates.latitude],zoom:13.5,duration:reduceMotion()?0:750})}));
    state.root.querySelector('[data-ftc-ai-retry]')?.addEventListener('click',()=>{state.aiDraft.status='idle';loadAiDayDraft(state,{force:true})});
    state.root.querySelectorAll('[data-ftc-draft-action]').forEach(button=>button.addEventListener('click',()=>setDraftAction(state,button.dataset.ftcDraftSlot,button.dataset.ftcDraftAction)));
    state.root.querySelectorAll('[data-ftc-draft-swap]').forEach(button=>button.addEventListener('click',()=>swapDraftSelection(state,button.dataset.ftcDraftSwap)));
    const destinationInput=state.root.querySelector('[data-ftc-destination]');bindDestination(state,destinationInput);
    state.root.querySelector('[data-ftc-suggestions]')?.addEventListener('click',async event=>{const retry=event.target.closest('[data-ftc-destination-retry]');if(retry){destinationInput?.dispatchEvent(new Event('input',{bubbles:true}));return;}const button=event.target.closest('[data-ftc-place-id]');if(!button)return;button.disabled=true;const sequence=++searchSequence;try{const selected=await within(details(button.dataset.ftcPlaceId),8500);if(sequence!==searchSequence||mounted!==state)return;state.data.destination=selected;if(state.data.entryMode!=='guided'&&!state.data.title.trim()){state.data.title=`Reise nach ${selected.name}`;state.data.subtitle=state.data.entryMode==='ai'?'Mit Luvia geplant':'';}persist(state);render(state,{focus:true});}catch(error){button.disabled=false;toast(state,'Ort noch nicht bestätigt',error?.message||'Places konnte diesen Ort nicht verifizieren.');}});
    state.root.querySelector('[data-ftc-create]')?.addEventListener('click',()=>save(state));
    if(!state.keyHandler){
      state.keyHandler=event=>{if(event.key==='Escape'){event.preventDefault();cancel(state)}else if(event.altKey&&event.key==='ArrowLeft'){event.preventDefault();go(state,state.index-1,{skipValidation:true})}else if(event.altKey&&event.key==='ArrowRight'){event.preventDefault();go(state,state.index+1)}};
      state.root.addEventListener('keydown',state.keyHandler);state.cleanup.push(()=>state.root.removeEventListener('keydown',state.keyHandler));
    }
  }
  function unmount(){if(!mounted)return false;searchSequence++;mounted.loadSequence++;clearTimeout(searchTimer);clearTimeout(mounted.toastTimer);mounted.mapProjection?.destroy?.();mounted.cleanup.forEach(fn=>{try{fn()}catch{}});mounted.root.classList.remove('ftc-host');if(mounted.options.clear!==false)mounted.root.innerHTML='';mounted=null;return true;}
  function mount(root,options={}){if(!root)throw new Error('FIRST_TRIP_COMPOSER_ROOT_REQUIRED');if(!trip()?.commands?.createFirstTrip)throw new Error('TRIP_FIRST_CREATE_OWNER_UNAVAILABLE');unmount();root.classList.add('ftc-host');mounted=initialState(root,options);if(reduceMotion())root.classList.add('ftc-reduce-motion');render(mounted);return Object.freeze({version:VERSION,snapshot:()=>clone({mode:mounted?.mode,entryMode:mounted?.data?.entryMode,step:mounted?currentStep(mounted):null,highest:mounted?.highest,saving:mounted?.saving,error:mounted?.error,idempotencyKey:mounted?.idempotencyKey}),destroy:unmount});}
  function open(options={}){const navigation=window.LuviaNavigationContractV1,app=window.LuviaApp;if(!navigation?.createIntent||!app?.show)throw new Error('FIRST_TRIP_COMPOSER_NAVIGATION_UNAVAILABLE');const mode=options.mode==='first-use'?'first-use':'create',entryMode=ENTRY_FLOWS[options.entryMode]?options.entryMode:'guided',returnTo=options.returnTo||app.activeView?.()||'today';return app.show(ROUTE,{intent:navigation.createIntent(ROUTE,{source:'first-trip-composer-open',params:{tripStep:options.step||'welcome',tripMode:mode,tripEntry:entryMode,returnTo}}),source:'first-trip-composer-open'});}

  window.LuviaFirstTripComposer=Object.freeze({version:VERSION,route:ROUTE,mount,unmount,open,shouldStart,defer,clearDeferred,draftStep});
})();

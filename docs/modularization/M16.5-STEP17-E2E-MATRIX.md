# M16.5 Schritt 17 — sichtbare E2E-Abnahmematrix

Stand: 2026-09-01

Veröffentlichter Integration-Release: App `13.82.126` / Core `4.82.126` / Cache `luvia-shell-v13.82.126`

Scope: ausschließlich Integration; Main, Production, DB/RLS, Secrets und Edge Functions sind gesperrt.

## Belegregel

- Sichtbare Bedienung erfolgt über Browser-Pointer oder Tastatur auf echten
  gerenderten Controls. `element.click()`, direkte DOM-Zustandsmutation und
  simulierte Owner-Receipts sind als Abnahmebeleg unzulässig.
- Das Step-15-Fixture lädt den produktiven Dashboard-, Action-, Intent-,
  Places- und MapLibre-Pfad. Seine kontrollierten öffentlichen Owner-Ports und
  Datensätze sind ausdrücklich Fixture-Evidence, keine Provider-Evidence.
- Ein Pin existiert nur bei endlichen, gültigen WGS84-Koordinaten aus
  `places.v1`. Fehlt die Evidenz, bleibt die Liste sichtbar und die Karte nennt
  den Ausfall; Luvia erfindet weder Koordinaten noch Events.
- `PASS (contract)` ist kein sichtbarer E2E-Pass. Nicht verfügbare Hardware-,
  System- oder Provider-Capabilities bleiben ausdrücklich offen.

## Delta-Matrix für den 13.82.126-Release

Die vollständige `.124`-Matrix darunter bleibt historische Baseline. `.125`
wurde nach einem realen Mixed-Runtime-Gegenbeleg verworfen; `.126` ist der
Forward-Fix und der einzige hier abgenommene aktuelle Release.

| Dimension | Reale Interaktion / sichtbarer Beleg | Lokaler/technischer Beleg | Öffentliche Integration `.126` | Restgrenze |
|---|---|---|---|---|
| Desktop 1440×900 | produktive lokale Fixture per realem Pointer; helles Bottom Sheet, Eventkarte und Time Brush | PASS sichtbar | In-App-Fenster blieb trotz angefordertem Desktop-Viewport bei 394 px; **kein öffentlicher Desktop-Pass behauptet** | öffentliche/physische Desktop-Wiederholung |
| Mobile / Pointer | realer Pointer auf mittleren Compass-Punkt; ein einziges unten öffnendes Living Sheet; Preview/Confirm/Undo im 394-px-Fenster erreichbar | PASS Regression | PASS sichtbar | physisches Touchgerät offen |
| Keyboard / Intent | echte Eingabe plus `Enter`: Trip-Liste, Journey-Read, GPS/Places, Verified Events und Trip-Wechsel | PASS Evals | PASS sichtbar | vollständige Tab-/Shift+Tab-/Space-/IME-Matrix offen |
| Explainable Planning Trace | `Zeige meine Reisen.` zeigt neun echte Trip-Projektionen, eine Owner-Entscheidung, eine belegte Quelle und keine fehlende/veraltete Quelle | PASS | PASS sichtbar | Trace ist Erklärung, kein Owner Receipt |
| Route Uncertainty / Day Rehearsal | `Zeige den Tagesplan.` zeigt drei Journey-Momente, zwei Bänder `28–39 Min.`, vier Tagesproben, aktuellen Disruption-Status und den abgeleiteten Twin | PASS | PASS sichtbar | keine Live-Verkehrsevidenz; 0%-Evidenz bleibt sichtbar |
| On-Device Context Gate | echte GPS/Places-Nachricht zeigt `Standort bleibt gesperrt`, `no-explicit-grant`, `persist:false` und manuellen Ort | PASS Evals | PASS sichtbarer Deny-by-default-Pfad | echte Gerätefreigabe, Revoke und präzise/coarse Hardware offen |
| Trip Mutation / Receipt / Undo | realer Pointer auf `Ostseeurlaub`; R1-Preview; echte Bestätigung; `trip · completed`; separat vorbereitete/bestätigte Compensation; `trip · compensated`; Reload stellt `Jfjd` wieder her | PASS Evals | PASS sichtbar | belegt genau `trip.active.select`, nicht alle 21 Aktionen |
| Verified Event Timeline | kontrolliertes lokales Fixture zeigt zwei streng verifizierte Claims | PASS sichtbar lokal | echte Anfrage zeigt `VERIFIED_EVENT_PROVIDER_UNAVAILABLE` und keinerlei erfundenes Event | positive öffentliche Source-Gateway-Evidence offen |
| Event Map / no substitution | lokales Fixture zeigt genau einen Places-provenanzierten Pin und einen list-only Claim ohne Koordinate | PASS sichtbar lokal | keine Karte ohne öffentlichen verifizierten Claim – korrekter leerer Fehlerpfad | positive öffentliche Venue-/Quellen-Evidence offen |
| Dynamic Map-Time Brushing | realer lokaler Pointer auf `Abend` filtert Event Graph, Timeline und Map gemeinsam | PASS sichtbar lokal | mangels öffentlicher Claims nicht positiv ausführbar | öffentlicher Provider, physischer Touch offen |
| Event failure modes | Source-Ausfall, Stale/Conflict/Venue-unresolved, Drift, Weather/Group-Foundation und Reconciliation | PASS Evals | Provider-unavailable sichtbar PASS | kein externer Event-/Booking-Abschluss autorisiert |
| Runtime-Upgrade / Cache | derselbe zuvor gemischte Stable-Client wird ohne Cache-/Storage-Löschung neu geladen | PASS Bridge-Test | PASS: ausschließlich `.126` Loader, Precontext und Postcontext; kein `.124/.125` Current Runtime | historische Worker vor `.121` nicht behauptet |
| Reload / Warm | Undo-Zustand anschließend reloaden | PASS PWA-E2E | ein Parse/Stream-Versuch blieb einmal bei `readyState=loading`; unmittelbarer Retry und zwei weitere Reloads PASS, davon 4,7 s und 4,9 s; `Jfjd` erhalten | breitere Zeitreihe; einmaliger Ausreißer bleibt dokumentiert |
| Back | bestehende History-Verträge und ältere sichtbare `.124`-Evidence | PASS contract | auf `.126` nicht erneut sichtbar ausgeführt | öffentliche `.126`-Wiederholung offen |
| Reduced Motion | neue Sheet-/Pin-/Map-Bewegung wird per Media Query deaktiviert | PASS contract | kein sichtbarer OS-Reduced-Motion-Lauf | Systemlauf offen |
| Offline / Reconnect | realer Browser-Service-Worker-/Offline-Test plus Compiler-/Journey-Day-Pack-Evals | PASS technisch | kein realer öffentlicher Flugmodus-/Reconnect-Lauf | öffentlich sichtbar offen |
| Group Decision | Collaboration bleibt owner-reserviert und wird nicht von Intelligence simuliert | PASS Block/Evals | kein Mehrgeräte-Owner-Lauf | S16.07/Collaboration-Write nicht autorisiert |
| Provider | reale Google-Places-Tagesquota und fehlender Event-Gateway bleiben sichtbare Fehler; keine erfundenen Treffer | PASS Failure Evals | PASS Failure Mode | positiver Places-Treffer, Providerfoto und Booking-Lifecycle offen |
| Technische Gesamtregression | vollständiger kontrollierter Safe-Regression-Lauf inklusive Owner-/DB-Grenzen, Visual Inventory und NFR-0 | **138/138 PASS**, NFR-0 **3/3** | n/a | technische Evidence ersetzt keine Hardware-/Provider-Abnahme |
| Stable/Immutable Bytes | sauberes Git-Archiv gegen Immutable und Stable | Archiv 2.925 Dateien, SHA-256 `f448e4949bf7b7593fb0ae2de282a5bee7fa076bbf996a9fd3cb44b66b0e29d8` | **28/28 PASS**, 10.571.949 Bytes | keine Restgrenze für die geprüfte Dateimenge |

## Sichtbare öffentliche `.126`-Sequenzen

### A. Kohärentes Upgrade aus dem verworfenen `.125`-Client

Auf Stable war zuvor eine `.125`-Indexseite mit gecachtem `.124`-Loader und
`.124`-Pre-/Postcontext reproduziert. Ursache war die fehlende `.mjs`-Erfassung
im versionierten Service-Worker-Zweig. Nach `.126` wurde derselbe Stable-Tab
ohne Cache- oder Storage-Löschung geladen. Sichtbar wurde die angemeldete Today-
Oberfläche; die gelesenen Runtime-URLs waren ausschließlich:

- `luvia-runtime-loader.mjs?v=13.82.126-split8`;
- `luvia-runtime-precontext-13.82.126.bundle.js?v=13.82.126-split8`;
- `luvia-runtime-postcontext-13.82.126.bundle.js?v=13.82.126-split8`.

### B. Owner Reads und USP-Projektionen

`Zeige meine Reisen.` lieferte neun echte Trip-Owner-Projektionen und S16.01.
`Zeige den Tagesplan.` lieferte drei Journey-Momente sowie S16.03, S16.04,
S16.05 und S16.08. `Nutze meinen Standort, um ruhige Orte in meiner Nähe zu
finden.` blieb ohne Grant gesperrt und speicherte keine Koordinaten. Die reale
Places-Tagesquota wurde als Owner-Fehler mit `RATE_LIMIT_COOLDOWN` gezeigt.
`Welche verifizierten Events gibt es am 13.06.2027 in Scharbeutz?` endete
korrekt mit `VERIFIED_EVENT_PROVIDER_UNAVAILABLE` und ohne Eventkarte.

### C. Bestätigte Trip-Aktion und Compensation

Nach `Wechsle zur Reise Ostseeurlaub.` wurde der konkrete Trip mit realem
Pointer gewählt. Erst die sichtbare R1-Vorschau und `Verbindlich bestätigen`
führten zu `Owner Receipt · trip · completed`. `Undo vorbereiten` erzeugte eine
neue Vorschau; erst eine zweite ausdrückliche Bestätigung erzeugte
`Undo Receipt · trip · compensated`. Der nachfolgende Reload zeigte wieder
`Jfjd` als aktiven Trip.

### D. Immutable Release und Rollback

- Release-Head: `4b5fce56177ad9393120f0e6fb11e1183ee83e3c`;
- Version: `df05650e-7776-4282-ac27-78efea65792c`;
- Deployment: `6a72556d-431e-4654-8599-0de9cb240de7`;
- Stable: `https://integration-luvia.njwnrvwbv5.workers.dev/`;
- Immutable: `https://df05650e-integration-luvia.njwnrvwbv5.workers.dev/`;
- exakter Rollback: `npx wrangler versions deploy 7e1068dd-dc21-4a82-8973-7d13c7105c80@100 --name integration-luvia --message "Rollback M16.5 App 13.82.126 to App 13.82.124" --yes`.

## Historische Baseline-Matrix für 13.82.124

| Dimension | Reale Interaktion / sichtbarer Beleg | Lokaler Kandidat | Öffentliche Integration | Restgrenze |
|---|---|---|---|---|
| Desktop 1440×900 / Bottom Sheet | realer Pointer auf `Luvia Compass öffnen`; genau ein helles, unten verankertes Living Sheet | PASS | ausstehend für `.124`; `.121` belegt Singleton auf Stable | öffentliche Wiederholung nach Deployment |
| Mobile 390×844 / Reachability | 390×844-Vertrags- und bisherige Pointer-Evidence; kompakte Bottom-Sheet-Geometrie ohne horizontales Dokument-Overflow | PASS Regression; `.124` nicht erneut auf physischem Handset | ausstehend | physisches Handset/Touch offen |
| Touch | keine physische Touch-Hardware in dieser Abnahmeumgebung | nicht behauptet | ausstehend | physischer Touch bleibt offen |
| Pointer / gestapelte Sheets | realer Pointer auf Map-Pin 2; `Kompass Bistro` öffnet als zweites Bottom Sheet über dem Chat; ein Schließen kehrt mit erhaltenem Chat-Zustand zurück | PASS | ausstehend | keine lokale Restgrenze |
| Keyboard / Sprache | echte Texteingabe und `Enter`: `I would like a good dinner and then minigolf with the children.` | PASS | ausstehend | physische IME-/Gerätetastatur offen |
| Multilingualer Intent Compiler | OpenAI-Structured-Understanding plus deterministische Rückprüfung erzeugt zwei geordnete `places.v1`-Ziele; DE/EN/FR/ES und weitere Locale-Fälle sind automatisiert gesichert | PASS sichtbar für EN und DE; Evals PASS | ausstehend | nicht jede Sprache kann einzeln sichtbar abgenommen werden |
| Sequentielle Mehrfachwünsche | Ziel 1 zeigt drei Restauranttreffer; realer Pointer auf `Ohne Übernahme mit Wunsch 2 weiter`; Ziel 2 zeigt drei Aktivitätstreffer | PASS | ausstehend | keine lokale Restgrenze |
| MapLibre / Compass-Palette | je Ziel genau eine gemeinsame MapLibre-Karte; Pins und Basiskarte nutzen Koralle `#ef6254`, Sonne `#f4b34c`, Meer `#2c93a9`, Hain `#2f8c73`; Status nennt `3 von 3 Treffern mit Owner-Koordinaten` | PASS sichtbar und Regression | ausstehend | Fixture-Koordinaten sind keine Provider-Evidence |
| Pin-Hover / Reduced Motion | MapLibre positioniert nur den äußeren Marker-Anker; der innere Pin schwebt höchstens 3 px vertikal, pausiert bei Hover/Fokus und bewegt sich nie seitlich; Reduced Motion schaltet die Bewegung ab | PASS sichtbar per realem Pointer und Regression | ausstehend | sichtbarer System-Reduced-Motion-Lauf offen |
| Räumliche Detailabsicht | echte Eingabe `Restaurant in Scharbeutz, eher im Zentrum statt am Strand`; strukturierte Präferenz `center`, Ausschluss `waterfront`, nur belegte Zentrumstreffer und Lagebadge | PASS sichtbar; DE/EN/ES/FR/IT/PT/NL Evals PASS | ausstehend | echte Providerdaten dürfen bei fehlender Lageevidenz nur `unbekannt`, nie erfunden `belegt` zeigen |
| Sitzungsvielfalt / Bilder | identische Anfrage zweimal im selben offenen Chat; zweite Antwort schließt die drei bereits gezeigten Provider-IDs aus und zeigt eine andere Zentrumgruppe. Bildflächen tragen Providerfoto nur mit Places-Evidence, Fixture-Bilder sind ausdrücklich als kontrolliert/nicht Provider markiert | PASS sichtbar und Regression | ausstehend | Google-Tagesquota und echte Foursquare-Fotoprojektion bleiben Providergrenze |
| Profil-Fallback | ohne explizite Vorlieben nennt die Antwort zwei bestätigte Identity-Profilbereiche; explizite Satzpräferenzen überschreiben nur die genannten Felder | PASS sichtbar / Evals | ausstehend | echter Mehrgeräte-Profilkontext offen |
| Positive Mutation | echter Pointer auf `2027-06-13 · 19:00 einplanen`; sichtbare R2-Vorschau; realer Pointer auf `Verbindlich bestätigen`; öffentlicher Places-Owner-Command | PASS | ausstehend | externer Provider nicht beteiligt |
| Receipt / Fortsetzung | sichtbares `Owner Receipt`, `places · completed`; danach automatische Fortsetzung zu Ziel 2 mit drei Minigolf-Treffern und MapLibre | PASS | ausstehend | keine lokale Restgrenze |
| Undo | realer Pointer auf `Undo vorbereiten`; zweite R2-Vorschau; erneute ausdrückliche Bestätigung; sichtbares `Undo Receipt`, `places · compensated` | PASS | ausstehend | keine lokale Restgrenze |
| Verbotener Befehl | Bestätigungsumgehung wird multilingual/deterministisch abgewiesen; keine Owner-Mutation | PASS Evals | `.121` sichtbar auf Stable; `.124` ausstehend | öffentliche `.124`-Wiederholung |
| Konflikte / fehlende Angaben | GPS-Widerspruch, fehlende Booking-/Privacy-/Location-Angaben und nicht produktiver Collaboration-Write werden sichtbar blockiert | PASS Fixture/Evals | `.121` sichtbar; `.124` ausstehend | keine Mutation als korrekter Ausgang |
| Reload / Warmstart | Browser-Neuaufruf rendert Fixture und Singleton neu; Runtime-/Cache-Regression PASS | PASS | ausstehend | kein Chat-Verlaufs-Restore behauptet |
| Back / Forward | bestehende Shell-/Sheet-History-Verträge und geschlossene Recovery-Slices PASS | PASS contract/ältere sichtbare Evidence | für `.124` ausstehend | Step-15-Chat öffentlich wiederholen |
| Reduced Motion | produktive Media Query entfernt neue Sheet-, Karten-, Pin- und Rich-Result-Bewegung ohne neues `!important`-Debt | PASS contract | nicht sichtbar hochgestuft | sichtbarer Systemlauf offen |
| Cold / Warm | Split-Bundle, Cache, First-Paint-/Warmstart-Verträge und vollständige Safe Regression | PASS contract | alter Stable-Worker-Boot und Reload PASS | breite öffentliche Zeitreihe offen |
| Upgrade von altem Service Worker | echter Stable-Client mit kontrollierendem `.121`-Worker: `.123` blieb wegen entfernter `.121`-Bundles im Splash; `.124` hält `.121`/`.122`/`.123` als nicht vorgecachte Netzwerkbrücke und lädt selbst ausschließlich `.124` | `.123` sichtbar FAIL; `.124` Bridge-Test PASS | `.124` echter Boot plus `Ctrl+R` PASS, keine Cachelöschung, keine neuen Fehler | keine Restgrenze für den reproduzierten Fehler; weitere historische Worker vor `.121` nicht behauptet |
| Offline / Reconnect | Offline-Compiler blockiert externe Writes; Journey-Day-Pack bleibt explizit/reloadfähig; keine erfundenen Live-Tiles/Fakten | PASS Fixture/Evals | nicht sichtbar hochgestuft | echter Flugmodus/Reconnect offen |
| GPS / Privacy | Pending/Bestätigung/Ablehnung aus geschlossenem Step 12; keine Rohposition im Journey-/Action-Ledger | PASS ältere sichtbare Evidence/Evals | kein `.124`-Gerätebeleg | präzises Geräte-GPS und Cloud offen |
| Group Decision | Collaboration Owner bleibt getrennt; nicht produktiver Mutationspfad blockiert statt direkt zu planen | PASS ältere sichtbare Evidence/Evals | kein `.124`-Mehrgerätebeleg | echte Cloud-Synchronisation offen |
| Provider | reale Providerfehler bleiben Fehler; keine erfundenen Treffer, Buchungen oder Events | PASS Evals; lokale positive Daten klar als Fixture markiert | Google Places Tagesquota derzeit echte Degradation | kein externer Buchungsabschluss oder E-Mail-Versand ausgelöst; Eventabschluss offen |
| Technische Gesamtregression | kompletter Safe-Regression-Lauf einschließlich NFR-0, visueller Inventur, Core-/DB-Grenzen und Step-15/16-Evals | **135/135 PASS** | n/a | technische Evidence ist keine Hardware-/Provider-Abnahme |
| Stable/Immutable Bytes | SHA-256 zwischen sauberem Git-Archiv, Stable und neuer Immutable URL | Commit-Archiv vorhanden | **20/20 PASS** einschließlich sechs Upgrade-Bundles | keine Restgrenze für die geprüfte Dateimenge |

## Sichtbare `.124`-Sequenzen

### A. Mehrsprachige Read-/Detail-Sequenz

Der englische Satz

> I would like a good dinner and then minigolf with the children.

wurde mit echter Tastatureingabe und `Enter` gesendet. Sichtbar entstanden zwei
geordnete `places.v1`-Ziele. Das erste Ziel zeigte drei Restaurants und eine
gemeinsame MapLibre-Karte. Ein echter Pointerklick auf den gelben Pin 2 öffnete
`Kompass Bistro` als gestapeltes Places-Sheet. Nach realem Schließen blieb der
Chat erhalten. `Ohne Übernahme mit Wunsch 2 weiter` zeigte anschließend drei
Aktivitäten (`Ostsee Minigolf`, `Küsten Adventure Golf`, `Familienpark am
Meer`) und eine zweite owner-koordinatengebundene MapLibre-Projektion.

### B. Positive Owner-Mutation und Undo

Der deutsche Satz

> Plane am 13.06.2027 um 19 Uhr ein Restaurant in die Timeline und danach
> Minigolf mit den Kindern.

zeigte drei Places-Vorschläge mit der Aktion `2027-06-13 · 19:00 einplanen`.
Der reale Pointerpfad war:

1. Owner-Aktion auswählen.
2. R2-Vorschau lesen.
3. `Verbindlich bestätigen` auswählen.
4. `Owner Receipt · places · completed` prüfen.
5. Automatisch fortgesetztes Minigolf-Ziel prüfen.
6. `Undo vorbereiten` auswählen.
7. Die neue R2-Undo-Vorschau erneut ausdrücklich bestätigen.
8. `Undo Receipt · places · compensated` prüfen.

Damit ist lokale Handlungsfähigkeit belegt, nicht nur eine Beispielantwort.
Die Domain-Mutation erfolgte über den öffentlichen Places Owner und nicht über
einen zweiten Intelligence-, Places- oder Timeline-Owner.

Beim blockierten Konflikt-/Bestätigungsumgehungspfad gilt dagegen unverändert:
ausgeführte Mutation: **keine**.

### C. Räumliche Präzision, Vielfalt und stabiler Marker

Die deutsche Detailanfrage

> Ich suche ein Restaurant in Scharbeutz, eher im Zentrum statt am Strand.

wurde mit echter Tastatureingabe und `Enter` gesendet. Der deterministische
Teil des Compilers erhielt `center` als positive und `waterfront` als negative
räumliche Bedingung. Sichtbar erschienen ausschließlich die kontrolliert
belegten Zentrumkandidaten `Marktküche`, `Gartenstube` und `Ortskern Café`, je
mit gemeinsamem MapLibre-Pin und `Gewünschte Lage belegt`. Eine zweite identische
Anfrage im selben offenen Chat schloss diese drei IDs aus und zeigte
`Hofküche Mitte`, `Lindencafé` und `Bahnhofstafel`; die Oberfläche erklärte den
Sitzungsausschluss sichtbar. Ein realer Pointer-Hover auf dem ersten Pin
veränderte seine horizontale Position nicht. Die eingeblendeten Fixture-Bilder
tragen ausdrücklich `Kontrollierte E2E-Bildreferenz · kein Provider-Foto`; im
Produktpfad darf dagegen ausschließlich die vom Places Owner aufgelöste erste
Google-Places-Fotoreferenz als `Google Maps` erscheinen. Fehlt diese Evidence,
steht sichtbar `Kein Provider-Foto belegt`.

### D. Öffentliche Runtime-Erholung von `.121` auf `.124`

Der erste `.123`-Stable-Lauf blieb mit einem real kontrollierenden `.121`-
Service-Worker im Splash. Die Browserkonsole belegte für die entfernte Datei
`app/luvia-runtime-precontext-13.82.121.bundle.js` zuerst HTML statt JavaScript
(`Unexpected token '<'`) und danach den fehlenden `LuviaTripStateReaderV1`.
Dieser Lauf ist **FAIL** und `.123` deshalb kein akzeptierter Release.

Nach dem `.124`-Forward-Deploy wurde derselbe Stable-Origin im selben Browser
ohne Cache- oder Storage-Löschung geöffnet. Die Anwendung erreichte sichtbar
das echte Profil-Onboarding. Ein echter Tastatur-Reload mit `Ctrl+R` erreichte
erneut dieselbe vollständige Oberfläche; seit dem `.124`-Deploy trat kein neuer
Konsolenfehler auf. Das aktuelle Bundle wurde nicht durch ein altes ersetzt:
der `.124`-Loader referenziert ausschließlich `.124`; die exakten `.121`,
`.122` und `.123` Split-Bundles dienen nur als netzwerkadressierbare Brücke für
den noch kontrollierenden alten Worker und werden nicht vorgecacht.

Das saubere Archiv des Runtime-Commits
`787e084a77f3752a96ff7ad9312bbf470660c0c4`, Stable und Immutable stimmen für
20/20 geprüfte Dateien SHA-256-genau überein. Enthalten sind Index, Kernel,
Loader, Compatibility-Manifest, alle acht aktuellen/retained Split-Bundles,
AI-Dashboard/Action-Runtime, Places-Discovery/Spatial-Contracts, Map-JS/CSS,
Experience-CSS und Service Worker.

## Veröffentlichungsgrenze

`.126` ist ausschließlich auf Integration als Stable und Immutable
veröffentlicht. Safe Regression 138/138, NFR-0 3/3, der kohärente alte-Client-
Upgradepfad, die beschriebenen öffentlichen Owner-/Mutation-Sequenzen und die
28/28-Byte-Provenienz sind grün. Das reicht bewusst nicht für eine pauschale
Abnahme der gesamten Step-17-Hardware-/Provider-Matrix: öffentlicher Desktop,
physischer Touch, echter öffentlicher Flugmodus/Reconnect, präzises Geräte-GPS,
sichtbarer System-Reduced-Motion-Lauf, Mehrgeräte-Cloud, positive Places-/Event-
Providerdaten und externe Booking-Abschlüsse bleiben ausdrücklich offen.

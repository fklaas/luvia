# M16.5 Schritt 17 — sichtbare E2E-Abnahmematrix

Stand: 2026-08-31
Release-Kandidat: App `13.82.121` / Core `4.82.121` / Cache `luvia-shell-v13.82.121`
Scope: ausschließlich Integration; Main, Production, DB/RLS, Secrets und Edge Functions sind gesperrt.

## Belegregel

- Sichtbare Bedienung erfolgt über Browser-Pointer oder Tastatur auf echten
  gerenderten Controls. `element.click()`, direkte DOM-Zustandsmutation und
  simulierte Owner-Receipts sind als Abnahmebeleg unzulässig.
- Ein Browser-Fixture darf nur den produktiven Runtime-Pfad mit kontrollierten
  öffentlichen Owner-Ports versorgen. Es ersetzt keine Provider-, Geräte- oder
  Cloud-Evidenz.
- Präzise Geräteposition, reale Buchung, E-Mail-Versand, echte Einladung und
  Passwort-/Kontomutation werden nicht still ausgelöst.
- `PASS (contract)` ist kein sichtbarer E2E-Pass. Nicht verfügbare Hardware-
  oder Browser-Capabilities bleiben ausdrücklich offen.

## Matrix

| Dimension | Reale Interaktion / Beleg | Ergebnis vor Veröffentlichung | Immutable Integration | Restgrenze |
|---|---|---|---|---|
| Desktop 1440×900 | frischer Viewport; realer Pointer auf `Luvia Compass öffnen`; helles, unten verankertes Living Sheet mit Timeline-/Places-Motion; Singleton-Dialog | PASS lokal nach Owner-Korrektur und Double-Overlay-Fix | PASS Stable bei 1440×720: ein Pointerklick → exakt ein benannter Dialog; ein Schließen → kein Dialog/Backdrop | 1440×900 zusätzlich lokal belegt |
| Mobile 390×844 | frischer Mobile-Viewport; realer Pointer; kompakter, unten verankerter Sheet | PASS lokal nach Owner-Korrektur | PASS Stable 390×844: ein Pointerklick → exakt ein Dialog; ein Schließen → kein Dialog/Backdrop | physisches Handset separat |
| Touch | keine Browser-`tap`-Capability in der gewählten Abnahmeumgebung | nicht behauptet | offen, nicht hochgestuft | physischer Touch bleibt offen |
| Keyboard | Fokus im Composer; echter Textinput; `Enter` sendet, `Shift+Enter` ist getrennt vertraglich gesichert | PASS lokal | PASS Stable für echte Texteingabe + `Enter`; `Shift+Enter` nur Regression | physische IME-/Gerätetastatur offen |
| Komplexer deutscher Mehrfach-Intent | Restaurant + Booking + vier Personen + widersprüchliche GPS-Policy + Einladung + Memory in einem Satz | PASS lokal: 7 Routen sichtbar, Konflikt/fehlende Inputs/Foundation blockiert, keine Mutation | PASS Stable: sieben Owner-Routen, `Befehl abgelehnt`, keine Mutation | keine |
| Verbotener Befehl | Bestätigungsumgehung wird vom Compiler abgewiesen | PASS in Evals | nicht öffentlich wiederholt | sichtbare öffentliche Wiederholung offen |
| Reload / Warmstart | echter Browser-Neuaufruf; Owner-/Runtime-Zustand erneut gerendert | PASS für lokale Shell-/Owner-Fixtures | Stable authentifiziert erneut gerendert; kein isolierter Chat-Zustands-Restore behauptet | vollständiger öffentlicher Chat-Restore offen |
| Back / Forward | echte Browser-History auf Shell- und Sheet-Pfaden | PASS in bisherigen geschlossenen Recovery-Slices | nicht für Step 15 öffentlich wiederholt | Step-15-Chat öffentlich offen |
| Reduced Motion | produktive `prefers-reduced-motion: reduce`-Regel entfernt Animation/Transition/Transform; Regression PASS | PASS (contract) | nicht sichtbar hochgestuft | Browser bietet keine Media-Emulation; sichtbarer Systemlauf offen |
| Cold / Warm | Runtime-Split, lokaler Server, First-Paint- und Background-Refresh-Evals | PASS lokal | Immutable öffentlicher Cold Start und Stable authentifizierter Start sichtbar; keine neue Zeitmessung | öffentliche Cold-/Warm-Zeitreihe offen |
| Offline / Reconnect | Compiler blockiert offline Writes; Journey-Day-Pack ist explizit/reloadfähig; keine erfundenen Live-Fakten oder Tiles | PASS in Owner-Fixtures und Evals | nicht sichtbar hochgestuft | echter Flugmodus/Reconnect offen |
| GPS / Privacy | Pending → ausdrückliche Bestätigung oder Ablehnung; keine Rohposition im Journey-/Action-Ledger | PASS sichtbar im geschlossenen Step-12-Fixture plus Evals | Konflikt/fehlender Grant öffentlich im Mehrfach-Intent blockiert; kein Geräte-GPS | präzise Browserposition und Cloud offen |
| Group Decision | Mehrpersonenpfad verlangt Collaboration Owner; fehlender Owner blockiert; Mehrheit/Frist separat sichtbar belegt | PASS sichtbar im geschlossenen Step-11-Fixture plus Evals | fehlender produktiver Collaboration-Mutationspfad öffentlich blockiert | echte geräteübergreifende Cloud-Synchronisation offen |
| Provider | nur verifizierte Places-/Booking-Evidence; kein source-loser Event/Pin, keine erfundene Buchung | PASS in Provider-/Evidence-Evals | fehlende Booking-Capability öffentlich blockiert; Places-Quota als echte Degradation sichtbar | kein externer Buchungsabschluss oder E-Mail-Versand ausgelöst |
| Mutation / Receipt / Recovery / Undo | Write wird nur Preview → explizite Bestätigung → öffentlicher Owner Command → Receipt; Undo ist neue bestätigte Kompensation | PASS Runtime/Evals | öffentliche Konfliktsequenz führte korrekt keine Mutation aus | konkrete positive externe Mutation/Undo owner-/providerabhängig offen |
| Desktop/Mobile Overflow und Reachability | Sheet, Log und Composer sichtbar; 44-px-Control-Grenze; horizontale Überläufe gesperrt | PASS lokal/Regression | PASS Stable Desktop und 390×844 Mobile sichtbar | physische Geräteabnahme separat |
| Stable/Immutable Bytes | SHA-256 von Version, Loader, Browser-Supabase, Pre-/Post-Bundle, CSS, AI/Orchestration und SW | ausstehend | PASS 10/10 exakt zwischen sauberem Git-Archiv, Stable und Immutable | keine für die zehn gemessenen Assets |

## Aktueller sichtbarer Step-15-Beleg und verworfener Gegenbeleg

Der lokale produktive AI-Pfad wurde ohne zweiten Intelligence Owner verwendet:

`App Shell → LuviaAIDashboard → LuviaAIActionRuntime → öffentliche Owner-Contracts`.

Der gesendete Satz lautete:

> Finde ein ruhiges Restaurant am Wasser, buche es heute um 19 Uhr für uns vier
> und teile meinen GPS-Standort nicht, aber aktiviere GPS; lade Mia in die Reise
> ein und speichere den Moment.

Die sichtbare Antwort zeigte sieben Owner-Routen (`places.v1`, `booking.v1`,
`identity.v1`, `LocationPort`, `journey.v1`,
`collaboration.membership.v1`, `memory.v1`), den GPS-Widerspruch, die fehlende
verifizierte Booking-Capability, fehlende Privacy-/Location-Angaben und die noch
nicht produktive Collaboration-Mutation. Status: `Befehl abgelehnt`; ausgeführte
Mutation: **keine**.

Der dabei zunächst sichtbare Desktop-Rechtsrail ist durch die ausdrückliche
Owner-Korrektur vom 2026-08-31 **verworfen**. Er ist kein Abnahmebeleg. Die
releasefähige Projektion fährt wie die Places-Vorschlagskarten in der Timeline
von unten ein. Frische reale Pointerläufe belegen dies lokal bei 1440×900 und
390×844 sowie öffentlich auf Stable bei 1440×720 und 390×844.

Ein anschließend vom Nutzer gemeldetes doppeltes Overlay wurde mit demselben
echten Pointerpfad reproduziert. Ursache war ausschließlich ein zusätzlicher
Click-Bridge der Browser-Fixture neben dem bereits produktiv registrierten
Dashboard-Handler. Der Bridge wurde entfernt; der bestehende
`LuviaAIDashboard`-Mount ist zusätzlich idempotent. Im frischen 1440×900-Lauf
enthält der semantische Baum nach einem Klick genau einen Dialog
`Mit Luvia sprechen`; ein einziger realer Klick auf Schließen entfernt Dialog
und Backdrop vollständig. Kein zweiter Intelligence- oder Overlay-Owner wurde
eingeführt.

## Schritt-17-Abschlussgrenze

Die veröffentlichbare Matrix ist am 2026-08-31 ehrlich ausgeführt: der
immutable Integration-Build startete öffentlich, Stable belegt den
authentifizierten Desktop-/Mobile-Singleton und den komplexen Keyboard-Intent,
und die zehn releasekritischen Dateien sind zwischen sauberem Git-Archiv,
Stable und Immutable bytegleich. Dies schließt die Release-Evidenz, aber nicht
die ausdrücklich nicht verfügbaren Abnahmen. Physischer Touch, echter
Offline-/Reconnect-Lauf, präzises Geräte-GPS, Mehrgeräte-Cloud, sichtbarer
System-Reduced-Motion-Lauf und externe Providerabschlüsse bleiben offen und
werden nicht aus Fixture-/Contract-Belegen hochgestuft.

# M3.2 Exit Gate — Places Contract Adapter Foundation

## Gate result
**PASS for local architecture/release gates.** Production deployment and browser smoke are the final operational checks before M3.2 is considered fully released.

## Required conditions
- [x] `places.v1` has an additive runtime implementation.
- [x] Runtime globals `LuviaPlacesContractV1` and `LuviaPlacesContract` exist.
- [x] Public reads are projected and do not expose raw provider/backend payloads.
- [x] Public commands delegate to existing mutation owners.
- [x] Command results are normalized instead of returning raw backend/RPC responses.
- [x] Existing Places state/persistence remains authoritative; no second store/repository/persistence path was created.
- [x] Contract events use the existing DOM CustomEvent transport and retain compatibility events.
- [x] Adapter load order is after existing Places providers and before AppShell.
- [x] Adapter is included in the service-worker shell.
- [x] Existing `LuviaPlaceCore.updateLifecycle` callers have a local compatibility method.
- [x] `places.v1.json` reports `implemented-m3.2`.
- [x] M3.2 adapter regression test passes.
- [x] M3.2 release integration test passes.
- [x] Global release version consistency test passes.
- [x] No DB migration, Edge Function or secret change is required.

## Deferred intentionally
- Places free-text/AI search performance optimization.
- Search result diversity/repetition and "more/refresh" product behavior.
- Places consumer migration to the new contract.
- Places folder/layer physical reorganization.
- Removal of compatibility APIs or direct legacy consumers.
- Provider architecture redesign.
- Social integration.
- Feature-gate rollout for Places.

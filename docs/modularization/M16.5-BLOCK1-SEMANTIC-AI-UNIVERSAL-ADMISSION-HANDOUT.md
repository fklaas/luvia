# M16.5 Block 1 handout — Semantic AI and universal admission booking

Date: 2026-09-02

Status: **SEMANTIC READ/OPEN + FAIL-CLOSED HOTEL FOUNDATION PUBLIC ON 13.82.136 / CHAT-NATIVE TRIP SELECTION PUBLIC ON 13.82.138 / P04–P05 PLACES MUTATIONS PUBLIC ON 13.82.139 / BOOKING LIFECYCLE + PLACE-ROUTE INTEGRITY PUBLIC ON 13.82.143 / WHOLE-SENTENCE + PLACES/HOTEL RECOVERY LIVE ON 13.82.147 WITH PROVIDER GATE OPEN / B1 CONTINUES**

## User outcome

Luvia interprets a whole request before choosing an owner. A sentence about
switching trips cannot become a Places search merely because it contains a
generic verb. When meaning is not reliable enough, Luvia asks one short
question. It does not guess.

For restaurants, activities, culture, sights, attractions and events, the same
Booking Owner provides a compact statement such as `Reservierung möglich`,
`Ticket erforderlich`, `Tickets verfügbar` or `Eintritt noch ungeklärt` and the
next safe action. The notice is consumed by AI Chat, Places and Journey/Timeline
suggestions; consumers do not invent booking policy.

## Owner and provider architecture

`booking.admission.v1` is a browserless component of the existing Booking Core.
It is exposed only through additive reads on `booking.v1`.

Provider layers:

| Layer | Providers / route | Current Integration state | What remains |
|---|---|---|---|
| Direct official | official ticket/reservation URL | immediately usable when present | public E2E with a real Place |
| Verified public contact | public booking email plus source URL and verified/public flags | immediately usable after owner evidence | public preview/send/receipt test; never infer raw email |
| Transaction API | Tiqets, Viator | capability + registry + partner-gated adapters | commercial/API access, secret authorization, Edge transport, live health and provider E2E |
| Activity handoff/partner discovery | GetYourGuide, FareHarbor, Bókun, Regiondo, bookingkit, Xola, Checkfront | capability + domain detection | provider-by-provider contract and activation decision |
| Event ticket handoff | EVENTIM, Reservix, Ticketmaster | capability + domain detection | verified event product and public handoff E2E |
| Dining compatibility | TheFork, OpenTable, Zenchef, Quandoo, Resy, SevenRooms, Tock and existing fallbacks | retained | existing provider-specific activation/evidence continues |

The provider claims above follow the official product boundaries, not feature
name similarity:

- Tiqets Distributor API documents catalogue, live availability/pricing,
  creation, confirmation, cancellation, order/ticket retrieval and webhooks;
  test and production access require a partner agreement and separate keys:
  <https://developers.tiqets.dev/>.
- Viator distinguishes affiliate handoff from merchant/commerce access. The
  local adapter therefore stays `partner_required` and does not borrow
  supplier-side amendment operations for the partner API:
  <https://docs.viator.com/partner-api/>.
- Ticketmaster Discovery exposes event discovery and purchase URLs. Direct
  reserve/purchase/order capability belongs to separate partner access and is
  not claimed by the Discovery adapter:
  <https://developer.ticketmaster.com/products-and-docs/apis/discovery-api/v2/>.

`partner_required` and `discovery` are deliberately not `connected`. An adapter
rejects before network access while partner activation is absent.

## Truth rules

- Category makes admission relevant but never proves that payment, a ticket or
  a reservation is required.
- `reservable=true` proves support only.
- A URL proves that a handoff exists only.
- A booking/ticket action is offered only when the selected Place kind supports
  that action. Lodging is not ticket admission; nightlife/activity is not
  silently canonicalized to restaurant.
- A syntactically safe URL or public email is not sufficient. The resolved
  provider venue/property must match the exact selected Place by provider ID,
  normalized name/address or a provider-specific property/product identifier.
  Generic corporate pages, sibling properties and weak name overlap fail closed.
- Required/recommended/free/timed-entry labels require explicit provider or
  official-place evidence.
- Email fallback requires a valid address, public+verified flags and a source
  URL. A raw Place email is excluded.
- Opening a route is not a purchase, reservation or message.

## Current implementation bundle

- Places provider order is now an executed gateway rule, not a merge preference:
  Google is attempted first; Foursquare is called only when Google is absent,
  fails or returns no destination-eligible result. Diagnostics expose the
  attempted providers and fallback reason, and total provider failure is an
  error rather than a false zero-result search.
- Explicit sentence destinations are independent of the active Trip destination.
  `Tonfink in Lübeck` searches Lübeck and must resolve Tonfink exactly before the
  Booking Owner may describe its reservation requirement; a Scharbeutz result
  or a different venue cannot substitute.
- semantic-first AI compiler precedence and low-confidence clarification;
- browserless universal admission decision;
- additive `booking.v1.reads.resolveAdmission` and provider catalog;
- generic `booking.place.open` AI action plus restaurant compatibility alias;
- Tiqets/Viator partner adapter counterparts and activity/event engine family;
- browserless `booking.lifecycle-policy.v1` for API, verified email thread,
  verifiable contact resolution, external handoff and terminal bookings;
- every external ticket/reservation handoff creates or reuses a Booking-owned
  record before navigation, stores the exact HTTPS/provider route, appears in
  the Booking Center and remains visibly unconfirmed until evidence returns;
- every external URL and verified-email handoff goes through the Booking route
  resolver's exact Place-identity check; cache identity includes the Place and
  route facts and is invalidated when either changes;
- free-form provider chat is exposed only after a real verified thread exists;
  modify/cancel appear only for an explicitly supported connected API or the
  verified email mutation path;
- compact consumer notices/actions in AI Chat cards/details, Places and Journey suggestions;
- focused semantic, admission, provider-gate and consumer tests;
- PCR `docs/architecture/PCR-M16.5-B1-SEMANTIC-AI-UNIVERSAL-ADMISSION-BOOKING.md`.

Current operated provider constraint after `.147` deployment: Supabase Edge
`luvia-gateway` ACTIVE version 124 has both credentials configured and executes
Google first. Google currently rejects the exact Hotel probe because the daily
`SearchTextRequest` quota is exhausted; Foursquare is then attempted and rejects
it because the account has zero API credits. The code prevents Foursquare quota
consumption beside a healthy Google response and exposes only bounded provider
diagnostics. At least one Place source must be reactivated before real Hotel,
Restaurant or Activity cards can complete public positive-path acceptance.

## Remaining work after the public foundation slice

The `.136` foundation chain, `.138` Chat-native Trip-selection repair and `.139`
Places mutation slice are complete. `.139` passed 191/191, visible local and
signed-in public 390 px operation, commit/archive/immutable deployment, 26/26
byte identity and exact app-only rollback to accepted `.138`. Favorite,
Unfavorite, Plan and Unplan now have seven total public-E2E registry rows when
combined with the three earlier public passes. The remaining B1 work is:

App `.140` is explicitly rejected evidence: public Read/Open passed, but a
fresh-chat Create sentence showed the Booking list instead of a confirmation
because the target resolver required a preceding Place card. No external write
occurred; Stable was restored to accepted `.139`. `.141` then derived the exact
subject from the public Booking Owner collection and publicly passed fresh-chat
Create, but its Modify confirmation omitted the requested new date and time.
Every preview was cancelled and Stable again returned to `.139`. `.142` then
projected exact changes, but public Modify twice supplied date/time as typed
constraints rather than `timeWindow`; the compiler incorrectly asked for
`booking change`. Stable returned to `.139` without a mutation. `.143`
normalizes both structured forms, and the exact public model shape passes the
real local lifecycle fixture at 5/5. Public operation, byte proof and exact
`.143 → .139` rollback are complete: Safe Regression is 197/197, runtime commit
is `11ff01ae`, Worker version `a8db9b4a-ace5-4144-b86c-db61d9586ccf` receives
100% Stable Integration traffic, and 24/24 changed public assets match the clean
release archive, Stable and Immutable.

Public `.139` subsequently supplied additional Place/Booking counterevidence:
Hotels appeared with `Eintritt` and `Tickets prüfen`; a reservation action on a
nightlife Place opened an unrelated A-ROSA/Straubinger Hotel property; and weak
Hotel-name overlap could be treated as an identity match. The accepted `.143`
correction adds the Place-Kind gate and exact venue/property identity proof for
all URL and email targets. Its dedicated visible fixture is **4/4 green**, and
focused tests cover those concrete negative cases plus valid exact
OpenTable/TheFork/ticket/Hotel routes. `booking-route-resolve` 2.7 is ACTIVE as
shared Supabase Edge version 13 with JWT verification retained; unauthorized
POST and untrusted-Origin preflight fail closed. Public Hotel search also proves
the honest provider-unavailable state. The separate Tonfink compound sentence
still fails closed with internal missing-field wording and is the immediate AI
counterexample for the next slice.

1. Prove real Restaurant and Activity/Culture provider routes with exact venue
   identity or an honest unavailable state, and repair the compound Tonfink
   find/check sentence without internal field names.
2. Add canonical Hotel property identity across providers and an exact selected-
   offer handoff carrying provider, rate, quote and expiry evidence.
3. Build the bright Demo-design Hotel UI over the existing Booking Owner; no
   duplicate Hotel core and no live activation against the legacy surface.
4. Activate Hotel/Affiliate providers in the binding order and never treat an
   affiliate handoff as live-price evidence.
5. Close P09/P10, then all remaining Step-17 rows, then Blocks 2–5 in order.
   Tiqets/Viator stay disabled until commercial access, authorized secrets,
   provider-specific Edge transports and live reconciliation exist.

Visible local evidence: the fixture reports `pass`, routes the trip-switch
sentence exclusively to Trip, renders four distinct admission states and eleven
provider-route tiers, has no horizontal overflow in the narrow run and emits no
browser warning/error.

## Position in the six-block program

- B0: public control-plane exit complete; product parity remains a cross-cutting gate.
- B1: this semantic/admission bundle is active; P04/P05 Places mutations are
  public, while the Booking lifecycle and real provider writes remain open.
- B2: route uncertainty, rehearsal, disruption, Identity, causal feedback, Trip
  and Memory closure follow.
- B3: verified Event Core and the complete Veranstaltungskalender follow after
  event-source authorization.
- B4: full orchestration, mutation matrix, hardware/provider Step 17 and S16.09–S16.12 acceptance follow.
- B5: the ten frontier capabilities remain queued behind their owners, privacy,
  data and infrastructure gates.

## Additive hotel decision lane — 2026-09-02

Hotels are now specified as an independent Planning and Booking product. Luvia
does not produce one card per portal and does not claim an impossible search of
every market source. The Booking Owner instead accepts normalized offers from
a small set of broad, approved supplier layers, deduplicates the same property
and compares provider prices only for equivalent stay/rate products.

The first compact result contains exactly three explainable choices: cheapest
complete comparable total, best flexible option and best personal fit. Required
taxes/fees, dates, party/rooms, currency, room/board/bed signature, freshness,
cancellation/payment conditions and provider reliability are explicit.
Commission is not a ranking input.

The local owner adapter now exposes `booking.v1.reads.compareStayOffers` over
the browserless `booking.stay-decision.v1` core. No live price is claimed until
an approved provider responds. Activation priority, owner application pack and
the KAYAK/Expedia/Booking/Hostelworld supply strategy are documented in
`M16.5-AFFILIATE-ACTIVATION-AND-HOTEL-DECISION-PLAN.md`.

The screenshot-confirmed currently reachable Hotel page is not the accepted
consumer for this foundation. Its turquoise frame, long form-first flow, small
stacked cards and repeated equal-weight actions belong to the retired design
world. A separate bright Demo-design migration is binding before Hotel/provider
activation: compact search, three decision lanes, large readable cards, total
price/flexibility/breakfast evidence, one primary next step, concise Chat mirror
and desktop/mobile/keyboard/reduced-motion acceptance over the existing Booking
Owner. No duplicate Hotel core will be created.

## Hotel live-price gateway — local acceptance on 2026-09-02

The Hotel lane now has an actual provider transport boundary rather than only
a normalized decision model:

- browserless `booking.live-stay-search.v1` accepts dates, occupancy, currency
  and a verified city/geocode/provider destination;
- `booking.v1.reads.searchStayOffers` is the only public Web entry;
- authenticated `booking-hotel-offer-search` invokes only providers whose
  connection, capability and activation state is ready;
- dedicated Amadeus and Hotelbeds Edge adapters normalize only live
  `provider_api` responses with mandatory total evidence;
- the additive service-only tables retain bounded search/offer evidence, not
  raw prompts, secrets or exact device position;
- the Hotel read `booking.stay.search` and the 24th AI runtime action, `booking.stay.offer.open`, consume structured
  whole-sentence meaning including dates, party, destination and coordinates.

Visible local Step‑17 evidence is green for the controlled product state:
default viewport and 390×844 both show `12.06.2027–14.06.2027`, two comparable
test-provider tariffs inside one Hotel card, an explicit no-market-best-price
limit and commission-free ranking. Switching to the no-provider state shows
`0 erfundene Preise`, keeps `fit_only`, produces no horizontal overflow and
emits zero browser warnings/errors. Stable `.136` additionally proves the
signed-in Berlin/date/occupancy sentence, the same truthful no-provider state,
390×844 without overflow and zero actual console errors. Stable/Immutable/archive
match 24/24 files. Real Amadeus/Hotelbeds credentials, external approval and
positive provider responses remain open.

The activation sequence is binding: Amadeus proof → Hotelbeds second-source
proof → KAYAK breadth application → Expedia Rapid qualification → Booking.com
Demand qualification. Affiliate links may monetize handoffs but never supply a
rate or influence Hotel ordering.

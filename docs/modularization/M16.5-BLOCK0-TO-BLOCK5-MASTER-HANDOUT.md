# M16.5 Arbeits-Handout — Block 0 plus fünf 10er-Blöcke

Date: 2026-09-02

Status: **PRODUCT RESET BINDING / B0 PUBLIC COMPLETE / B1 ACTIVE / APP 13.82.158 LOCAL CANDIDATE NOT YET PUBLICLY ACCEPTED / B2–B5 GATED**

This handout is the short operational index. The product order, Golden Journey,
capability maturity ladder and stop gates are binding in
`LUVIA-PRODUCT-RESET-MASTERPLAN-2026.md`. The canonical package wording and
owner-first technical order remain in
`LUVIA-FIFTY-POINT-OWNER-FIRST-EXECUTION-PLAN.md`. No item is complete because
code exists; the same visible owner, user-value, test and release chain is
mandatory for every coherent slice.

## Mandatory chain for every slice

`Owner Contract → browserless Core → public Owner Adapter → bright Living Sheet / Map / Timeline consumer → AI Chat → evals and failure modes → real Step 17 → commit → immutable Integration version → Stable/Immutable byte proof → exact Step-18 rollback`

## Actual program position

| Block | Packages | Current position | Next binding work |
|---|---:|---|---|
| B0 — Human ↔ AI control plane | B0.01–B0.10 | **Publicly complete on App 13.82.135 and retained as a live gate.** The live inventory now contains 330 semantic user actions, 246 public Owner paths, 24/24 typed runtime actions, 124 guarded writes and 903 audited source markers. | Every later human action must receive the same AI path or an equally honest unavailable state. |
| B1 — Productive AI and first agency | P01–P10 | **The `.152` contextual map and clean Place-detail slice is publicly accepted on Stable Integration. `.153` is the local correction after real `.152` operation proved the reservation action could look unresponsive and the user clarified the required submit boundary: opening Booking must not create a Timeline entry. Block remains active.** Earlier rejected versions remain counterevidence for all-card pin selection, grey map refresh, Hotel ticket semantics, cross-venue routing and weak Hotel identity. | Release `.153` only after complete regression; prove no Timeline mutation on route open, an exact post-submit requested entry for the embedded form, compact map/sheet pin navigation, visible route state and the upper detail layer; then operate one exact Restaurant route and one Activity/Culture route, close P09/P10 and complete the applicable Step-17 rows. |
| B2 — Personal robust planning | P11–P20 | **Queued after B1 exit.** Some S16 read projections exist, but positive mutation/provider acceptance is incomplete. | Route Uncertainty, Day Rehearsal, Disruption, Context Gate, Identity Preferences, confirmed Causal Feedback, Trip closure, Memory Story, Destination Twin and event-source authorization. |
| B3 — Verified event/calendar product | P21–P30 | **Queued behind P20 source authorization.** No synthetic public calendar data is permitted. | Verified Event Core, full Veranstaltungskalender, map-time brushing, Event-to-Memory, culture, serendipity, group divergence, weather substitution, cancellation/venue drift and live reconciliation. |
| B4 — Complete orchestration and safety | P31–P40 | **Queued.** Partial contracts/evals do not equal public acceptance. | Spatio-temporal graph, public S16.09–S16.12, complete event Chat, multilingual multi-intent, full mutation matrix, high-risk failures, entire Step-17 hardware/provider matrix, separate S16.07 decision, per-slice immutable releases and P40 Journey Multiverse. |
| B5 — Frontier travel operating system | P41–P50 | **Retained, not pre-authorized.** | Travel Constitution, provenance mesh, accessibility/energy twin, private group negotiation, sovereign offline brain, scene-to-action lens, destination pulse, regenerative optimizer, recovery war room and proof-of-journey vault. |

## Active B1 bundle: what is now implemented

1. Whole-sentence semantic owner routing; low confidence produces one short
   clarification instead of a guessed Places search.
2. Browserless `booking.admission.v1` for dining, lodging, attraction, culture,
   activity, event, transport, rental and other.
3. Truth states: free, not required, reservation supported/recommended/required,
   ticket available/required, timed entry and unknown.
4. `booking.v1.reads.resolveAdmission` plus provider catalog through the public
   Booking Owner adapter.
5. Canonical `booking.place.open`; restaurant Open survives only as a migration
   alias. `booking.stay.search` remains the Hotel read; `booking.stay.offer.open` is the 24th typed runtime action and opens only an identity-verified selected offer through
   `booking.v1.reads.searchStayOffers` through the same public Owner boundary.
6. Compact ticket/reservation statements in Chat cards/details, Places cards
   and Journey/Timeline suggestions.
7. Verified route order: connected API, official HTTPS ticket/reservation URL,
   provider handoff, then explicitly public+verified email with source URL.
8. Tiqets and Viator transaction-capable adapters exist but fail closed as
   `partner_required` until commercial access and authorized transport exist.
9. GetYourGuide, FareHarbor, Bókun, Regiondo, bookingkit, Xola, Checkfront,
   EVENTIM, Reservix and Ticketmaster are discoverable/handoff candidates, not
   falsely connected APIs.
10. Focused semantic, admission, consumer, provider-gate, Hotel live-price,
    Booking Owner, registry and runtime suites are green locally. The visible
    desktop and 390×844 Hotel test has zero overflow and zero console warnings/
    errors in both two-source and no-source states.

## Hotel decision and commercial activation now added

- Hotel selection is not a restaurant-style list and not one card per portal.
  `booking.stay-decision.v1` deduplicates the same property, rejects incomplete
  or stale bait prices and compares providers only for equivalent stay/rate
  products with complete mandatory charges.
- The compact user result has three explainable lanes: cheapest comparable
  total, best flexible option and best personal fit. Affiliate commission is
  excluded from every result-ranking input.
- A small set of breadth/depth layers replaces an unmaintainable “thousand
  providers” strategy: KAYAK metasearch, Expedia/Booking deep APIs after
  qualification, Duffel Stays as the first requested live-price path, Hotelbeds B2B evaluation,
  Expedia/Booking tracked handoffs, Hostelworld specialist supply and verified
  hotel-direct fallback.
- Live price is a hard product gate. Affiliate deeplinks cannot produce a price
  ranking; one successful live source permits only a named single-source view;
  cross-source comparison needs at least two live sources, and Luvia never
  claims a universal best-market price.
- `booking.live-stay-search.v1` now validates stay, occupancy and destination,
  invokes only readiness-approved Provider adapters and feeds only
  `source=provider_api` plus `live=true` responses into the decision core.
  The authenticated Integration gateway and additive service-only evidence
  schema are prepared for Duffel Stays, Hotelbeds and the retained Amadeus adapter. Duffel
  Stays was submitted by the owner on 2026-09-02, but remains
  `application_pending`; no access or credential is inferred. Missing credentials, partner
  status, destinations, taxes or response evidence produce `fit_only`, never a
  synthetic rate.
- `booking.affiliate-activation.v1` ranks eleven programmes with six immediate
  affiliate application candidates. Duffel Stays is separately recorded as
  `application_submitted_2026_09_02`; all other unsubmitted applications remain
  `not_started`/`paused`. No contract, credential or provider activation is invented.
- The screenshot-confirmed turquoise, form-heavy Hotel page remains rejected
  counterevidence. Its local successor now reuses the same Booking Owner in a
  dedicated bright Hotel surface: compact stay inputs, `TT.MM.JJJJ`, one
  source-labelled live total, concise flexibility facts and one primary
  `Dieses Angebot öffnen` action. It contains neither `Tickets prüfen` nor a
  generic Place-booking command. The real module passes the controlled desktop
  and 390×844 fixture with zero ticket labels, zero overflow and the exact
  selected property/offer receipt `HBX-9 · HBX-RATE-9`.
- An already normalized stay offer now survives the public Owner handoff
  unchanged. Property ID, offer ID, dates, occupancy, currency, mandatory total
  and verified provider URL are revalidated together; missing or foreign facts
  fail closed instead of falling back to a sibling Hotel or corporate page.
- The complete current evidence, ranking, owner application pack and three
  execution waves are binding in
  `M16.5-AFFILIATE-ACTIVATION-AND-HOTEL-DECISION-PLAN.md`.

## B1 Booking lifecycle parity now added

- `booking.lifecycle-policy.v1` is the single browserless decision for visible
  manage, modify, cancel, message and status-refresh capabilities.
- An official/partner HTTPS handoff is stored as a normal Booking record before
  the external window is consumed. Repeated clicks within the bounded window
  reuse the record instead of multiplying bookings.
- External completion remains `forwarded`/unconfirmed until provider or message
  evidence returns. A click is never reported as a completed booking.
- A provider chat button exists only for an actual verified email thread.
  Modify/cancel may bootstrap the already existing verified contact-discovery
  path, but arbitrary messages never guess an address.
- Tiqets includes prepared catalogue, availability, create, confirm, get order,
  get ticket and cancel adapter operations. Viator remains constrained to the
  officially authorized partner/merchant operations. Ticketmaster Discovery is
  search/handoff only. None becomes `connected` without partner activation,
  provider-specific Edge transport and live reconciliation evidence.

## `.139` Place-route counterevidence and `.143` integrity correction

Public `.139` operation disproved the generic route assumptions in three
concrete ways: Hotel cards displayed `Eintritt`/`Tickets prüfen`; a nightlife
reservation action could open an unrelated A-ROSA/Straubinger Hotel; and partial
Hotel-name overlap could pass as property identity. A safe HTTPS URL alone is
therefore insufficient.

The accepted `.143` correction first applies a Place-Kind gate: lodging never
inherits ticket/admission actions, and activity/nightlife is not persisted or
routed as restaurant merely because a reservation may exist. It then requires
the resolved URL or verified-email route to match the exact selected venue or
property through provider place ID, normalized name/address or a
provider-specific property/product identifier. Generic corporate pages,
sibling properties, weak name-token overlap and cross-venue targets fail
closed. Route-cache keys include Place and route identity and are invalidated
when those facts change.

The visible local Place/Booking fixture is **4/4 green** and the focused
identity/source/unit tests cover the concrete cross-venue negatives plus exact
OpenTable, TheFork, ticket and Hotel provider/property positives. The Edge
Function `booking-route-resolve` **2.7 is ACTIVE as version 13 with JWT
verification retained**. Runtime commit `11ff01ae`, Worker version
`a8db9b4a-ace5-4144-b86c-db61d9586ccf`, 100% Stable Integration traffic,
197/197 Safe Regression and 24/24 byte-identical public assets complete the
`.143` acceptance. Public Hotel search fails honestly when its Places provider
is unavailable. A compound Tonfink find/check sentence exposed explicit new AI
counterevidence without reopening the route-identity acceptance. The local
successor now treats the first clause as a Places read and `muss ich dort
reservieren?` as a Booking requirement read, without leaking internal field
names or turning the question into a reservation write.

## Remaining B1 acceptance sequence

The foundation release gates are complete: `.136` proves the universal
admission/Hotel foundation and `.138` closes the Chat-native Trip-selection
read counterexample. `.138` passed 189/189 regression, visible local and public
390×844 operation, commit `3beb33f3`, immutable/Stable deployment, 23/23 byte
proof and exact app-only rollback to accepted `.136`. Block 1 now continues in
this order:

Rejected evidence is preserved: `.140` passed public Booking Read/Open but a
fresh-chat Create sentence fell back to the Booking list because the target
resolver depended on a preceding Place card. It performed no external write and
was rolled back immediately to accepted `.139`. `.141` fixed that exact target
resolution and publicly passed fresh-chat Create, but was rejected because its
Modify confirmation hid the requested new date and time. Every preview was
cancelled and Stable returned to `.139`. `.142` rendered nested changes but was
rejected when a valid public Modify model shape placed date/time in typed
constraints rather than `timeWindow`; the compiler twice asked for `booking
change` and Stable again returned to `.139`. `.143` normalizes both structured
forms and is the current local 5/5 candidate; it is not accepted until the full
public release chain passes.

1. Keep the accepted `.138` Trip-selection and concise-clarification gates green
   as permanent whole-sentence-routing regression evidence.
2. Keep the publicly accepted `.139` Favorite/Unfavorite and Plan/Unplan
   Preview, confirmation, Owner-readback, Receipt and Undo paths green; carry
   the observed cross-surface Chat-subject hydration gap as an explicit AI eval.
3. Keep accepted `.143` plus Edge `booking-route-resolve` 2.7 as the exact
   rollback baseline for the current successor slice.
4. Release and operate the local whole-sentence requirement repair, exact Hotel
   property/offer handoff and bright Hotel surface on Integration. Prove the
   visible selected-offer identity, no ticket semantics, narrow/mobile layout,
   byte identity and exact rollback before accepting it.
5. Prove one real Restaurant reservation route and one real Activity/Culture
   admission route, including exact venue identity or an honest unavailable
   result.
6. Activate Hotel/Affiliate providers in the binding order: Duffel Stays named
   single-source proof after approval, Hotelbeds second-source proof, then KAYAK, Expedia Rapid
   and Booking.com Demand qualification. Affiliate handoffs remain non-price
   evidence.
7. Close P09/P10.
8. Complete the remaining Desktop, narrow/mobile, keyboard, reload/back,
   physical-touch and system-Reduced-Motion Step-17 rows.
9. Continue Blocks 2–5 in their binding package order.
10. Publish each coherent slice immutably with tests, public evidence, byte
    proof and exact rollback while keeping the action inventory and failure
    evals green.

## Binding provider activation order

1. **Duffel Stays:** application submitted on 2026-09-02. After Duffel grants
   Stays and the Integration secret is present, the minimal live probe must pass
   before search → complete rates → final quote can become the first named
   single-source proof. Booking/cancellation stay locked until the Booking Owner
   ledger, explicit confirmation, idempotency and reconciliation gates are live.
2. **Hotelbeds evaluation:** second independent source and first controlled
   cross-source comparison; requires verified destination/hotel identifiers.
3. **KAYAK application:** strategic breadth/metasearch application in parallel;
   never represented as available before approval.
4. **Expedia Rapid:** deep supply/terms/booking qualification after a truthful
   working Hotel surface exists.
5. **Booking.com Demand:** managed affiliate/API qualification; not confused
   with a normal affiliate link.
6. **Amadeus Hotels Enterprise:** retained as a prepared legacy adapter and
   possible later distribution contract, no longer assumed to be an immediate
   self-service production path.

Redirect affiliate programmes may be applied for in parallel, but no tracked
handoff is a live-price source. Credentials remain outside the repository and
provider activation is one immutable slice at a time.

## Provider activation is separate work

Tiqets and Viator are the closest transaction-API counterparts to the existing
TheFork/OpenTable-style architecture. Their local adapters are real interfaces,
not mocks, but commercial/API access, credentials, Edge transport, bounded
health diagnostics, idempotency, receipts, reconciliation and public E2E must
all be completed before `connected`. Official booking URLs are useful now;
they remain external handoffs rather than Luvia-created reservations.

## Canonical linked handouts

- `LUVIA-PRODUCT-RESET-MASTERPLAN-2026.md`
- `M16.5-BLOCK1-SEMANTIC-AI-UNIVERSAL-ADMISSION-HANDOUT.md`
- `M16.5-AFFILIATE-ACTIVATION-AND-HOTEL-DECISION-PLAN.md`
- `M16.5-AI-MUTATION-COVERAGE-MATRIX.md`
- `M16.5-HUMAN-AI-ACTION-PARITY-REPORT.md`
- `M16.5-STEP16-OWNER-FIRST-USP-AND-EVENT-BACKLOG.md`
- `M16.5-STEP17-E2E-MATRIX.md`
- `M16.5AB-RECOVERY-TRUTH-LEDGER.md`
- `../architecture/PCR-M16.5-B1-SEMANTIC-AI-UNIVERSAL-ADMISSION-BOOKING.md`

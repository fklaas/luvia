# M4.2 Exit Gate – Ownership & Cross-Core Repository Guardrails

## Release / Architekturstand

- App: **13.81.8**
- Core: **4.81.8**
- Meilenstein: **M4.2**
- Name: **Ownership & Cross-Core Repository Guardrails**
- Datum: **2026-08-15**
- Ausgangs-Commit: **52c70bdf5612f820334a7e8602e07449a359191c**
- Ausgangs-Meilenstein: **M4.1 – Parallel Repository Topology Foundation**
- Arbeitsbranch: **feature/platform-core**
- Runtime-Auswirkung: **keine**
- App-/Core-Versionserhöhung: **keine**

## Ziel

M4.2 macht die bereits in M2 definierte Domain- und Datei-Ownership für die parallele Entwicklung operativ überprüfbar.

Dabei wird ausdrücklich keine zweite Architektur-Wahrheit eingeführt.

Normative Grundlagen bleiben:

- `MODULE-OWNERSHIP.md`
- `FILE-OWNERSHIP.csv`
- `DATABASE-DOMAIN-MAP.csv`
- `CROSS-CORE-ACCESS.md`
- `GLOBAL-ACCESS-INVENTORY.csv`

M4.2 ergänzt darauf aufbauend Repository-Guardrails für Review-Sichtbarkeit und automatische Erkennung neuer Cross-Core-DB-Verstöße.

---

## 1. Ownership-Baseline-Gate

Die vorhandenen Ownership-Grundlagen wurden vor der Implementierung geprüft.

Verifiziert:

- [x] `MODULE-OWNERSHIP.md` bleibt normative Domain-Ownership-Quelle.
- [x] `FILE-OWNERSHIP.csv` deckt alle vorhandenen DB-Zugriffspfade der M1-Baseline ab.
- [x] `DATABASE-DOMAIN-MAP.csv` bleibt maschinenlesbare Tabellen-Ownership-Quelle.
- [x] `GLOBAL-ACCESS-INVENTORY.csv` bleibt historische M1-Diff-Baseline.
- [x] `CROSS-CORE-ACCESS.md` verbietet neue direkte Cross-Core-`.from(...)`-Zugriffe.
- [x] Bestehende Cross-Core-Zugriffe gelten als Migrationsschuld und dürfen nicht wachsen.

**Status: PASS**

---

## 2. FILE-OWNERSHIP-Konsistenz-Gate

Während M4.2 wurde konkrete Ownership-Drift gefunden und kontrolliert korrigiert.

### Navigation Registry

Vorher:

~~~text
app/navigation-registry.js,Consumer,Experience,...
~~~

Korrigiert auf:

~~~text
app/navigation-registry.js,Platform,Shared Runtime,Cross-product platform/runtime infrastructure
~~~

Begründung:

`MODULE-OWNERSHIP.md` definiert `app/navigation-registry.js` ausdrücklich als Platform-/Shared-Pfad.

### Fehlende M3 Contract Adapter

Vier reale M3-Adapter fehlten in `FILE-OWNERSHIP.csv` und wurden ergänzt:

~~~text
core/platform/identity-contract-adapter.js
core/platform/media-contract-adapter.js
core/platform/places-contract-adapter.js
core/platform/trip-contract-adapter.js
~~~

Zuordnung:

~~~text
Owner: Platform
Classification: Shared Runtime
~~~

Verifiziert:

- [x] Keine neue Ownership-Kategorie erfunden.
- [x] Normative M2-Ownership beibehalten.
- [x] Navigation Registry an Platform-Ownership angeglichen.
- [x] Vier vorhandene M3-Adapter maschinenlesbar ergänzt.
- [x] `git diff --check` nach der Änderung ohne Fehler.

**Status: PASS**

---

## 3. CODEOWNERS-Gate

Neu eingeführt:

~~~text
.github/CODEOWNERS
~~~

Ziel:

GitHub-seitige Review- und Visibility-Unterstützung für geschützte Repository-Bereiche.

Aktuell bestätigter Repository-Owner:

~~~text
@fklaas
~~~

Abgedeckte Bereiche umfassen:

- Platform / Shared
- Trip
- Places
- Booking
- Media / Memory
- Identity / Preferences
- Intelligence

Wichtige Regeln:

- [x] `CODEOWNERS` ersetzt `MODULE-OWNERSHIP.md` nicht.
- [x] Keine globale `* @fklaas`-Regel, die Ownership-Lücken verdeckt.
- [x] Shared Migration History wird nicht pauschal einem Domain-Owner zugeschlagen.
- [x] Social-Pfade werden vor S0 nicht spekulativ erfunden.
- [x] `sync/gallery.js` wird nicht als neuer Media-Write-Owner deklariert.
- [x] Booking-Edge-Functions werden über `/supabase/functions/booking-*/` geschützt.
- [x] `/supabase/functions/luvia-intelligence/` wird Intelligence zugeordnet.
- [x] Vorhandene referenzierte Pfade wurden auf Existenz geprüft.
- [x] `.github/CODEOWNERS` selbst ist geschützt.

GitHub Branch Protection beziehungsweise verpflichtende Review-Enforcement-Regeln wurden in M4.2 nicht blind aktiviert, da eine ungeprüfte Solo-Repository-Konfiguration den einzigen Repository-Owner blockieren könnte.

**Status: PASS**

---

## 4. DB-Ownership-Audit-Gate

Für die automatische Cross-Core-Prüfung wurden drei vorhandene maschinenlesbare Grundlagen miteinander abgeglichen:

~~~text
FILE-OWNERSHIP.csv
+
DATABASE-DOMAIN-MAP.csv
+
GLOBAL-ACCESS-INVENTORY.csv
~~~

Historische M1-Baseline:

~~~text
300 Supabase .from(...) Zugriffseinträge
~~~

Davon:

~~~text
261 auf in DATABASE-DOMAIN-MAP.csv gemappte Tabellen
39 auf dort nicht gemappte DB-Objekte
~~~

Für alle 300 historischen Zugriffseinträge existiert ein Eintrag in `FILE-OWNERSHIP.csv`.

Ergebnis für die 261 gemappten Zugriffe:

~~~text
235 Same-Domain
26 Cross-Domain
~~~

Verifizierte bestehende Cross-Domain-Schuld:

~~~text
Places -> Trip             10
Media/Memory -> Legacy      5
Intelligence -> Trip        5
Media/Memory -> Trip        4
Intelligence -> Places      2
--------------------------------
Gesamt                     26
~~~

Diese 26 Zugriffe werden nicht als erlaubte neue Architektur betrachtet.

Sie sind ausschließlich eingefrorene Bestands- beziehungsweise Migrationsschuld.

**Status: PASS**

---

## 5. Bestehende Cross-Core-Schuld

Die 26 gemappten Cross-Core-Zugriffe wurden konkret untersucht.

Betroffene Bereiche umfassen:

~~~text
core/context/journey-knowledge-graph.js
  -> places
  -> restaurants

core/recommendations/schedule-intelligence-service.js
  -> trip_schedule_events

sync/gallery.js
  -> gallery_photos

core/media/media-core.js
  -> timeline_events

core/places/place-lifecycle-resolver.js
  -> timeline_events

core/places/timeline-core.js
  -> timeline_events
  -> trip_members
  -> trip_schedule_events
~~~

Die vorhandene Architektur-Dokumentation bestätigt:

- `trip_members` ist Trip-Owned.
- `timeline_events` ist Trip-Owned.
- `trip_schedule_events` ist Trip-Owned.
- `places` und `restaurants` liegen im Places-Bereich.
- `gallery_photos` ist Legacy/Paris.
- `sync/gallery.js` ist nur Legacy-Kompatibilität.
- neue direkte fremde Tabellenzugriffe sind gemäß `CROSS-CORE-ACCESS.md` verboten.

**Status: PASS**

---

## 6. Nicht gemappte DB-Objekte

39 historische Zugriffseinträge zeigen auf DB-Objekte, die nicht in `DATABASE-DOMAIN-MAP.csv` enthalten sind.

Dabei wurden zwei unterschiedliche Klassen nachgewiesen.

### Booking Views / Projections

Folgende Objekte wurden anhand der SQL-Migrationshistorie als Views beziehungsweise Projections bestätigt:

~~~text
booking_integration_summary
booking_status_attribution_v2_summary
booking_correlation_conversion_summary
booking_return_orchestration_summary
booking_provider_connection_readiness_v6
booking_provider_availability_readiness_v1
booking_provider_reservation_create_readiness_v1
booking_reservation_mutation_reconciliation_queue_v1
booking_email_readiness_v2
booking_email_runtime_v2
booking_email_recovery_queue_v2
booking_monetization_provider_readiness_v1
booking_monetization_runtime_v1
booking_commission_runtime_v1
booking_provider_orchestration_readiness_v1
booking_route_decision_runtime_v1
booking_provider_runtime_health_v1
booking_route_failover_runtime_v1
~~~

Sie werden nicht fälschlich als Tabellen in `DATABASE-DOMAIN-MAP.csv` eingetragen.

### Historische / Legacy Tabellen

Zusätzlich wurden folgende reale Legacy-/Base-Tabellen identifiziert:

~~~text
budget_settings
reminder_status
custom_reminders
live_moment_status
daily_member_stats
~~~

Belege existieren über historische Setup-SQLs, `ALTER TABLE`, Grants, Policies und produktive Zugriffspfade.

Im aktuellen Repository existiert jedoch keine ausreichend belastbare separate M0-Produktionsinventar-Datei, um nachträglich künstlich den Evidence-Wert `M0 remote/base referenced` zu behaupten.

Deshalb wird keine unbelegte Evidenz erfunden.

Regel:

- bestehende unmapped Zugriffe sind eingefrorene Baseline,
- neue unmapped Zugriffe werden blockiert,
- bestehende unmapped Schuld darf nur gleich bleiben oder sinken.

**Status: PASS**

---

## 7. JavaScript-/TypeScript-Scope-Gate

Die historische M1-DB-Baseline besteht aus:

~~~text
183 JavaScript-Zugriffen
117 TypeScript-Zugriffen
~~~

Die 117 TypeScript-Zugriffe wurden separat geprüft.

Ergebnis:

~~~text
117 TypeScript DB-Zugriffe
111 auf gemappte Tabellen
6 auf bestätigte Booking Views / Projections
0 gemappte Cross-Domain-Verstöße
~~~

Die TypeScript-Zugriffe liegen fast vollständig in Booking Edge Functions sowie einmal in `luvia-intelligence`.

Der neue M4.2-Guardrail scannt deshalb gemeinsam:

~~~text
*.js
*.ts
~~~

**Status: PASS**

---

## 8. Scanner-Härtung

Der Scanner unterscheidet bewusst zwischen verschiedenen `.from(...)`-Formen.

Nicht als DB-Tabellenzugriff gewertet:

~~~text
Array.from(...)
storage.from(...)
~~~

Als DB-Zugriff gewertet werden die nicht als Array-/Storage-Zugriffe klassifizierten Supabase-`.from(...)`-Aufrufe.

Zusätzlich werden sowohl statische als auch dynamische Targets erkannt.

Aktuell verifizierter Repository-Stand:

~~~text
326 getrackte JS-/TS-Dateien
343 echte nicht-Storage DB-.from()-Aufrufe
316 statische Targets
27 dynamische Targets
~~~

**Status: PASS**

---

## 9. Erweiterte aktuelle Scanner-Baseline

Der neue Scanner prüft nicht nur die historische M1-Baseline.

Er scannt alle aktuell getrackten JS-/TS-Dateien.

Die 316 aktuellen statischen Zugriffe liegen um 16 Zugriffe über der historischen 300er-Baseline.

Die Differenz stammt vollständig aus:

~~~text
supabase-sync.js
~~~

Dort wurden zusätzliche historische Zugriffe gefunden auf:

~~~text
day_notes
favorites
gallery_photos
live_moments
~~~

`supabase-sync.js` fehlt vollständig in `GLOBAL-ACCESS-INVENTORY.csv`.

Die Datei ist jedoch in älteren Luvia-Audits ausdrücklich als Parallel-/Altpfad dokumentiert und wird von historischen HTML-Einstiegspunkten geladen.

M4.2 blendet diesen Pfad deshalb nicht aus.

Auch historische Legacy-Dateien werden zukünftig auf neue Ownership-Verstöße überwacht.

**Status: PASS**

---

## 10. Dynamische DB-Zugriffe

Aktuell existieren 27 dynamische `.from(variable/expression)`-Aufrufe.

Verifizierte stabile Baseline:

~~~text
1  core/booking/booking-reservation-mutation.js
   -> view

1  core/context/journey-knowledge-graph.js
   -> table

1  core/diagnostics/media-readiness.js
   -> table

5  intelligence/data-layer.js
   -> table

1  profile-center.js
   -> t

13 supabase/functions/booking-provider-reservation-mutation/index.ts
   -> table

1  supabase/functions/booking-provider-reservation-mutation/index.ts
   -> VIEWS[action]

1  supabase/functions/booking-provider-reservation-mutation-status/index.ts
   -> TABLES[action]

3  supabase/functions/booking-provider-reservation-reconcile/index.ts
   -> table
~~~

Regel:

- [x] Bestehende dynamische Muster werden als Bestandsbaseline eingefroren.
- [x] Ihre Anzahl darf sinken.
- [x] Neue dynamische Muster sind nicht erlaubt.
- [x] Bestehende dynamische Muster dürfen nicht wachsen.
- [x] Dynamische Targets werden nicht fälschlich als statisch ownership-verifiziert dargestellt.

**Status: PASS**

---

## 11. Automatischer M4.2 Cross-Core-DB-Guardrail

Neu eingeführt:

~~~text
tests/m4.2-cross-core-db-ownership-guardrail.test.cjs
~~~

Der Test arbeitet ohne zusätzliche npm-Abhängigkeiten und folgt der bereits vorhandenen direkten Node-`.test.cjs`-Teststruktur.

Der Guardrail verwendet:

~~~text
FILE-OWNERSHIP.csv
DATABASE-DOMAIN-MAP.csv
GLOBAL-ACCESS-INVENTORY.csv
~~~

und scannt den aktuellen Git-Stand selbstständig.

Er prüft:

- getrackte `.js`- und `.ts`-Dateien,
- statische Supabase-DB-Targets,
- dynamische Supabase-DB-Targets,
- Datei-Ownership,
- Tabellen-Ownership,
- historische Cross-Core-Schuld,
- historische unmapped DB-Objekt-Schuld,
- neue Dateien mit DB-Zugriff ohne File-Ownership.

Er blockiert:

~~~text
neuen Cross-Core Direct DB Access
Wachstum bestehender Cross-Core-Schuld
neuen Zugriff auf unbekannte DB-Objekte
Wachstum bestehender unmapped Schuld
neue dynamische .from(...) Zugriffsmuster
Wachstum bestehender dynamischer Muster
DB-Zugriffe aus Dateien ohne FILE-OWNERSHIP-Eintrag
~~~

Bestehende Schuld darf ausschließlich gleich bleiben oder sinken.

**Status: PASS**

---

## 12. Syntax-Gate

Ausgeführt:

~~~text
node --check tests/m4.2-cross-core-db-ownership-guardrail.test.cjs
~~~

Ergebnis:

~~~text
keine Ausgabe
Syntaxprüfung erfolgreich
~~~

**Status: PASS**

---

## 13. Positivtest

Ausgeführt:

~~~text
node tests/m4.2-cross-core-db-ownership-guardrail.test.cjs
~~~

Ergebnis:

~~~text
M4.2 cross-core DB ownership guardrail: OK
tracked JS/TS files: 326
static DB calls: 316
mapped cross-core debt: 26 / baseline 26
unmapped DB-object debt: 39 / baseline 39
dynamic DB calls: 27 / baseline 27
~~~

Damit akzeptiert der Guardrail den verifizierten aktuellen Repository-Stand.

**Status: PASS**

---

## 14. Kontrollierter Negativtest

Zur Funktionsprüfung wurde temporär ein künstlicher neuer Cross-Core-Zugriff erzeugt:

~~~text
app/navigation-registry.js -> bookings
~~~

Ownership:

~~~text
app/navigation-registry.js -> Platform
bookings                   -> Booking
~~~

Der Guardrail musste diesen Zugriff blockieren.

Ergebnis:

~~~text
M4.2 cross-core DB ownership guardrail failed.

New/increased cross-core direct DB access:
app/navigation-registry.js -> bookings = 1, allowed baseline = 0
~~~

Node Exit Code:

~~~text
1
~~~

Verifikation:

~~~text
ExpectedGuardrailFail = True
~~~

**Status: PASS**

---

## 15. Negativtest-Recovery-Gate

Vor dem Negativtest wurde `app/navigation-registry.js` bytegenau gesichert.

SHA-256 vor dem Test:

~~~text
910E7608EB4C182579F897BD54FDBAC7B849AAA79122367903BB3461C2BD1ED6
~~~

SHA-256 nach Wiederherstellung:

~~~text
910E7608EB4C182579F897BD54FDBAC7B849AAA79122367903BB3461C2BD1ED6
~~~

Verifikation:

~~~text
FileRestoredExactly = True
~~~

Damit ist bestätigt, dass der Negativtest keine Runtime-Datei verändert zurückgelassen hat.

Anschließend wurde der normale Guardrail erneut ausgeführt.

Ergebnis erneut:

~~~text
M4.2 cross-core DB ownership guardrail: OK
tracked JS/TS files: 326
static DB calls: 316
mapped cross-core debt: 26 / baseline 26
unmapped DB-object debt: 39 / baseline 39
dynamic DB calls: 27 / baseline 27
~~~

**Status: PASS**

---

## 16. Repository-Safety-Gate

Nach Negativtest und Recovery zeigte der Platform-Worktree ausschließlich die beabsichtigten M4.2-Arbeiten:

~~~text
 M docs/modularization/FILE-OWNERSHIP.csv
?? .github/CODEOWNERS
?? tests/m4.2-cross-core-db-ownership-guardrail.test.cjs
~~~

Keine temporäre Runtime-Änderung blieb zurück.

**Status: PASS**

---

## 17. Runtime-/Backend-Gate

M4.2 verändert ausschließlich Repository-/Architektur-Guardrails und Dokumentation.

- [x] Keine Consumer-UI geändert.
- [x] Kein Booking Runtime Code geändert.
- [x] Kein Domain Contract geändert.
- [x] Keine Runtime-Datei dauerhaft geändert.
- [x] Keine DB-Migration.
- [x] Kein SQL-Deployment.
- [x] Keine Edge-Function-Änderung.
- [x] Keine Secret-Änderung.
- [x] Keine Cloudflare-Production-Änderung.
- [x] Remote-only `luvia-media-delivery` unangetastet.
- [x] Kein Production-Deployment erforderlich.

**Status: PASS**

---

## 18. Versions-Gate

- [x] App bleibt **13.81.8**.
- [x] Core bleibt **4.81.8**.
- [x] Kein künstlicher Produkt-Versions-Bump für einen reinen Repository-/Architektur-Schritt.

**Status: PASS**

---

## 19. Source-of-Truth-Gate

- [x] Git bleibt Source of Truth.
- [x] `MODULE-OWNERSHIP.md` bleibt normative Domain-Wahrheit.
- [x] `CODEOWNERS` ist operativer GitHub-Guardrail und keine zweite Fach-Ownership.
- [x] `FILE-OWNERSHIP.csv` bleibt maschinenlesbare Datei-Zuordnung.
- [x] `DATABASE-DOMAIN-MAP.csv` bleibt Tabellen-Ownership-Map.
- [x] `GLOBAL-ACCESS-INVENTORY.csv` bleibt historische Debt-Baseline.
- [x] Der neue Test validiert vorhandene Maps und Baselines, statt eine parallele Domain-Wahrheit zu erzeugen.
- [x] ZIP-Dateien bleiben Release-/Handoff-Artefakte und keine parallele Entwicklungsbasis.

**Status: PASS**

---

## 20. Abgrenzung der nächsten M4-Schritte

Nicht Bestandteil von M4.2 sind:

- Feature-Flag-Konvention,
- Feature-Flag-Governance,
- wiederholbarer vollständiger Regression Harness für Parallelentwicklung,
- Integration-/Regression-Automatisierung,
- Cloudflare Preview-/Integration-Pfad,
- erster kontrollierter Feature-Branch-Test-Merge,
- finaler Nachweis `PARALLEL DEVELOPMENT READY`.

Diese Themen folgen in:

~~~text
M4.3 – Feature Flag & Regression Harness Foundation
M4.4 – Integration / Preview / Merge Proof
~~~

---

## 21. Exit-Entscheidung

Ownership- und Cross-Core-Repository-Guardrails:

~~~text
M4.2 TECHNICAL GATE: PASS
~~~

Verifizierter Zustand:

~~~text
normative ownership
+
corrected machine-readable file ownership
+
GitHub CODEOWNERS visibility
+
automatic JS/TS DB scanner
+
frozen historical cross-core debt
+
frozen unmapped DB-object debt
+
dynamic-access baseline
+
new-cross-core rejection
+
new-unmapped rejection
+
dynamic-access growth rejection
+
positive regression proof
+
negative enforcement proof
+
byte-exact recovery proof
=
OWNERSHIP & CROSS-CORE REPOSITORY GUARDRAILS ESTABLISHED
~~~

Der Gesamtmeilenstein M4 ist noch nicht abgeschlossen.

~~~text
M4.1 – Parallel Repository Topology Foundation: COMPLETE
M4.2 – Ownership & Cross-Core Repository Guardrails: TECHNICALLY COMPLETE
M4 – Parallel Development Foundation: IN PROGRESS
~~~

Der formale Git-Commit und dessen Remote-Verifikation erfolgen erst nach vollständiger Prüfung dieses Exit-Gates.